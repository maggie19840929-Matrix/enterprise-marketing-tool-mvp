# Codex 任务书：客户版 UI 精修 —— 高端营销站方向 V1

> 目标:把客户页(`/`,`#customerApp`)从"功能都在但偏密、偏 SaaS 后台"改成**高端营销公司官网**的精致感。
> **只改样式 + HTML 结构/信息层级 + 字体加载。严禁改任何功能逻辑**(见硬约束)。
> 视觉目标以对话里给出的 `customer_highend_landing_mockup` 为准。

---

## 0. "精致"在这里的定义(先对齐审美,再动手)

| 不要(现状问题) | 要(高端做法) |
|---|---|
| 首屏堆 6 层(hero+能力标签+结果卡+表单头+5步条+表单) | **一屏一焦点**:hero 只留 标题+副文案+1 个 CTA |
| 每个字段 4 层(编号+标题+输入+提示+3 示例胶囊) | 字段只留 **标题 + 输入**(提示改聚焦时/placeholder) |
| 紫 `#5e6ad2`+绿+橙 多强调色混用 | **单一强调色 + 中性灰**(默认走墨黑/近黑做主 CTA,彩色极少用) |
| 重投影 `0 24px 80px` | **发丝边(0.5–1px)+ 极轻或无投影**,编辑感 |
| 系统字随手用、无字重对比 | **精修无衬线大标题**(重字重+收紧字距+大字号,不靠换衬线) |
| 圆角 26/22/999 混、间距随意 | **一套 8px 间距 + 收敛到 2 档圆角** |

核心原则:**信息量可以不减,但同一时刻只让用户看该看的一件事。** 其余延后/折叠。

---

## 1. 字体(精致感第一杠杆)

**方向:中文大标题用"精修无衬线",不用中文衬线。精致靠 字重对比 + 收紧字距 + 大字号 + 留白 做出来,不靠换字体。**

- **中文大标题**(hero h1、各 section h2):优先直接用系统 **苹方 `PingFang SC`**(零加载、每台设备都清晰),`font-weight` 用较重字号(600/700)拉出字重对比,`letter-spacing:-.01em~-.02em`,`line-height:1.12`。若确需思源黑那种质感,**必须自托管字体子集**(只打包首屏用到的字,`woff2`,`font-display:swap`),**不得依赖 Google Fonts CDN**。
- **拉丁小标签 / 数字**(如 kicker `CONTENT GROWTH LOOP`、"3 分钟"):可用一个**拉丁衬线**做点缀(字符集极小、几 KB),给一点编辑感;中文主标题保持无衬线。
- 正文/输入/按钮:保持现有系统栈(`PingFang SC` 等),别动。
- 大标题字号收敛:hero ≈ 40–46px、`line-height:1.12`、`letter-spacing:-.01em`;section 标题 ≈ 22–26px。
- **硬性:禁止在大陆首屏依赖 Google Fonts CDN 加载中文整包字体**(站点 `fpmatrix.cn` 面向大陆,该 CDN 可能慢/被墙,整包 CJK 数 MB,易 FOUT,反而掉档次)。中文一律走系统字或自托管子集。

---

## 2. 调色 + 描边 + 投影

- **单一强调色**:全站主行动(主 CTA、选中态)统一走**近黑墨色**(如 `#111`/现有 `--war-text`)做底、浅色字;彩色强调**只在极少处**点缀(如激活的平台标签边),不要紫绿橙齐上。
- 描边统一:`0.5px`(或 1px)细线,弱对比(现有 `--war-line` 可保留但调更淡)。
- **投影**:去掉 `0 24px 80px` 这类重投影;卡片改**发丝边 + 白底**,最多一层极淡投影(`0 1px 2px rgba(0,0,0,.04)`)。
- 背景:大留白、干净底,不要多层半透明叠色。

---

## 3. 间距 + 圆角

- 间距走 **8px 体系**(8/16/24/32/48/64),section 之间留白拉大;卡片内 padding ≈ 32px。
- 圆角收敛到 **2 档**:卡片 `16px`、胶囊/按钮 `999px`。删掉 26/22 混用。

---

## 4. 结构 / 信息层级改造(逐块)

### 4.1 Hero(`.customer-hero.customer-product-hero`)
- 只保留:`kicker(小字母间距标签) + h1(衬线大标题) + 一句副文案 + 一个主 CTA + 一行微文案(如"3 分钟,无需注册")`。
- **移除/下移**:`.customer-proof-row`(业务诊断/内容计划/…标签行)、`.customer-hero-outcome`(结果预览卡)、右侧 `.customer-hero-product` aside —— 这些放到表单**下方**做一个安静的"能力/流程"小节,或直接精简掉。

### 4.2 进度条(`.customer-progress-strip`,5 步)
- **首屏不出完整 5 步条**。要么删,要么改成一条极细、安静的指示(如"第 1 步 / 共 5 步"小字);完整 stepper 等用户进入第 2 步后再出现。别在用户没动前就压 5 个按钮。

### 4.3 Brief 表单(`.customer-form-card` / `.customer-brief-field`)
- 字段只留 `标题 + 输入`。**删除**每个字段下的 `.customer-field-examples`(3 个示例胶囊)——改成聚焦时才提示,或并入 placeholder。
- 编号 `<i>01</i>` 徽章:删除,或改成极淡的行内小数字,不抢视觉。
- `.customer-brief-badges`(3 项核心信息/平台语境识别/…)已 hidden,保持隐藏或删。
- 保留但**安静化**平台组(`.customer-platform-choice`)和生成策略组:标签做成细边胶囊一行,不用大 fieldset 框。**平台和策略必须留在首页可见**(这是既定要求,别塞回折叠)。
- 其余字段仍在「补充更多(可选)」折叠里;底部一句引导"补充资质/素材/对标账号等可选信息 →"。

### 4.4 导航(`.customer-site-nav`)
- 精简:品牌 + 2–3 个安静链接;去掉多余装饰。

---

## 5. 硬约束(违反即不通过 —— 功能零回归)

**这一节最重要。最近修好的东西不许因样式改动而回归。**

1. **不改任何 JS 功能逻辑**。以下必须 100% 保留:
   - 客户数据持久化(localStorage、`customer_key` 不误判专属、刷新/重开恢复内容);
   - **多轮生成**(第二个七天按反馈变、不重复);
   - **平台选择生效**(选啥平台出啥,不乱铺);
   - 火山内容生成、脱敏/零代号泄露、「开始填写/修改」按钮行为。
2. **不得重命名/删除** 任何被 JS 依赖的 `name`、`data-customer-platforms`、`data-customer-content-mode`、`data-customer-problems`、`data-value`、`id`、`data-customer-step-*` 等钩子——**只能重排 DOM 与换样式**。
3. **平台 + 生成策略必须留在首页可见**(不许塞回折叠)。
4. **内部版 `/internal`** 不受影响、不降级。
5. `npm test` 通过:若改动了被测试断言的文案/class(如平台文案、`推荐模式：平台适配`、`建议适配，不强迫适配`、`可多选` 等),需同步更新对应断言,**但不得删除功能性断言**;能保留被测文案就尽量保留。
6. 不引入重型新依赖;字体只走 Google Fonts CDN(见 §1),`font-display:swap`,注意首屏性能。
7. 可部署到现 Netlify(`f0efe912-…`,禁用旧短 ID `9af65138`)。

---

## 6. 验收材料(必须逐条给)

1. 改了哪些文件(应集中在 `index.html` + CSS + 字体引入,JS 尽量不动或仅动样式钩子)。
2. **首屏对比**:改后首屏只剩 hero + CTA(+进入表单),无成排胶囊/徽章/结果卡堆叠——截图/说明。
3. 精修无衬线大标题(重字重+收紧字距)、单一强调色、发丝边、8px 间距 已落地;中文字体无 Google CDN 依赖。
4. **功能零回归自证**(逐条跑一遍并说明):
   - 填信息→生成→关浏览器重开,内容仍在;
   - 选抖音→7 天全抖音;
   - 记录反馈→第二个七天与第一个不同;
   - 客户视图零代号/内部字段泄露。
5. `npm test` 结果;`/api/health` 正常;浏览器控制台无报错。
6. 内部版 `/internal` 仍正常。

---

## 7. 一句话给 Codex
**这是"精修",不是"重写"。审美目标是高端营销官网:大留白、精修无衬线大标题(重字重+收紧字距,中文不依赖 Google CDN)、单色克制(近黑)、发丝边、一屏一焦点。功能一行都不许坏——尤其数据持久化、多轮生成、平台逻辑。改完务必按 §6 逐条自证功能没回归,再交付。**
