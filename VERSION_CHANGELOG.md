# 企业营销增长工具版本更新台账

## 版本号规则

- 当前从 `1.0` 开始，采用 `1.x` 功能版本 + `1.x.y` 修订版本迭代。
- `1.0`：最小可用闭环版本，能完成营销体检、诊断、7天内容计划、反馈回填、周复盘。
- `1.1`：在 1.0 基础上增加「推荐起步平台」，并区分本账号发布平台与目标客户可能适用平台。
- `1.2`：在 1.1 基础上增加「账号冷启动配置模块」，把发布前的账号定位、简介、头像方向、平台表达规则纳入门禁。
- `1.3`：在 1.2 基础上增加「内容决策局测试样例入口」和「首次发布链接回填门禁」，解决测试页初始空状态与发布闭环无链接凭证的问题。
- `1.4`：在 1.3 基础上增加「多时间点反馈回填」，支持 T+24 / T+72 / T+7 分阶段记录，并防止复盘统计重复叠加。
- 后续规则：
  - 同一功能版本内的小功能补齐、文案修正、字段门禁、QA 修复：采用 `1.x.y` 递增，例如 `1.3.1`；
  - 新增一个清晰功能模块或业务流程节点：采用 `1.x` 递增，例如 `1.4`；
  - 数据结构或业务流程明显变化：仍在 MVP 阶段先递增 `1.x`；
  - 形成可交付客户试用版、字段与数据回流稳定后，再进入 `2.0`。

## 更新记录

### v1.6.153 · 反馈保存即时响应版

日期：2026-08-15

1. 发布效果点击保存后立即进入保存中状态并锁定按钮，避免模型建议生成期间重复点击。
2. 反馈数据先保存再生成优化建议，慢模型或网络延迟不再阻塞数据落地。
3. 同一轮同一计划重复保存按更新处理，并统一同步计划状态、反馈与客户复盘记录。

### v1.6.152 · 项目恢复稳定版

日期：2026-08-15

1. 修复旧项目迁移函数返回不存在变量、导致私密链接和本地项目恢复中断的问题。
2. 恢复时保留原项目、7 条计划、计划 ID、反馈和内容轮次，并继续精确迁移已确认的旧错误选题文案。
3. 增加回归断言，防止项目状态再次因变量命名错误退回空白表单。

### v1.6.151 · 私密链接恢复诊断版

日期：2026-08-15

1. 私密项目恢复错误改为不含客户内容的纯文本诊断码，便于线上定位浏览器恢复失败原因。
2. 诊断信息不包含项目字段、访问令牌或客户隐私数据。

### v1.6.150 · 私密链接恢复修复版

日期：2026-08-15

1. 私密保存链接恢复失败时记录不含客户内容的最小错误证据，不再静默退回空白表单。
2. 保留当前匿名项目仓与旧匿名项目找回能力，继续以原项目、原计划和原轮次为恢复对象。

### v1.6.148 · 当前项目仓恢复版

日期：2026-08-14

1. 当前客户试用状态为空时，继续检查同一匿名 ID 下仍然存在的项目仓，不再误判为全新客户。
2. 恢复入口沿用原项目、原计划 ID、反馈和轮次数据，不重建、不删除任何云端项目。
3. 保留旧匿名 ID 找回与旧错误选题精确迁移能力。

### v1.6.147 · 旧匿名项目找回版

日期：2026-08-14

1. 当前匿名客户 ID 为空时，从同一浏览器的旧项目存储中发现最近项目，并显示明确的继续入口。
2. 点击继续后切回原客户 ID，恢复诊断、计划、反馈和轮次；点击新建空白项目则明确忽略该旧项目。
3. 保留 v1.6.146 的旧错误选题迁移，不重建、不删除云端项目。

### v1.6.146 · 项目恢复与旧文案修复版

日期：2026-08-14

本次修复：
1. 点击“继续这个项目”后真正恢复并重新渲染当前项目，同时关闭顶部恢复提醒。
2. 对已经保存的三条已确认错误旧选题做精确文案迁移，不改变计划 ID、反馈数据和内容轮次。
3. 保留旧地址栏状态的读取兼容，但停止把整份项目状态写入 URL，避免地址栏异常。

### v1.6.145 · 三平台账号起步设置版

日期：2026-08-14

本次更新：
1. 小红书、抖音、视频号分别生成账号名、定位、简介、主页重点、头像和背景图建议。
2. 客户明确选择的平台优先用于账号起步设置，不再机械使用推荐列表第一项。
3. 主页内容称呼按平台切换为置顶笔记、置顶视频或主页优先展示视频。
4. 账号头像与背景图生成继续复用当前项目，但使用对应平台的视觉方向。

### v1.6.144 · 账号起步设置排版修复版

日期：2026-08-14

本次修复：
1. 优化客户版「查看账号起步设置」的标签、正文和列表间距。
2. 增加分组留白与轻分隔，提升长文本可读性。
3. 手机端改为标签与内容上下排列，避免拥挤和重叠。

### v1.6.143 · 客户账号视觉生成版

日期：2026-08-14

1. 客户生成内容建议后，可直接生成与当前业务和账号定位匹配的小红书账号头像与主页背景图。
2. 图片生成沿用后端 GPT-Image-2 与异步任务链路，API Key、模型信息和内部任务字段不下发到客户页面。
3. 增加生成中、失败重试、结果预览、图片下载和刷新恢复，生成资产绑定当前客户项目，不串项目。
4. 账号头像按手机端小尺寸识别设计，主页背景为头像和简介预留安全区域，两者保持同一视觉语言。

### v1.6.142 · 生产链路与客户体验收口版

日期：2026-08-14

1. 首屏目标输入增加常见目标建议，同时保留自由输入；“平台不确定”和“不拍正脸”改成跨行业可理解的客户语言。
2. 客户生成结果新增轻量账号起步设置，补齐头像、背景图和“只能置顶已发布笔记”的小红书规则。
3. 小红书选题在模型提示词之外增加服务端 20 字硬校验，避免超限标题进入客户计划。
4. 成稿与成品链接复制在浏览器拒绝剪贴板权限时自动降级，失败时给出明确提示。
5. 延续 v1.6.141 的业务上下文、同批素材、多模态输入和任务幂等能力；同一图片任务重复提交不得重复调用模型。

### v1.6.141 · 业务上下文生产链路版

日期：2026-08-14

1. 内容生产任务由服务端自动关联客户业务、当前选题、历史反馈、对标信号和平台规则，减少运营重复粘贴与跨行业串线。
2. 同一选题下已生成的封面和配图可自动关联到后续脚本/文案任务；有可读图片地址时同时交给多模态文本模型理解。
3. 增加生成请求幂等键，网络重试不再重复创建任务；后台生成前会再次核验素材状态。
4. 修复生产工作台历史成功任务掩盖当前失败、生成耗时被更新时间重置、测试项目 ID 覆盖当前项目等状态问题。
5. 平台规则作为结构化生产上下文进入每次任务，不以通用模型回答替代客户经营数据与发布约束。

### v1.6.140 · 首轮选题语义质量修复版

日期：2026-08-13

1. 扩展线上营销咨询、内容增长工具等 B2B 营销服务识别，避免套入普通门店或培训行业视角。
2. 收紧小红书选题提示词，要求标题脱离正文也能独立看懂，不用模糊指代和空泛情绪制造网感。
3. 增加系统级标题质量门禁，拦截语义不完整、未经提供的自动发布暗示和非目标客户视角标题。
4. 不改变客户版页面结构、套餐边界、项目数据或完整成稿生产任务规则。

### v1.6.139 · 对标内容洞察内测版

日期：2026-08-11

1. 内部端新增独立「对标内容洞察」工作区，支持按客户和项目维护对标账号、代表内容、公开指标与截图证据。
2. 新增异步洞察任务、证据引用、行业隔离和人工审核门禁；模型失败或结构解析失败时明确失败，不以本地模板冒充市场洞察。
3. 审核通过的洞察可以生成一份不落正式项目的测试计划，用于检查建议是否更具体、是否串行业以及是否照抄来源标题。
4. 对标账号、代表内容、洞察与任务使用独立 Blob 集合，不迁移或修改现有客户项目数据。
5. 本期仅在 `/internal/benchmark-insights` 内部验证，不改变公开客户首页、客户生成流程、套餐额度或正式下一轮计划。

### v1.6.138 · 导航反馈增强版

日期：2026-08-10

1. 公开页面主导航增加统一的前置线性图标，悬停时文字与图标同步变色，点击进入后保留清晰的当前页面状态。
2. 移除转圈、进度条和状态提示等过度加载反馈，不改变现有页面结构、账号菜单和业务流程。
3. 已确认未登录时立即展示邮箱登录框，账号状态在后台静默刷新，避免慢网速下只看到空白加载提示。

### v1.6.137 · 商业订单与权益开通版

日期：2026-08-09

1. 套餐页新增账号订单、月付/年付选择和订单状态查询，价格由服务端套餐配置锁定，不接受前端传入金额。
2. 订单使用账号级幂等请求标识，网络重试不会重复创建；客户只能查看和取消自己的未付款订单。
3. 内部端新增受保护的商业订单确认区，运营核对到账凭证后可幂等开通 Plus 权益，重复确认不会重复延长订阅。
4. 订单、索引、请求幂等和审计日志使用独立 Blob 命名空间，不迁移或修改匿名客户项目数据。
5. 当前明确采用人工到账确认；微信支付与支付宝自动回调在取得商户凭据后进入下一阶段，不以假支付状态替代。

### v1.6.136 · 邀请奖励与权益闭环版

日期：2026-08-09

1. 新增账号专属邀请链接和独立邀请中心，展示已邀请、待生效、累计奖励、本月奖励及明细。
2. 好友必须通过专属链接首次注册并成功生成第一份内容建议，邀请人才获得 7 天 Plus 权益；单纯复制或打开链接不发放奖励。
3. 免费用户获得 7 天 Plus 权益，付费用户的当前会员顺延 7 天；每月最多奖励 28 天。
4. 邀请归因、奖励和订阅顺延全部由后端独立 Blob 记录并按被邀请账号幂等结算，不修改客户项目数据，也不依赖浏览器静态状态。

### v1.6.135 · 账号菜单与流程排版精修版

日期：2026-08-09

1. 所有公开页面统一读取真实登录状态，登录后右上角稳定显示“我的账号”，不再在套餐与信息页退回静态“登录”。
2. 新增成熟的账号下拉菜单，包含我的账号、真实剩余用量、邀请好友、设置与退出登录，并复用现有账号、权益及隐私接口。
3. 修复“方法与案例”增长路径被通用列表间距影响而出现的步骤和箭头错行问题。
4. 不改变账号认证、套餐额度、客户项目与内容生成逻辑。

### v1.6.134 · 登录与套餐排版精修版

日期：2026-08-09

1. 验证码登录态将“修改邮箱”收为字段标题右侧的轻量文字操作，不再使用突兀的胶囊按钮。
2. 套餐页缩短主标题并优化中文换行、字号、字重和卡片层级，避免“内容周期”出现孤字换行。
3. 登录、套餐与额度业务逻辑保持不变，仅提升公开页面的排版完成度。

### v1.6.133 · 套餐与额度地基版

日期：2026-08-09

1. 新增 Free / Plus / Pro 套餐配置与公开套餐接口，价格、策略周期、完整内容、每日生成和项目上限均从环境变量读取。
2. 新增账号及匿名客户的权益快照、独立用量账本与幂等额度预留；完整策略交付后核销，失败或限流时释放，不改动既有项目数据。
3. 首轮完整计划计 1 轮策略周期；反馈阶段仅在达到下一轮解锁门槛时预留 1 轮，单条效果记录不重复计量。
4. 账号面板新增本期策略周期用量和刷新时间，并新增独立“套餐与额度”页；不展示 Token、模型供应商或内部成本。
5. 商业化强制开关默认关闭。关闭时继续记录权益和用量，但不阻断现有匿名客户；开启后由服务端返回友好的额度不足提示。

### v1.6.132 · AI 内容策略定位版

日期：2026-08-09

1. 首页明确获客罗盘是会结合真实业务与发布结果持续调整的 AI 内容策略助手，而非固定内容模板。
2. 客户生成结果新增“本次建议已结合”动态依据，直观展示业务、目标客户、平台和当前问题。
3. 新增“方法与案例”页面，以平台内容机制、典型经营场景和增长路径说明内容营销的长期价值。
4. 场景内容明确标注为演示，不冒充真实客户案例；不展示模型供应商或技术参数，不改变内容生成、数据记录、账号与内部工作区逻辑。

### v1.6.131 · 品牌信息页排版精修版

日期：2026-08-09

1. 收敛关于我们及公开信息页的标题字号、字重和正文行宽，改善阅读节奏与品牌质感。
2. 去除信息页大卡片套小卡片的厚重结构，改用留白、细分隔线和克制的三栏价值表达。
3. 统一政策、协议和联系页面的编辑式排版；不改变主页、账号体系或客户业务流程。

### v1.6.130 · 信息页导航统一版

日期：2026-08-09

1. 顶部一级导航收敛为“使用工具 / 关于我们 / 登录”，联系方式下沉到关于我们内容与页脚。
2. 关于我们、联系我们、隐私政策和用户协议统一使用当前导航结构与样式版本，修复内页回到旧导航的问题。
3. 内页登录入口返回主页后直接打开登录窗口，不改变匿名使用、账号认证或项目数据逻辑。

### v1.6.129 · 顶部导航细节精修版

日期：2026-08-09

1. 收轻顶部导航字重与文字对比度，减少功能按钮感，保持清晰可读。
2. 缩短桌面端导航间距，并将登录胶囊收至更克制的高度、内边距和边框强度。
3. 不改变账号、项目、隐私设置或匿名使用逻辑；手机端继续保留足够的点击区域。

### v1.6.128 · 账号导航层级优化版

日期：2026-08-09

1. 顶部账号入口改为未登录显示“登录”、登录后显示“我的账号”，去掉含义模糊的“账号与项目”。
2. 项目列表和隐私设置收进账号面板，形成“账号 → 我的项目 / 隐私设置”的清晰层级。
3. 隐私设置不再占用一级导航；未登录用户仍可从页脚直接进入，不影响合规可达性。
4. 保留匿名直接使用、邮箱验证码登录、项目绑定和跨设备找回的既有逻辑，不迁移或修改客户数据。

### v1.6.127 · 账号与跨设备找回版

日期：2026-08-09

1. 客户页新增可选的邮箱验证码登录入口；匿名用户仍可直接完成原有内容增长流程，不设置登录墙。
2. 登录后可将当前项目绑定到账号，并在其他设备查看账号自身已绑定的项目清单和继续项目。
3. 服务端新增账号会话对已绑定客户数据的受限访问授权；未绑定客户桶仍被拒绝，不能通过 client_id 枚举或越权读取。
4. 新增退出登录、项目列表刷新和跨设备项目选择；账号界面不展示邮箱摘要、账号 ID、客户桶 ID 等技术信息。

### v1.6.126 · 验证码重发时区修复版

日期：2026-08-09

1. 修复验证码创建时间按北京时间保存、却被服务器按 UTC 解析，导致 60 秒重发限制被错误延长约 8 小时的问题。
2. 重发校验统一复用既有时区安全的 `timestampToEpoch()`，并新增北京时间无时区字符串的回归断言。
3. 不改变匿名客户流程、账号安全边界、邮件模板或客户页面结构。

### v1.6.125 · 账号与跨端找回地基版

日期：2026-08-08

1. 新增可插拔邮箱验证码、服务端账号会话、退出和会话撤销接口；生产默认关闭，未配置邮件服务时不影响匿名客户流程。
2. 新增账号与既有 `client_id` 的安全绑定：必须同时持有账号会话和原浏览器客户访问凭证，不复制、不迁移、不删除原项目数据。
3. 新增账号自身的跨端项目清单接口，只返回该账号已绑定的客户桶，禁止通过裸 `account_id` 或 `client_id` 枚举其他客户。
4. 邮箱身份仅保存 HMAC 摘要，会话使用同源 HttpOnly Cookie；测试验证码仅在 `NODE_ENV=test + AUTH_TEST_MODE=true` 时返回。
5. 商业化产品说明书正式锁定 Plus ¥299、Pro ¥899 邀请制，以及“策略周期 + 完整内容”的客户可理解计量方式。

### v1.6.124 · 客户身份边界加固版

日期：2026-08-08

1. 客户访问凭证统一覆盖计划生成、任务轮询、下一轮建议、隐私设置和旧反馈接口，裸 `client_id` 不再能读取或消耗他人额度。
2. 个性化推荐设置强制绑定当前已认证客户，不再信任请求中另行传入的其他客户设置 ID。
3. 保存链接仍可继续项目与记录效果，但不能修改项目所有者的隐私设置。
4. 旧项目浏览器认领增加截止时间开关 `CUSTOMER_LEGACY_CLAIM_UNTIL`，默认迁移窗口到 2026-09-30，到期后自动关闭可推导的旧认领路径。
5. 客户 API 响应明确设置 `Cache-Control: no-store`，避免项目数据被浏览器或中间缓存保留。

### v1.6.123 · 客户数据保护与生成等待修复版

日期：2026-08-08

1. 客户云端项目状态改为浏览器访问凭证保护；裸 `client_id` 不再能够读取或改写已有客户项目。
2. “保存链接”升级为 90 天有效、只允许恢复一个项目的 share token，不再暴露客户 `client_id`。
3. 兼容原浏览器中的既有项目：下一次正常同步时用本地项目证明认领访问凭证，不迁移、不删除现有项目数据。
4. 客户生成等待超时后不再覆写同一个计划任务为规则模板；保留原任务并提供“继续获取结果”，避免真实模型结果被静默替换。

### v1.6.122 · 内部项目交付协同地基版

日期：2026-08-04

1. 新增“专业项目交付型”和“持续增长运营型”两套交付模板，分别覆盖安标检测式专业项目与伊美德儿式门店增长运营。
2. 新增交付项目、周周期、协作任务、审批、拍摄安排、周报和飞书绑定 7 类独立 Blob 集合，不迁移、不合并现有客户项目数据。
3. 新增受 `INTERNAL_ACCESS_TOKEN` 保护的内部 CRUD 接口，并对项目归属、周期归属、字段所有权和状态流转做服务端校验。
4. 飞书 P0 仅建立项目级绑定与字段映射地基，明确标记 `binding_only`，不冒充已完成自动同步。
5. 增加两类客户、跨客户隔离、非法状态跳转、客户/内部字段权限和原项目数据不变的回归测试；客户公开页、公开应用版本和原有内容闭环保持在已验收的 v1.6.121。

### v1.6.121 · GPT Image 2 图片预览修复版

日期：2026-08-01

1. 修复内部素材工作台的前端 API 响应脱敏误改图片/视频 data URL，导致模型已成功出图但浏览器无法解码的问题。
2. 前后端脱敏器统一跳过 `data:image/...` 与 `data:video/...` 媒体数据，客户文案脱敏规则保持不变。
3. 增加前后端媒体 data URL 双重保护断言，继续沿用 `gpt-image-2` 作为封面与图文默认模型。

### v1.6.120 · GPT Image 2 接入修复版

日期：2026-08-01

1. 封面与图文的 OpenAI Images 默认模型由 `gpt-image-1` 升级为官方 API 型号 `gpt-image-2`。
2. 后端客户脱敏逻辑增加媒体 data URL 保护，避免 API 响应阶段改写图片与视频数据。
3. 增加服务端媒体 data URL 完整性回归测试，确保模型证据和客户脱敏继续生效且不破坏二进制成品。

### v1.6.119 · 图片后台生成稳定版

日期：2026-08-01

1. 封面与图文在配置真实 OpenAI Images 后改走现有 Netlify 后台函数，不再占用同步接口的短超时窗口。
2. 图片任务提交后立即返回生成中，后台最长允许 180 秒完成出图，页面沿用现有轮询自动显示成品。
3. 未配置图片密钥时仍保留明确标注的同步模拟产物，真实失败不静默伪装成功。
4. 新增真实图片后台点火、模型调用、成品落库和 QA 待验收状态的自动化回归测试。

### v1.6.118 · 多类型素材生成稳定版

日期：2026-08-01

1. 修复脚本/文案成稿没有图片或视频素材时，任务卡读取空媒体地址导致页面报错的问题。
2. 补齐脚本、文案、视频、封面、图文五种生成任务的服务端默认类型映射，避免文案或图文被误标为脚本。
3. 健康接口增加图片与视频 Provider 的非敏感配置状态和实际模型标识，明确区分真实模型与模拟适配器。
4. 增加图文任务、类型专属参数、默认类型和文字空媒体渲染的回归测试；客户版流程与视觉保持不变。

### v1.6.117 · 个性化推荐控制版

日期：2026-07-31

1. 客户页新增“隐私设置 → 个性化推荐/推送”开关，默认开启，支持主动关闭并在刷新后保持。
2. 新增 `GET/PATCH /api/user/settings`，按当前浏览器账户标识将设置保存到独立云端配置，不改已有项目数据。
3. 关闭后，首轮计划不再读取对标账号、历史内容偏好和账号偏好；下一轮建议不再读取历史轮次与历史反馈。
4. 非个性化模式仍保留通用规则、用户主动填写的业务条件、当前项目与当前发布记录，基础服务不中断。
5. 隐私政策补充关闭个性化推荐/推送的权利说明。

### v1.6.116 · 多类型生成与进度修复版

日期：2026-07-30

1. 脚本、文案、视频、封面和图片按类型展示各自需要的输入字段，不再共用尺寸、时长等无关设置。
2. “创建任务”收敛为一次点击创建并开始生成，减少内部运营的重复操作。
3. 生成中任务新增持续进度提示与主动检查入口；后台任务未点火或异常退出时可自动补触发并落明确失败状态。
4. 图片与视频任务生成后直接显示成品预览或可复制链接，模拟产物也明确标注，不再只有技术状态。
5. 项目关联、参考素材、历史任务和技术信息继续默认折叠；客户版页面与业务主流程不变。

### v1.6.115 · 生成流程收敛版

日期：2026-07-29

1. 内部 Kimi 脚本任务生成期间每 5 秒自动刷新，生成完成后自动显示成稿，不再停留在旧的 generating 画面。
2. 视频任务在自动刷新时同步推进异步轮询，脚本与视频统一使用可理解的中文状态。
3. 内容生产工作台收敛为“创建内容 → 查看成稿 → 验收/交付”主流程。
4. 参考素材、关联 ID、模型技术字段、历史任务和飞书调试默认折叠，避免调试信息占满页面。
5. 任务卡只显示当前状态可执行的按钮，客户版页面和主流程保持不变。

### v1.6.114 · Kimi 成稿预览版

日期：2026-07-29

1. 内部素材生成工作台的脚本/文案模型标识由旧的 Claude Opus 更新为实际使用的 Kimi K2.6。
2. 脚本生成完成后直接在任务卡展示成稿全文，无需再通过 Console 或素材接口查找。
3. 增加“复制成稿”按钮，并提供不支持 Clipboard API 时的兼容复制路径。
4. 成稿完整性结果默认折叠展示，包括首稿问题、自动续写轮次、完整重写和模型调用次数。
5. 本次仅调整内部工作台展示与版本标识，客户版页面和业务主流程不变。

### v1.6.113 · Kimi 成稿完整性修复版

日期：2026-07-29

1. Kimi 后台出稿新增完整性守门，识别模型 token 截断、未闭合 Markdown、悬空标题、冒号及短列表残片。
2. 首稿不完整时最多自动续写两轮，并对断点文本做重叠去重与标点衔接，避免重复整段内容。
3. 续写仍无法完整收尾时自动从头重写一次，并使用更高输出预算要求模型压缩但完整交付。
4. 自动修复全部失败时任务明确进入 failed，不生成输出素材、不进入 QA；内部保留具体失败原因和完整性证据。
5. 正常首稿、自动续写、完整重写及最终失败四条路径均增加冒烟回归，客户版流程和 UI 不变。

### v1.6.112 · Kimi 后台异步出稿

日期：2026-07-28

1. 脚本与文案生成任务改为 Netlify Background Function 执行，提交后立即进入生成中，不再占用同步函数响应时间。
2. Kimi 后台调用支持独立长超时、瞬时过载重试和指数退避，生成完成后将成稿持久化到任务输出素材并进入内部 QA。
3. 后台入口增加独立签名校验，默认复用内部访问令牌；缺少令牌或非法请求会拒绝执行，不暴露模型凭据。
4. 增加后台任务执行锁与终态幂等保护，重复点火不会重复调用模型或重复生成输出素材。
5. 点火失败会写入明确任务状态和原因，避免任务永久停留在“生成中”。

### v1.6.105 · 飞书阶段C协同写入版

日期：2026-07-17

1. 新增受内部口令保护的 `POST /api/feishu/push`，按指定 `client_id + project_id` 将项目内容计划批量写入飞书排期表。
2. 以真实内容计划 ID 做 upsert：首次批量创建、重复推送批量更新，避免产生重复排期行。
3. 新增 Bitable 日期、单选、文本与超链接反向格式化，并对缺凭据、缺表 ID、缺写权限保持 fail-closed。
4. 内部项目页新增“飞书协同”区，显示推送/拉取时间、同步汇总和可选飞书工作区入口；客户页保持不变。
5. 阶段 A webhook 与阶段 B 主动拉取继续保留，并补齐匿名 401、跨客户隔离、字段格式和写入幂等冒烟测试。

### v1.6.104 · 飞书阶段B实际字段兼容版

日期：2026-07-16

1. 飞书主动拉取兼容真实表格中的“曝光量”等常用指标列名，避免有效数据被归一化为 0。
2. 补齐咨询数、预约数及运营观察等同义字段，不改变阶段 A、阶段 B 的权限、幂等和项目隔离逻辑。
3. 冒烟测试改用真实“曝光量”列名，继续覆盖分页拉取、重复更新、Wiki 解析和定时同步。

### v1.6.103 · 飞书 Wiki 表格自动解析版

日期：2026-07-16

本次更新：
1. 飞书主动拉取同时支持独立 `/base/` 多维表格和知识库 `/wiki/` 多维表格，保留现有 Base Token 配置方式。
2. 新增 `FEISHU_WIKI_NODE_TOKEN`；未配置 Base Token 时，后端以应用身份读取知识库节点并将 `obj_token` 解析为真实 `app_token`。
3. Wiki 解析结果在函数实例内短时缓存，避免每次定时拉取重复查询；接口仅返回 `token_source`，不暴露 Token 或应用凭据。
4. 显式请求中的 `app_token` / `wiki_node_token` 优先于环境变量，便于内部按需验证不同表格；原有幂等入库、定时拉取和安全网关保持不变。

### v1.6.102 · 飞书多维表格主动同步版

日期：2026-07-15

本次更新：
1. 新增受内部令牌保护的 `POST /api/feishu/pull`，通过飞书开放平台 Bitable API 主动分页读取效果、打卡与口碑三类多维表格。
2. 抽取阶段 A/B 共用的飞书记录入库核心，继续使用记录 ID 幂等更新；重复全量拉取不会重复累计反馈、审计记录或运营指标。
3. 新增文本数组、毫秒时间戳、超链接等 Bitable 富字段抽取，写入项目前统一转为可读标量。
4. 新增每 15 分钟运行的 Netlify Scheduled Function；缺少应用凭据、Base Token 或表 ID 时 fail-closed，只记录明确跳过原因且不写数据。
5. 保留阶段 A webhook、安全隔离、项目不存在 404 保护、异步生成、多轮循环、计量限流与客户响应脱敏逻辑。

### v1.6.101 · 客户证据策略框架版

日期：2026-07-15

本次更新：
1. 在不新增客户页面的前提下，增加统一的客户证据策略上下文，结构化保留客户原话、购买异议、真实素材、对标市场校准和平台任务。
2. 首轮与后续轮次的豆包提示词统一读取该上下文，要求选题优先回应真实客户表达，并用现有素材支撑案例、过程和信任内容；证据不足时禁止编造结果。
3. 每条计划增加内部策略质量记录，明确客户依据、证据强度、平台角色、可验证假设和数据一般时的调整规则；这些内部字段不进入客户公开 API 响应。
4. 现有“为什么这样发”折叠内容补充客户原话、可用证据和数据调整依据；内部计划卡增加策略证据验收项，首屏与客户主流程保持不变。
5. 保留 Doubao-Seed-2.1-Turbo、异步生成、多轮循环、平台审核、脱敏、计量限流与云端项目数据结构。

### v1.6.100 · 2.1 Turbo 模型切换版

日期：2026-07-15

本次更新：
1. 将客户公开版首轮计划与后续周期建议的默认火山方舟模型统一切换为 Doubao-Seed-2.1-Turbo Endpoint。
2. 后续周期建议改为结构化 JSON 输出、关闭深度思考并提高输出预算，减少完整 7 天计划被截断后回退模板的情况。
3. 将后续周期模型调用默认超时调整为 15 秒（最高可配置 18 秒），在响应速度与生成成功率之间保持稳定余量。
4. 保持客户页面、业务主流程、内容安全门禁和规则模板兜底逻辑不变。

### v1.6.99 · 获客罗盘产品标识接入版

日期：2026-07-15

本次更新：
1. 依据正式品牌组合规范新增“指南针大象”获客罗盘产品标志，并以轻量矢量资产落地。
2. 首屏新增“获客罗盘 by FP Matrix”产品标识，明确母品牌与产品品牌的层级关系。
3. 导航、页脚和 favicon 继续使用 FP Matrix 母品牌标志，页面结构与业务流程保持不变。

### v1.6.98 · FP Matrix 品牌标识定稿版

日期：2026-07-15

本次更新：
1. 按正式品牌规范启用 `#F23B49` FP Matrix 大象标志，并优化图形比例与视觉重心。
2. 将品牌字标统一为中等字重的 MATRIX 与收紧后的中文定位，导航、页脚和独立信息页保持一致。
3. 新增方形安全区 favicon，不再使用横向主标志充当浏览器小图标。
4. 保持获客罗盘页面结构、功能流程、业务文案和内部工作区逻辑不变。

### v1.6.97 · 品牌信息页排版修正版

日期：2026-07-15

本次修复：
1. 为“关于我们”等独立信息页增加导航栏与内容卡片之间的稳定留白，避免主卡片紧贴页眉。
2. 移除品牌导语旧有的 760px 窄宽度限制，使用卡片完整内容宽度，避免右侧有空白时提前换行。
3. 手机端保持较紧凑的 16px 顶部间距，并继续允许自然折行；文案、配色、卡片和业务逻辑均未修改。

### v1.6.96 · 品牌叙事完善版

日期：2026-07-15

本次更新：
1. 重写“关于我们”核心叙事，从功能说明提升为 FP Matrix 的第一方增长理念与品牌主张。
2. 明确 `FP = First Party`、`Matrix = 将分散业务信号组织成持续学习的增长系统`，建立名称与产品方法的联系。
3. 将原“我们做什么 / 我们不做什么”调整为“第一方真实 / 持续学习 / 智能辅助”三项品牌原则。
4. 保留原页面结构、视觉体系、服务主体与合规信息，不改客户业务流程和内部端能力。

### v1.6.95 · FP Matrix 品牌升级版

日期：2026-07-15

本次更新：
1. 在保留原“大象头 + FP”品牌寓意的基础上，将标志升级为适合网页与移动端清晰显示的矢量版本。
2. 网站页头、页脚、浏览器图标及独立信息页统一使用 `FP Matrix｜企业第一方增长智能` 品牌组合。
3. 首页主张统一为“获客罗盘，让每一次发布都成为下一次增长的依据”，强化主品牌与产品能力的层级关系。
4. 仅更新品牌识别与对应测试，不改页面结构、业务流程、内容生成、效果记录或内部端能力。

### v1.6.94 · 时间戳一致性修复版

日期：2026-07-13

本次修复：
1. 云端项目写入与同名项目归一化统一按真实时间比较，兼容 `YYYY-MM-DD HH:mm:ss`、ISO `Z/offset` 和秒/毫秒时间戳。
2. 无时区业务时间固定按 Asia/Shanghai 解析；任一时间无法解析时优先保留本次写入，避免格式异常导致静默丢数据。
3. 真正较旧的写入不会覆盖较新项目；客户列表、合并预演、反馈最新阶段和前端本地/云端合并同步使用统一比较规则。
4. 新增混合格式覆盖、旧写入保护、非法时间兜底及飞书回流落库读取回归测试；飞书鉴权、隔离、幂等和既有业务流程保持不变。

### v1.6.93 · 飞书回流阶段A版

日期：2026-07-13

本次更新：
1. 新增严格鉴权的 `POST /api/feishu/inbound`，无令牌或错误令牌统一拒绝，不形成公网无鉴权写入口。
2. 回流只在请求指定的 `client_id + project_id` 范围内查找与写入；可用 `x-feishu-client-id` 绑定自动化，防止跨门店误写。
3. 飞书效果记录幂等对齐现有 `feedback / records`，每日打卡与口碑任务分别进入专用集合，并保留统一审计记录。
4. 现有 `/api/feishu/sync` 支持配置群机器人 webhook 后真实推送；缺少 webhook 时只返回人工导入 payload，并明确标注未同步。
5. 新增 401、403、404、跨客户隔离、记录幂等、效果/打卡回流和 webhook 真实请求 smoke test；客户主流程、异步生成、多轮、计量与现有项目数据结构保持不变。

阶段边界：本版本仅完成阶段 A。阶段 B 的飞书自建应用与 Bitable API 自动双向同步需在提供应用凭据和表标识后独立实施、独立部署。

### v1.6.92 · 首屏无闪烁修复版

日期：2026-07-13

本次修复：
1. 客户首页在 HTML 首次绘制前直接启用 `customer-mode`，避免先显示旧深色背景再切换浅色产品页。
2. `/internal*` 路径在 body 解析起点同步切换到 `internal-mode`，不改变内部端既有视觉与鉴权流程。
3. 后续模式状态仍由现有 `setAppShell()` 统一接管；业务流程、内容生成、平台规则、多轮、计量与持久化均未修改。

### v1.6.91 · 视频号语感增强版

日期：2026-07-12

本次更新：
1. 补强视频号平台规则，明确负责人/老板口播、真实案例复盘、成熟受众、干货与情感共鸣的表达方向。
2. 增加微信生态内群聊、朋友圈和好友社交推荐的转发逻辑，选题可适度强调值得收藏或转发。
3. 明确公众号、社群、企业微信及私信等私域承接方向；小红书、抖音及其他平台规则保持不变。
4. 字段结构、平台映射、多轮、脱敏、异步任务、计量限流和持久化均保持不变。

### v1.6.90 · 小红书语感与 CTA 精修版

日期：2026-07-12

本次更新：
1. 收紧 7 天计划 CTA 提示词，要求 14 字以内、完整口语句、动作多样，禁止叠词和“引导客户/家长”式内部表达。
2. 在计划入库前增加容错去噪，清理“咨询咨询”“咨询问”“点击咨询”等语病，并按业务场景轮换保存、主页咨询、预约、到店、截图、了解等安全动作。
3. 仅对小红书加强轻情绪钩子、数字清单感和真实口语表达，清理“一文讲清”“干货整理”等教科书式标题；抖音和视频号继续使用各自语感。
4. 同一套 CTA 清洗与动作轮换复用于后续轮次，字段结构、平台映射、多轮、异步任务、计量限流、脱敏和持久化保持不变。
5. 新增财税代理、美甲、推拿、少儿篮球质量回归，以及小红书语感不外溢到视频号的自动测试。

### v1.6.89 · 成本保护与漏斗埋点版

日期：2026-07-11

本次更新：
1. 为异步内容计划与下一轮建议增加按匿名客户和 IP 的短窗口、每日计量，首版默认影子模式，只记录潜在限流，不阻断真实试用。
2. 以 `request_id` 绑定 reservation 和确定性任务 ID，网络重试不重复预留；产品使用量只在结果成功交付时记录一次。
3. 将 `SAFE_TO_RUN` 前置到 Ark、OpenAI、Anthropic、GLM 和 Seedance 的真实模型调用前，关闭时保留可渲染规则或 mock 兜底，不产生付费调用。
4. 新增首页访问、开始填写、提交生成、生成结果、记录效果、进入下一轮六个无 PII 漏斗事件，并使用独立 Blobs 命名空间保存。
5. `/api/assessments` 改为 `INTERNAL_ACCESS_TOKEN` 专用；客户主流程继续使用异步 `/api/plan-jobs`，匿名内测、项目数据、多轮和持久化保持不变。
6. 模型输出若夹带客户未提供的优惠/效果承诺，先做可审计的局部安全清理，不因单个词把整批 Ark 内容降级为模板。
7. Ark 7 条结构化计划的输出预算提升到 1400 tokens，避免 JSON 截断造成 `partial_parse` 回退。
8. 若 Ark 首次 JSON 仍无法解析，保留已完整返回的 AI 选题，只对缺少条目进行 1 次低温度、9 秒上限的结构修复；两次真实 provider attempt 均进入成本计量，产品额度仍只在最终交付时计 1 次。
9. 仅对 Ark 瞬时网络异常、超时、429 或 5xx 允许 1 次有界重试；缺 Key、缺模型或 `SAFE_TO_RUN=false` 等配置错误不重试。

部署护栏：生产环境必须同时配置 `SAFE_TO_RUN=true`；`RATE_LIMIT_ENFORCE=false` 保持影子模式，观察数据后再人工切换强制拦截。

### v1.6.88 · 客户内页收尾清理版

日期：2026-07-11

本次收尾：
1. 将客户浅色页预览 kicker 统一为高对比深墨色，避免淡紫小字影响可读性。
2. 将仍在使用的结果页次要入口改为墨色文字、常规字重和实线发丝边，移除隐形白字与旧虚线胶囊风险。
3. 将计划卡选中态统一为白底深墨边，清除旧通用规则残留的绿色 tint。
4. 将客户区区块 kicker 和计划 kicker 加深为 `#667085`，确保小字达到 AA 对比度。
5. 所有修正仅作用于客户浅色页；内部深色页、安全、平台、多轮、异步任务和持久化逻辑保持不变。

### v1.6.87 · 客户内页设计统一版

日期：2026-07-11

本次更新：
1. “确认方向”改为竖排单选行卡，使用明确的空心/实心圆点与勾选态，独立保留唯一深色主行动。
2. 结果建议、平台矩阵和选题列表统一为白底发丝边、中性文字和轻量数字标记。
3. 内容计划改为圆形日期编号、灰色平台胶囊、标题与角度两级信息，并默认展示前三条、渐进展开全部七天。
4. “为什么这样发”、记录效果和下一轮建议统一为低调折叠行与白底发丝边结构，移除大面积绿色 tint 和重阴影。
5. 所有变更仅作用于客户浅色页；方向钩子、计划 ID、反馈、多轮、异步、安全和内部深色页逻辑保持不变。

### v1.6.86 · 对比度收尾版

日期：2026-07-11

本次收尾：
1. 客户浅色页彻底移除残留的 `#bdf7df` 淡绿文字，统一使用 `#065f46` 深绿墨。
2. “补充更多”摘要和旧计划日期态由低对比淡紫改为 `#312e81` 深紫墨。
3. 内部深色页通过内部作用域保留原有等效淡紫色值，不改变 war-room 视觉。
4. 保留“最大内容问题”无必填星号，并保持安全、多轮、异步、持久化和 P0/P1 功能不变。

### v1.6.85 · P0颜色与文案补漏版

日期：2026-07-11

本次补漏：
1. 将客户效果区已选计划提示残留的 `#bdf7df` 改为深绿墨 `#065f46`。
2. 将客户建议编号徽标的低对比浅紫 `#d8dcff` 改为深紫墨 `#312e81`。
3. 去掉“你当前最大的内容问题是什么？”标题中多余的必填星号，不改变字段默认值与生成逻辑。
4. 保留内部深色主题、计划差异化、失败重试、门槛文案、安全隔离和多轮持久化逻辑。

### v1.6.84 · P0可读性与低门槛修正版

日期：2026-07-11

本次返工：
1. 不再只依赖末尾覆盖，直接将客户浅色作用域内计划日期、平台标签、记录状态和下一轮提示的淡绿色文字改为 `#065f46`。
2. 保留内部深色主题 `.war-tag.green` 的 `#8ff2cb`，客户与内部配色边界明确分开。
3. 客户首访主区严格只展示行业、目标、目标客户三项；平台、内容生成方式和最大问题移入默认关闭的“补充更多（可选）”。
4. 客户效果记录主区严格只展示曝光、互动、咨询；点赞、评论、转发位于默认关闭的“更多（可选）”，默认值为 0。
5. 保留 v1.6.83 已完成的计划差异化、失败重试和鼓励式下一轮文案，不修改 P1 行为。

验收：
- 客户首访表单 `required` 字段仅 3 个；
- 三项核心效果数据可独立保存并推进本条优化建议；
- 客户浅色计划标签和下一轮区域关键文字对比度不低于 AA 4.5:1；
- 内部深色主题、安全门禁、异步任务、多轮与持久化保持不变。

### v1.6.83 · 可读性与循环体验优化版

日期：2026-07-10

本次优化：
1. 客户浅色页的绿色标签、计划日期、成功提示和下一轮建议统一改用深绿墨文字，绿色仅保留为浅色点缀，正文达到 AA 对比度。
2. 发布效果默认只填写曝光、互动、咨询三个核心指标；点赞、评论、转发收进“更多（可选）”，空值按 0 兼容原有循环逻辑。
3. 首次录入保持三项必填，平台默认“还不确定”、最大问题默认“不知道发什么”，客户可直接进入方向确认。
4. 计划模型加入按客户任务轮换的选题切口和轻度温度提升，降低同行业重复；规则兜底同步轮换安全模板顺序。
5. 异步生成失败后提供原地重试按钮，复用上次已确认的信息，无需重新填写。
6. 下一轮门槛改为鼓励式提示：“再记录 X 条，就能解锁更准的下一轮建议”，门槛逻辑保持不变。

验收：
- 仅填写三项核心信息即可生成，平台和最大问题使用安全默认值；
- 三个核心效果指标可独立保存并推动本条建议，选填明细为空不报错；
- 客户计划任务继续按 client_id 隔离，内部接口令牌保护与客户模型字段脱敏保持不变。

### v1.6.82 · 客户计划异步生成版

日期：2026-07-10

本次优化：
1. 客户首轮计划改为提交 `plan-jobs` 后轮询结果，提交请求立即返回，不再等待模型完成。
2. 计划任务按 `client_id` 写入独立 Blobs 键，只能读取本客户持有的 `job_id`，不提供客户任务列表。
3. 使用 Netlify `context.waitUntil()` 在响应发出后继续生成，慢模型在 19 秒硬超时后写入规则兜底结果。
4. 前端轮询有次数上限与退避；超时后主动请求安全规则结果，再沿用原有项目保存、云同步和渲染链路。
5. 内部 `/generation-tasks*` 安全门禁保持不变，客户响应继续移除模型与供应商字段。

验收：
- 创建任务返回 202，慢模型不会让创建请求出现 504；
- 同 client_id 可取回 7 条计划，跨 client_id 返回 404，无 client_id 返回 400；
- `/api/plan-jobs` 不提供列表，`/api/generation-tasks*` 继续要求内部令牌。

### v1.6.81 · 生成延迟一期优化版

日期：2026-07-10

本次优化：
1. 内容计划生成新增 `ARK_PLAN_MODEL` 专用轻量模型配置，不影响后续建议和其他模型链路。
2. 模型只生成选题、角度、内容类型和行动引导四个核心字段，其余计划字段由规则稳定补齐。
3. 精简模型上下文并限制字段长度，降低首轮七条计划的输出 token 与解析风险。
4. 模型调用默认超时收紧至 19 秒，超时后返回规则模板，避免客户页面出现 504。
5. 客户等待按钮分阶段提示“分析业务 → 生成选题 → 适配平台”。
6. 模型若编造客户未提供的优惠、接送或效果承诺，自动改走安全规则模板。

验收：
- 计划仍返回 7 条、每条保持完整 7 字段下游结构；
- 客户所选平台继续生效；
- 模拟模型超时返回 201 和规则兜底，客户响应不暴露模型元数据。

### v1.6.80 · 团队鉴权与数据隔离安全版

日期：2026-07-10

本次安全整改：
1. 内部客户聚合、诊断/计划/复盘聚合和素材生产工作台接口统一要求 `INTERNAL_ACCESS_TOKEN`，URL 参数或 payload 自称不再授予内部权限。
2. 内部入口增加口令验证，验证成功前不加载或渲染客户数据；验证后内部请求统一携带 `x-internal-token`。
3. 客户匿名自助流程保持免登录，并继续按浏览器保存的 `client_id` 读写自身项目；内部状态桶禁止匿名访问。
4. 新匿名客户 ID 改用 `crypto.randomUUID()` 或 `crypto.getRandomValues()`，旧客户 ID 保持兼容。
5. 客户 API 响应递归移除模型、供应商、用量和 fallback 元数据；持有效内部令牌的请求保留排障证据。

验收：
- 匿名访问 `/api/customers`、`/api/customers/merge-preview`、`/api/dashboard`、`/api/diagnoses`、`/api/plans`、`/api/reviews` 和工作台接口均返回 401；
- 正确 `x-internal-token` 或 Bearer 令牌可正常访问内部接口；
- `/api/health` 继续匿名可用；
- 客户匿名填写、生成、保存项目态和重新读取流程不受影响。

### v1.6.79 · 记录入口状态保持修正版

日期：2026-07-10

本次修正：
1. 修复客户点击内容卡片「去记录效果」时，只保存 `selected_plan_id` 可能覆盖完整生成状态的问题。
2. 当本地存储暂未写入完整生成结果时，记录入口会从当前页面内存中的生成结果补齐状态，避免回到初始填写页。
3. 进入「记录效果」步骤时保留上方本轮计划上下文，避免客户误以为内容被清空。

验收：
- 点击第一天/任意一天「去记录效果」后，仍保留本轮建议和 7 天计划；
- 记录区自动显示所选内容，且不会跳回初始填写表单；
- `/api/health` 返回 `version: "1.6.79"`。

### v1.6.78 · 下一轮生成门禁修正版

日期：2026-07-10

本次修正：
1. 保存 1 条内容反馈后，只显示「本条内容优化建议」，不再提示已生成正式下一轮计划。
2. 新增下一轮启用门禁：至少记录 3 条不同内容的效果后，才开放「结束本轮，使用下一轮计划」。
3. 保留模型/规则生成的下一轮候选数据，但客户侧不再把候选数据误导为正式换轮。
4. 调整 STEP 5 文案为「效果判断」，强调先看本条内容怎么调整。

验收：
- 单条反馈后页面显示「本条内容优化建议」；
- 单条反馈后不出现「结束本轮，使用第 2 轮内容计划」按钮；
- 至少 3 条不同内容反馈后才允许进入下一轮；
- `/api/health` 返回 `version: "1.6.78"`。

### v1.6.77 · 顶部导航文案修正版

日期：2026-07-10

本次修正：
1. 将客户首页顶部导航入口从「开始填写」改为「使用工具」。
2. 保留原跳转位置，仍然进入获客罗盘主工具填写区。
3. 不修改首屏布局、生成逻辑、客户数据和内部版功能。

验收：
- 顶部导航出现「使用工具」；
- 客户首页不再出现「开始填写」；
- `/api/health` 返回 `version: "1.6.77"`。

### v1.6.76 · 首屏右侧对齐修正版

日期：2026-07-09

本次修正：
1. 将客户首页首屏宽度与顶部导航统一为 `1120px`，让右侧 Brief 卡片更接近导航最右侧边界。
2. 将首屏中间间距扩大到 `56px`，让左侧文案和右侧填写区之间更有呼吸感。
3. 明确右侧 Brief 卡片 `justify-self:end`，避免在网格内视觉偏左。
4. 窄屏仍保持单列布局，不影响移动端填写。

验收：
- 右侧 Brief 卡片最大宽度为 `520px`；
- 首屏两列间距为 `56px`；
- `/api/health` 返回 `version: "1.6.76"`。

### v1.6.75 · 首屏视觉居中修正版

日期：2026-07-09

本次修正：
1. 调整客户首页首屏网格宽度与两列比例，让左侧文案和右侧填写卡片作为一个整体居中。
2. 将 hero 区域与填写卡片垂直居中，避免左侧内容看起来偏上或偏散。
3. 收窄右侧表单卡片最大宽度，增加两列间距，让首屏视觉重心更稳定。
4. 移动端仍保持单列布局，不影响客户填写、生成建议和记录效果。

验收：
- 首屏容器使用 `max-width:1128px`；
- 桌面首屏使用 `align-items:center`；
- `/api/health` 返回 `version: "1.6.75"`。

### v1.6.74 · 首屏标题排版修正版

日期：2026-07-09

本次修正：
1. 下调客户首页左侧主标题字号，避免大字块显得笨重。
2. 放开中文标题行高，取消负字距，让首屏排版更接近成熟产品页。
3. 收窄标题和副标题文本宽度，让信息阅读更轻。
4. 不修改客户表单、生成建议、记录效果和内部版结构。

验收：
- hero 标题使用更克制的 `clamp(38px,4.2vw,52px)`；
- hero 标题行高为 `1.18` 且 `letter-spacing:0`；
- `/api/health` 返回 `version: "1.6.74"`。

### v1.6.73 · 淡灰标准页脚修正版

日期：2026-07-09

本次修正：
1. 将上一版过重的底部备案信息条改为成熟网站常见的淡灰标准页脚。
2. 去掉 footer 的白色卡片、黑色粗字和重阴影，改为透明背景、细分割线和中性灰文字。
3. 保持公司名、版权年份、邮箱和备案号清晰可读，但不抢主页面视觉。

验收：
- footer 使用淡灰文字与细分割线；
- `Copyright 2016-2026 南京尚下联信息科技有限公司` 和 `苏ICP备2026037570号` 仍在线上页面可见；
- `/api/health` 返回 `version: "1.6.73"`。

### v1.6.72 · 底部备案信息可见性修正版

日期：2026-07-09

本次修正：
1. 将客户页 footer 从浅灰备注升级为独立白色备案信息条。
2. 提高公司名、`Copyright 2016-2026`、邮箱和备案号的文字颜色与字重。
3. 保留首屏产品化结构、表单生成逻辑、记录效果和内部版结构不变。

验收：
- footer 明确显示 `Copyright 2016-2026 南京尚下联信息科技有限公司`；
- footer 明确显示 `苏ICP备2026037570号`；
- `/api/health` 返回 `version: "1.6.72"`。

### v1.6.71 · 正式产品首屏重构版

日期：2026-07-09

本次优化：
1. 将客户默认首页首屏重排为正式产品入口：左侧产品价值说明，右侧填写 Brief 面板。
2. 保留客户表单、生成建议、记录效果和下一轮优化逻辑，不修改内容生成接口。
3. 降低补丁式视觉观感，使用更统一的浅色产品画布、留白、标题层级和表单容器。
4. 强化底部公司版权、邮箱和备案号的可见性，避免被误认为缺失。

验收：
- 首屏出现 `customer-first-screen` 与 `customer-first-form-shell`；
- 首页标题为「把内容发布，变成持续获客的经营动作」；
- footer 保留并清晰展示 `Copyright 2016-2026 南京尚下联信息科技有限公司`；
- `/api/health` 返回 `version: "1.6.71"`。

### v1.6.70 · 进度导航可见性修正版

日期：2026-07-09

本次修正：
1. 修复客户页进度导航第一步白字落在浅色背景上导致看起来空白的问题。
2. 使用更高权重的 customer-mode active button selector 覆盖旧的 transparent button 规则。
3. 不修改首页结构、内容生成逻辑和内部版结构。

验收：
- 客户进度导航第一步显示「填入基本信息」；
- active pill 有深色背景和白色文字；
- `/api/health` 返回 `version: "1.6.70"`。

### v1.6.69 · 首页降噪与导航修正版

日期：2026-07-09

本次修正：
1. 修复客户进度导航第一步文案，统一为「填入基本信息」。
2. 首屏去掉第二个次要按钮，避免客户一进入页面就看到过多动作。
3. 默认隐藏右侧产品能力卡、实时 Brief 预览、填写示例和 Brief 徽章，减少首页信息密度。
4. 表单改为更直接的单列输入，隐藏示例 chip，让客户先完成基础信息填写。
5. 保留浅色产品视觉、内容生成逻辑、记录效果和内部版结构。

验收：
- 客户进度导航第一步显示「填入基本信息」；
- 首屏只保留一个主动作按钮；
- 首页默认不展示右侧产品能力卡和辅助预览；
- `/api/health` 返回 `version: "1.6.69"`。

### v1.6.68 · 浅色产品视觉升级版

日期：2026-07-09

本次优化：
1. 客户公开页从深色战情室视觉切换为浅色产品画布，降低压迫感。
2. 参考 LOVART 类成熟产品的表达方式，为产品能力卡、Brief 标签增加自有线性小 icon。
3. 表单区、预览区、进度条和 footer 改为白色卡片与轻阴影，减少表格感。
4. 平台选择仍保持规范文字标签，不使用非官方平台 logo 或仿 logo。
5. 内部版保持原有深色运营工作区，不修改内容生成逻辑和客户主流程。

验收：
- 默认客户页为浅色背景；
- 首页出现自有小 icon 样式 `.customer-mini-icon`；
- `/api/health` 返回 `version: "1.6.68"`。

### v1.6.67 · 备案号修正版

日期：2026-07-09

本次修正：
1. 根据备案成功截图，将公开页备案号从带错误尾号的旧文案修正为 `苏ICP备2026037570号`。
2. 同步更新首页、关于我们、联系我们、隐私政策、用户协议页底部备案号。
3. 不修改客户主流程、内容生成逻辑、平台展示和内部版结构。

验收：
- 所有公开页面 footer 显示 `苏ICP备2026037570号`；
- 页面不出现带错误尾号的旧备案文案；
- `/api/health` 返回 `version: "1.6.67"`。

### v1.6.66 · 平台展示规范化版

日期：2026-07-09

本次修正：
1. 公开客户页按成熟 SaaS 网站做法收敛平台展示：无官方授权素材时只展示平台名称。
2. 删除抖音、小红书、视频号、朋友圈/私域的品牌色点缀，避免任何非官方图形被误认为标准 logo。
3. 右侧获客 Brief 预览同步改为中性文字标签。
4. 为后续接入官方授权 SVG/PNG 预留平台文字标签位置，不修改客户主流程、内容生成逻辑和内部版结构。

验收：
- 首页平台选择只显示平台名称，不出现伪 logo 或品牌色点；
- 右侧获客 Brief 预览使用中性文字标签；
- `/api/health` 返回 `version: "1.6.66"`。

### v1.6.65 · 平台标识规范修正版

日期：2026-07-09

本次修正：
1. 撤掉抖音、小红书、视频号的非官方伪 logo 标识，避免页面显得不规范。
2. 平台选择改为完整平台名 + 品牌色识别点，保留视觉识别但不冒充标准 logo。
3. 右侧获客 Brief 预览同步改为规范平台标签。
4. 不修改客户主流程、内容生成逻辑、记录效果、下一轮计划和内部版结构。

验收：
- 首页不再出现「抖 / 书 / 号」伪平台 logo；
- 平台选择仍保留抖音、小红书、视频号、朋友圈/私域；
- `/api/health` 返回 `version: "1.6.65"`。

### v1.6.64 · 客户增长 Brief 填写体验版

日期：2026-07-09

本次优化：
1. 客户填写区从普通表单升级为「客户增长 Brief」式呈现。
2. 三个核心必填项增加编号、示例 chip 和辅助说明，降低第一次填写的混乱感。
3. 抖音、小红书、视频号、朋友圈/私域平台选择增加轻量平台标识。
4. 右侧新增实时获客 Brief 预览，让客户看到系统正在整理需求。
5. 不修改内容生成逻辑、记录效果、下一轮计划和内部版结构。

验收：
- 首页出现「提交你的获客 Brief」；
- 平台选择出现轻量平台标识；
- `/api/health` 返回 `version: "1.6.64"`。

### v1.6.63 · 客户流程导航修复版

日期：2026-07-09

本次修复：
1. 修复客户版顶部流程导航被共享 3 步逻辑覆盖的问题。
2. 客户版统一展示 5 步：填写信息 → 确认方向 → 内容计划 → 记录效果 → 下一轮优化。
3. 内部版仍保留原有 3 步主流程，不改变内部运营视图密度。
4. 不修改客户表单、内容生成逻辑、记录效果和下一轮计划逻辑。

验收：
- 客户版流程导航不再出现重复/混乱的“记录效果”步骤；
- `/api/health` 返回 `version: "1.6.63"`。

### v1.6.62 · 获客罗盘品牌标识版

日期：2026-07-09

本次优化：
1. 对外产品名称由「引客罗盘」统一调整为「获客罗盘」。
2. 顶部导航、首页主视觉、页脚和独立信息页同步使用新产品名。
3. 新增轻量罗盘品牌标识，增强产品识别度和页面细节。
4. 不修改客户主流程、内容生成逻辑、备案信息和内部版能力。

验收：
- 首页标题显示「获客罗盘 · 内容增长循环工具」；
- 页面包含罗盘品牌标识；
- `/api/health` 返回 `version: "1.6.62"`。

### v1.6.61 · 页脚备案文案规范版

日期：2026-07-09

本次优化：
1. 页脚公司信息改为 `Copyright 2016-2026 南京尚下联信息科技有限公司`。
2. 备案链接文案去掉“网信备案：”前缀，仅展示 `苏ICP备2026037570号`。
3. 首页与关于我们、隐私政策、用户协议、联系我们独立页面保持一致。
4. 不修改客户主流程、内容生成逻辑和内部版结构。

验收：
- 首页 footer 不出现“网信备案：”；
- 页脚保留工信部备案跳转链接；
- `/api/health` 返回 `version: "1.6.61"`。

### v1.6.60 · 标准页脚优化版

日期：2026-07-09

本次优化：
1. 将客户版首页和独立信息页的页脚从“按钮式链接区”调整为更标准的站点底部信息层。
2. 页脚统一展示产品名、法务链接、公司主体、联系邮箱和备案信息。
3. 保留独立的关于我们、隐私政策、用户协议、联系我们页面，不把长文重新塞回首页。
4. 不修改客户主流程、内容生成逻辑、内部版结构和备案号内容。

验收：
- 首页页脚不再使用胶囊按钮样式；
- 四个独立信息页页脚与首页保持一致；
- `/api/health` 返回 `version: "1.6.60"`。

### v1.6.59 · 信息页独立化版

日期：2026-07-09

本次优化：
1. 将「关于我们」「隐私政策」「用户协议」「联系我们」从首页 HTML 中剥离为独立静态页面。
2. 首页只保留轻量导航和页脚链接，不再预埋整篇隐私政策和用户协议。
3. Netlify 与本地开发服务均改为将 `/about`、`/privacy`、`/terms`、`/contact` 指向对应独立页面。
4. 客户主流程、生成逻辑、备案号展示和内部版结构不变。

验收：
- 首页源码不再包含完整隐私政策和用户协议正文；
- `/privacy` 和 `/terms` 可独立访问；
- `/api/health` 返回 `version: "1.6.59"`。

### v1.6.58 · 协议与隐私政策完善版

日期：2026-07-09

本次优化：
1. 关于我们、隐私政策、用户协议和联系我们页面补充服务主体「南京尚下联信息科技有限公司」。
2. 隐私政策扩展为条款式草案，覆盖信息收集、使用目的、本地存储与云端同步、第三方模型调用、用户权利和未成年人/敏感信息提示。
3. 用户协议扩展为条款式草案，覆盖服务内容、用户责任、AI 内容说明、禁止行为、效果免责声明、知识产权、服务变更和投诉处理。
4. 投诉与联系邮箱暂统一为 `contact@fpmatrix.cn`。
5. 客户主流程、生成逻辑、备案号展示和内部版结构不变。

验收：
- `/privacy` 显示完整隐私政策草案；
- `/terms` 显示完整用户协议草案；
- 客户可见页包含服务主体和投诉邮箱；
- `/api/health` 返回 `version: "1.6.58"`。

### v1.6.57 · 备案信息补充版

日期：2026-07-09

本次优化：
1. 底部备案信息由占位文案更新为「网信备案：苏ICP备2026037570号」。
2. 暂不展示公安备案占位，避免客户看到未完成的合规项。
3. 保持客户版主流程、生成逻辑、反馈记录和内部版结构不变。

验收：
- 客户页底部显示真实备案号；
- 客户页底部不显示公安备案占位；
- `/api/health` 返回 `version: "1.6.57"`。

### v1.6.56 · 产品可信度与合规底座版

日期：2026-07-09

本次优化：
1. 客户公开页新增网站级导航，补齐「关于我们」「隐私政策」「用户协议」「联系我们」四个信息入口。
2. 页脚新增备案信息展示位，暂不填写虚假备案号，待国内备案完成后替换为正式备案号与链接。
3. 新增本浏览器旧项目提示，区分“继续上次项目”和“新建空白项目”，避免运营测试记录被误认为客户默认页。
4. 保持客户主流程、内容生成内核、反馈记录和下一轮优化逻辑不变，仅补齐产品可信度与合规展示底座。

验收：
- 默认客户首页仍为「引客罗盘」通用产品页，不默认展示篮球客户；
- `/about`、`/privacy`、`/terms`、`/contact` 可作为客户可见信息页访问；
- 页脚只展示备案占位，不伪造备案号；
- `npm test` 通过。

### v1.6.55 · 客户版品牌与产品感收敛版

日期：2026-07-09

本次优化：
1. 客户公开页产品名收敛为「引客罗盘」，强化“持续带来客户咨询”的对外心智。
2. 客户版文案从一次性的“7 天计划”改为“本轮内容计划 → 记录效果 → 下一轮优化”的循环表达，适配后续按月/按年持续服务。
3. 仅调整客户版对外呈现和主题主色，生成内核、计划数组、回填复盘与多轮循环能力不变。
4. 客户版主色从偏紫收敛到深色底上的青绿色增长色，内部版信息密度和工具结构不变。

验收：
- 客户首页不再突出固定 7 天心智；
- 保存效果后提示下一轮内容计划；
- `npm test` 通过。

### v1.6.54 · 商户画像差异化建议版

日期：2026-07-09

本次优化：
1. 在诊断和每条内容计划中新增 `merchant_profile`，记录服务类型、目标客户、当前瓶颈、转化动作和可用素材。
2. 每条计划新增 `customer_reasoning`，把“为什么这样发”拆成客户痛点依据、平台表达依据、转化动作依据、本条验证目标和发布前注意。
3. 下一轮 7 天计划继续继承商户画像、发布前检查和具体解释，避免第二轮、第三轮退回通用模板。
4. 保持客户版轻量呈现：默认仍只看标题、平台和记录入口，详细依据折叠在“为什么这样发”中。

验收：
- 子武限武术搏击计划的 `merchant_profile.service_type` 为 `martial_arts`；
- 所有计划均带 `customer_reasoning` 与 `publish_audit`；
- `npm test` 通过。

### v1.6.53 · 客户版解释与发布审核增强版

日期：2026-07-09

本次优化：
1. 将客户版“为什么这样发”从泛泛说明升级为结构化解释，方便运营人员理解每条内容背后的判断。
2. 新增发布前规则检查 `publish_audit`，优先覆盖小红书场景下的联系方式/站外导流、绝对化用词、互动诱导和课程效果承诺风险。
3. 发布前检查仅作为经验规则提示，不冒充平台官方审核结果。
4. 不新增客户版主流程、不加入内部生成工作台、不改变内容生成内核。

验收：
- 小红书计划出现发布前自查项；
- 高风险表达能被标注为中/高风险；
- 客户默认页面不暴露内部模型/QA/生成工作台信息。

### v1.6.52 · 武术搏击串行业修复版

日期：2026-07-09

本次修正：
1. 新增武术/搏击/散打/拳击/跆拳道等独立体育培训识别，不再落入少儿篮球模板。
2. 收紧少儿篮球识别条件，避免“体验课、体能训练、周末班”等泛词误触发篮球内容。
3. 为武术搏击补充专属选题、平台推荐、共创方向和下一轮复盘题池。
4. 修复客户回填后生成下一轮计划时，武术搏击内容串出“篮球课、运球、投篮”等错行业词的风险。
5. 增加子武限武术搏击 smoke test，要求输出含武术/搏击/安全/体验课语义，且不含篮球错行业词。

验收：
- 子武限武术搏击样例不出现篮球课、少儿篮球、运球、投篮、篮筐、篮球商品；
- 首轮计划、客户共创、下一轮本地复盘题池均识别为武术搏击场景；
- `npm test` 通过。

### v1.6.51 · 团队生产区隔离版

日期：2026-07-02

本次修正：
1. 冻结客户版 v1.6.50 的分步主流程，不改客户公开页体验。
2. 将内部「客户运营工作区」和「素材生产工作台」按路由隔离。
3. `/internal/generation-workbench` 只显示素材、生成任务、QA 和交付相关内容，不再混入诊断、计划、记录和复盘模块。
4. `/internal/` 默认隐藏素材生产工作台，只保留客户运营、诊断、计划和反馈复盘。
5. 增加 CSS 防线，避免未来渲染误解除 `hidden` 后再次同屏混杂。

验收：
- `/internal/` 不显示素材生产工作台；
- `/internal/generation-workbench` 不显示客户运营/诊断/计划/回填/复盘区；
- 客户公开页主流程保持不变；
- `/api/health` 返回 `version: "1.6.51"`。

### v1.6.50 · 客户分步体验版

日期：2026-07-01

本次优化：
1. 客户公开页改为 5 步式体验：填写业务 → 确认方向 → 本周计划 → 记录效果 → 下一轮建议。
2. 只显示当前步骤，避免客户首次使用时同时看到计划、记录和下一轮复盘。
3. 顶部进度条支持在已完成步骤之间切换，未完成步骤会给出友好提示。
4. 记录效果步骤只保留选择内容与关键数字填写；下一轮步骤再展示复盘判断和下一轮计划入口。
5. 保留 v1.6.49 的客户共创确认、观察标签、云端同步和连续内容周期能力。

验收：
- 默认首页只显示业务填写；
- 提交后进入方向确认，不直接堆出全部模块；
- 确认方向后只展示本周计划；
- 点击计划卡片后进入记录效果步骤；
- 保存记录后进入下一轮建议步骤；
- 内部版和模型生成逻辑不变。

### v1.6.49 · 客户共创确认版

日期：2026-07-01

本次新增：
1. 客户公开页在生成 7 天计划前增加“方向确认”层，先让客户选择本周优先测试的内容方向。
2. 篮球培训等客户会看到更贴近业务的方向卡，例如家长痛点型、教练专业信任型、体验课转化型。
3. 支持客户排除不想发的内容，例如不想太硬广、不想强调价格、不想露脸、不想拍孩子正脸、不想承诺效果。
4. 7 天计划生成时会读取 `co_creation` 字段，优先围绕客户确认方向和强调点，并避开客户排除项。
5. 发布效果记录新增“客户主要反馈”标签，下一轮建议会参考这些观察信号。
6. `co_creation` 和 `observation_tags` 会随客户云端同步进入项目库，用于行业品类样本沉淀。

验收：
- 客户表单提交后先进入方向确认，不直接强行生成计划；
- 确认方向后再生成 7 天计划；
- 少儿篮球“体验课转化型”应优先生成体验课、周末班、适合年龄、预约相关选题；
- 回填时可记录客户观察标签，并用于下一轮内容建议。

### v1.6.48 · 客户数据云端同步版

日期：2026-07-01

本次新增：
1. 客户公开页首次生成建议后，会将业务输入、诊断、7 天计划同步到云端项目库。
2. 客户记录发布效果后，会将发布链接、曝光、互动、咨询、备注和下一轮建议同步到云端项目库。
3. 客户启用第 2 / 第 3 / 第 N 轮后，会将当前轮次和历史内容周期同步到云端项目库。
4. 老客户浏览器里已有本地数据时，打开新版页面会自动补一次云端同步。
5. 云端同步失败不影响客户继续使用，本地 localStorage 仍保留完整闭环状态。

验收：
- 内部端“全部客户”列表可读取客户公开页同步后的项目；
- 进入对应客户后可查看业务信息、当前轮次、7 天计划、发布效果和复盘建议；
- 客户公开页生成、记录、启用下一轮的主流程不受云端同步失败影响。

### v1.6.47 · 连续内容周期产品化版

日期：2026-06-30

本次新增：
1. 客户版支持将“下一个七天建议”一键启用为下一轮当前计划。
2. 新增内容周期历史区，显示当前第 N 轮、已归档轮次和每轮前三个选题摘要。
3. 下一轮生成时携带历史轮次标题，避免第 2 / 第 3 / 第 N 轮重复旧选题。
4. 客户复盘模型超时收敛到 Netlify 安全范围内，模型慢时主动 fallback 到本地可用计划，避免 504。
5. 保持客户版轻量呈现，不暴露内部模型、QA 或素材生产模块。

验收：
- `npm test`；
- 客户版记录发布效果后可生成下一轮 7 天计划，并可启用为新一轮当前计划；
- 第 2 / 第 3 轮计划需避开历史选题，内容周期历史区可查看已归档轮次。

### v1.6.46 + generation-workbench-v1 · 项目化素材生成与验收工作台

日期：2026-06-18

本次新增：
1. 在 `/internal/generation-workbench` 增加项目化素材生成与验收工作台，客户默认首页不受影响。
2. 新增素材 asset 与生成任务 generation_task 两类 blobs 数据集合，沿用 `enterprise-marketing-tool-state` store，并按 client_id 分 key。
3. 新增 Provider Adapter mock：`GPT-Image-2` 封面/图片同步、`Seedance 2.0` 视频异步 submit/poll、`Claude Opus` 脚本/文案同步。
4. 新增内部 QA 状态流转，QA 未 passed 的内容不会进入客户交付区。
5. 新增飞书回写 adapter mock，预留 A 客户资料 / B 内容计划 / C 外包制作 / D 内部验收 / E 客户交付 / F 数据回流 六类视图结构。
6. 扩展客户脱敏清单，客户交付区隐藏模型、fallback、provider_job_id、error 等内部字段。

验收：
- `/api/health` 返回 `module: generation-workbench` 与 `module_version: generation-workbench-v1`；
- `npm test` 覆盖视频异步任务 submit → generating → poll → qa_pending → QA failed → QA passed，以及封面同步任务；
- 客户交付视图只返回 QA passed 且 client_visible 的任务。

### v1.6.41 · 回填复盘短模型链路版

日期：2026-06-11

本次修补：
1. 将回填后模型调用收敛为短输出：只让火山方舟生成复盘判断、下一步策略和客户短结论。
2. 下一轮 7 天计划由系统基于模型判断、回填指标和历史 7 天计划展开，避免长生成触发 Netlify 平台 504。
3. 超时上限恢复到平台安全范围内，线上若模型失败继续明确 fallback_reason，不冒充模型输出。

验收：
- npm test；
- 线上 customer-growth-advice 烟测需返回 7 条 next_7_day_plan，并保留模型证据。


### v1.6.40 · 回填复盘模型超时修正版

日期：2026-06-11

本次修补：
1. 将火山方舟调用默认超时时间从 25 秒提高到 45 秒，上限提高到 60 秒，避免回填后 7 天计划生成被过早截断。
2. 保持 fallback 证据透明：若模型仍失败，继续返回 provider/fallback/fallback_reason，不冒充正式模型输出。
3. 继续保留 v1.6.38 冻结锚点和 v1.6.39 回填闭环结构。

验收：
- npm test；
- 线上 /api/customer-growth-advice 模型烟测需确认 provider=volcengine_ark 且 fallback=false。


### v1.6.39 · 回填复盘与下轮7天计划版

日期：2026-06-11

本次修补：
1. 冻结 v1.6.38 火山方舟接入版为可回滚锚点，并保存工作树快照、diff、变更清单和 tag。
2. 客户版效果回填从“下一条建议”升级为“复盘判断 + 下一轮 7 天内容计划”。
3. 回填字段保持营销复盘边界：曝光/播放、点赞、收藏、评论、分享、私信/咨询、预约/到店和备注，不引入 CRM/ERP/销售跟进流程。
4. /api/customer-growth-advice 返回 next_round、review_judgment、customer_summary、next_7_day_plan 和模型证据，火山失败时显式 fallback。
5. 客户版和 /internal/ 共用主闭环能力；内部调试能力仍由 /internal/ 路由门禁保留，/?mode=internal 仍为客户版。

验收：
- npm test；
- Netlify 生产部署后验证 /api/health、客户页、篮球预填链接、/?mode=internal 和 /internal/；
- 线上模型烟测确认 provider/fallback/requested_model/actual_model 证据。


### v1.6.38 · 豆包/火山方舟模型接入版

日期：2026-06-10

本次修补：
1. public 客户版首轮 7 天内容建议默认接入火山方舟 OpenAI-compatible Chat Completions API。
2. 客户发布效果记录后的下一步建议默认接入同一条火山方舟模型链路。
3. 增加统一 `model_info / generation_meta` 模型证据，明确 provider、requested_model、actual_model、fallback、fallback_reason、latency_ms 和 usage。
4. 未配置 `ARK_API_KEY` 或模型 Endpoint ID 时，明确回退到 `rule_template`，不冒充真实 AI。
5. internal 版保留 `model_provider / model_mode` 后端路由入口，后续可测试 Ark、OpenAI、Anthropic 或 local。

验收：
- `node --check static/app.js`；
- `node --check netlify/functions/api.mjs`；
- `npm test`；
- Netlify preview 验证 `/api/assessments` 与 `/api/customer-growth-advice` 均返回模型证据。

### v1.6.37 · AI项目入口门禁版

日期：2026-06-07

本次修补：
1. 内测新增项目入口增加「系统理解卡」，把 AI 从一句业务描述中提取到的业务、目标、客群、平台、问题、主推产品、客户顾虑和素材依据显性展示。
2. 增加缺项提示：当主推产品/服务、客户真实顾虑、现有素材/近期最好内容不足时，先提示补齐，不再让系统直接生成泛模板。
3. 增加生成门禁：`internal_test` 模式下，后端同步拦截缺少关键精度字段的提交，防止前端绕过。
4. 客户公开版不展示「AI项目助手 / 系统理解卡 / 生成门禁」等内测入口文案，继续保持原客户轻表单主路径。
5. 版本统一升级为 `1.6.37`。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `npm test` 通过；
- 本地与线上验证 `/` 客户页无内测入口文案；`/internal` 新增项目入口可展示系统理解卡和缺项提示；
- 线上部署：`https://sales-improve.fpmatrix.cn`，deploy `6a24f32c5cc5de2f94e02d05`。

### v1.6.31 · 项目数据隔离与安标检测模块边界修复版

日期：2026-06-05

本次修补：
1. 修复安标检测客户脚本反馈面板错误出现在“轻工业作战台”的问题。
2. 根因：安标检测客户反馈展示门禁曾把“轻工业”和泛化“检测”写入命中条件，导致轻工业项目也被判定为安标检测项目。
3. 新门禁只允许明确命中：安标检测、安标、医疗器械注册、医疗器械送检、注册送检。
4. 将“安检项目 / 客户真实数据”更名为“安标检测 / 客户脚本反馈样本”，明确该模块是项目级客户反馈资产，不属于作战台核心运营指标。
5. 面板默认 `hidden`，只在明确命中安标检测项目后展示，避免首屏加载闪现和跨项目误显。
6. 新增并更新 `PROJECT_DATA_BOUNDARY.md`，明确工作台主数据、客户反馈资产、模块承载位置、项目级数据隔离和新增真实客户数据模块前的设计要求。

验收：
- `npm test` 通过；
- 线上已验证：轻工业作战台不显示安标检测反馈模块；切换到安标检测项目后显示“安标检测客户脚本确认表”；页面不再出现“安检项目/安检客户”旧称。

### v1.6.28 · 首次生成主路径收口版

- 客户首次生成建议后，自动收起 STEP 1 填写表单，主路径聚焦到“内容建议 + 7 天发布计划 + 效果记录”。
- 刷新页面后如本地已有生成结果，自动恢复 STEP 2/STEP 3，不再把客户带回空白首访表单。
- “修改信息并重新生成”会显式展开 STEP 1，并回填上次输入，避免客户找不到编辑入口。
- 统一前端与 API 版本标记为 `1.6.28`，并更新前端缓存参数。

### v1.6.27 · 计划卡片回填稳定修复版

- 修复计划卡片按钮回填链路：任意计划卡片按钮点击后，稳定写入 `#feedbackForm [name=content_plan_id] = plan.id`。
- 修复 `#selectedPlanDisplay` 同步显示，点击后稳定显示 `计划 #N`。
- 移除计划卡片按钮对 `Number(plan.id)` 的依赖，保留原始 `plan.id`，避免非纯数字 ID 或字符串 ID 被写成 `NaN`/空值。
- 增加 `data-plan-id` + 委托点击绑定，降低动态渲染后按钮事件丢失风险。
- 回填保存时保留 `content_plan_id` 原始值，并用统一 `samePlanId()` 做计划匹配与去重。

### v1.6.26 · 新增项目反馈闭环修复版

- 修复反馈回填后 `project_stage` 未在保存前同步的问题，避免项目列表/跨端状态仍显示“待启动”。
- 验证新增项目 → 计划 → 发布链接回填 → feedback → review → projectStore 持久化闭环。

### v1.6.25 · 新增项目工作台闭环修复版

日期：2026-05-29

本次修补：
1. 内测新增项目入口统一客户轻表单字段，同时保留内测智能诊断、作战台、反馈回填和周复盘能力。
2. 修复平台/最大问题 chip 在真实点击路径下未写入 canonical hidden fields 的风险，补充 button 直绑、pointerup/click 委托和提交前二次同步。
3. 新增项目提交后强制进入 `projects[] + activeProjectId`，当前项目工作台、计划、反馈、周复盘读取同一项目状态。
4. 保持 `/api/state` 云端项目状态同步路径，用于 PC / 移动端刷新后读取同一项目集合。
5. 本版本为内测闭环验证版，客户公开页不展示内部 smart_context。

验收：
- `node --check static/app.js`；
- `node --check netlify/functions/api.mjs`；
- `node tests/api_smoke.mjs`；
- 本地 API bridge 下验证新增项目、历史项目恢复、反馈回填、刷新同步和云端状态读取。

### v1.6.17 · 客户首页视觉基线修复版

日期：2026-05-28

本次修补：
1. 保留 v1.6.16 的客户试用首页、5 个问题填写和所见即所得引导图。
2. 修复客户默认首页误用浅色 SaaS 壳的问题，视觉基线重新对齐内部验证版的深色经营工具风格。
3. 客户表单、填写引导图、按钮卡片、生成结果和发布效果记录统一使用深色卡片/看板样式。
4. 不新增产品功能，不改变客户首页信息架构，不影响内部验证入口。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 线上默认首页仍为客户试用版，但视觉色系与内部验证版保持一致。

### v1.6.16 · 所见即所得填写引导版

日期：2026-05-28

本次修补：
1. 默认客户首页新增“所见即所得填写引导图”，用模拟表单贴着 5 个真实字段说明怎么填写。
2. 引导图字段名、平台选择、最大问题选项和真实表单保持一致，避免客户看完示例后找不到对应输入项。
3. 桌面端在表单旁展示示例，手机端默认折叠为“不知道怎么填？看示例”，降低首屏压力。
4. 增加“我知道怎么填了”按钮，收起示例并聚焦第一个输入框。
5. 保留客户版生成建议、复制建议、发布后记录效果和内部验证入口。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 线上默认首页引导图不展示内部词，真实表单仍可提交并生成建议。

### v1.6.15 · 客户试用入口版

日期：2026-05-28

本次修补：
1. 默认 `/` 改为真实客户试用版，隐藏版本号、内部看板和内部术语。
2. 新增内部验证入口 `/internal`，保留作战台、回填、周复盘、判断依据等内部验证能力。
3. 客户首页改为 5 个问题表单，当前最大问题使用按钮卡片选择。
4. 生成结果改为客户可读的内容建议：当前问题、内容方向、3 个选题、第一条怎么发、发布后记录效果。
5. 客户版增加复制建议和轻量发布效果记录，记录保存在当前浏览器。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 线上默认首页不展示内部版本号或内部系统语言，`/internal` 可访问内部验证版。

### v1.6.13 · 可读性完整修复版

日期：2026-05-28

本次修补：
1. 顶部版本信息改成可见胶囊标签，提高对比度，不再像灰色弱提示。
2. 新增项目页「返回当前运营项目」从表单内部移到首屏右上「更多」按钮左侧，进入新项目时优先可见。
3. 「客户输入与诊断依据」从密集双列表改为“客户输入摘要 / 系统诊断摘要”两张卡，完整字段折叠到「查看完整客户输入」。
4. 完整 7 天计划从密集表格改为逐条卡片：主题、状态、日期/平台、发布角度、合规承接、观察目标和操作分区展示。
5. 保留 v1.6.12 的周复盘指标卡和按钮显性化，但补齐证据区与完整计划的可读性。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 浏览器验证：新项目返回按钮在「更多」左侧；版本标签高对比显示；客户证据区为摘要卡；完整 7 天计划为卡片列表。

### v1.6.12 · 复盘区可读性修复版

日期：2026-05-27

本次修补：
1. 周复盘从大段文字改为卡片化结构：顶部时间卡、4个指标卡、胜出主题/当前瓶颈/下一步动作分区展示。
2. 「更新复盘」移到「周复盘」标题同一行，改为蓝绿渐变主按钮，并加刷新符号，降低“看不出能点”的问题。
3. 顶部「当前项目」标签强制单行展示，项目下拉框用 flex 布局，避免“当前项目”几个字被挤成两行。
4. 回填记录改成轻量标签式指标，减少一整段文字堆叠。
5. 调整回填/复盘两列空间分配与间距，复盘列优先保证阅读宽度；窄屏自动单列。
6. 保留 v1.6.11 的移动端 hidden 修复，避免新项目创建页露出回填/复盘模块。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器验证：`#reviewBtn` 位于 `.review-panel-head` 内，样式为 `review-primary-btn`；复盘卡片包含 4 个指标卡；「当前项目」标签 `white-space: nowrap`；视觉二次验收未见明显换行/按钮不可点击问题。

### v1.6.11 · 移动端隐藏状态修复版

日期：2026-05-27

本次修补：
1. 修复移动端「新项目创建 / 首访填写」页面底部错误露出「内容数据回填」「周复盘」的问题。
2. 根因：`war-room-v1.6.1.css` 在移动端使用 `.feedback-focus{display:grid!important}`，覆盖了 HTML `hidden` 状态。
3. 在最后加载的战情室 CSS 末尾增加 hidden-state guard：`[hidden] / .feedback-focus[hidden] / .grid[hidden] / .panel[hidden] / .cards[hidden]` 强制 `display:none!important`。
4. 保留 v1.6.10 的导航修复：作战台按钮恢复、新增项目页可返回当前运营项目、计划编号按项目内 #1/#2/#3 显示。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器验证：进入新项目填写状态后，`#feedbackWorkflow.hidden === true` 且 computed display 为 `none`；页面文本不再包含「内容数据回填」「周复盘」。

### v1.6.10 · 页面降噪与导航修复版

日期：2026-05-27

本次修补：
1. 恢复作战台头部右侧操作按钮区：保留「主动作 / 查看计划 / 周复盘」，避免状态卡右侧空白。
2. 新增项目填写页增加「返回当前运营项目」入口，可回到最近运营项目。
3. 内容计划显示编号改为当前项目内序号：即使后端真实 plan id 是 8/9/10，界面仍显示 #1/#2/#3；回填仍使用真实 id，不影响数据保存。
4. 保留 v1.6.9 已确认的降噪逻辑：顶部不恢复伪主按钮/看示例，依据入口继续收口到复盘区域。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器验证：作战台头部按钮恢复；新增项目页出现返回当前运营项目；模拟 plan id=8/9/10 时界面显示 #1/#2/#3，今日动作显示发布 #1。

### v1.6.9 · 页面降噪与主路径收口版

日期：2026-05-27

本次修补：
1. 以线上 `v1.6.6 · 回填闭环与标题体验修复版` 为界面地基，撤回本地 v1.7.x 首访重构思路，不混入新视觉/新框架/新产品方向。
2. 删除首屏标题旁重复主按钮：不再长期展示「开始增长诊断 / 查看周复盘」和「看示例」。
3. 合并「依据 R / 为什么 / 查看依据」为一个用户可理解入口：「查看判断依据」。
4. 7天内容计划默认只展示前三条扫读卡片：标题、平台、状态、日期/反馈、核心操作；发布角度和目标收进展开层。
5. 「内容复盘依据」移动到「回填记录」之后，符合发布 → 回填 → 记录 → 复盘 → 判断依据的生成路径。
6. 「客户输入与诊断依据」并入「周复盘」右侧的复盘与判断依据区域，作为复盘依据链条的一部分，不再单独插在计划执行前后打断主路径。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器 `http://127.0.0.1:4173` 载入样例后确认：无首屏伪主按钮/看示例；作战台只保留一个「查看判断依据」；计划卡片默认 3 条；复盘依据在回填记录后；Console 0 error。

### v1.7.3 · 战情室视觉基线恢复版

日期：2026-05-27

本次修补：
1. 将 `static/war-room-v1.6.1.css` 精确恢复到 commit `183fec6` 的深色战情室视觉基线。
2. 移除 `8ffee10` 的浅色 v17 视觉覆盖作为当前视觉来源。
3. 保留 5 问题首访流程、问题按钮、生成建议、前三条计划和发布后回填链路。
4. 同步前端与 Netlify Function 版本号为 `1.7.3`。

验收：
- `static/war-room-v1.6.1.css` 与 `183fec6` 对应文件一致；
- 线上 CSS body 背景包含 `#08090a / #0c0d10 / #111214`；
- `/api/health` 返回 `version: "1.7.3"`。

### v1.7.2 · CSS 精确回滚版

日期：2026-05-27

本次修补：
1. 将 `static/war-room-v1.6.1.css` 精确恢复到 commit `8ffee10` 的版本。
2. 保留 v1.7.1 的 5 问题首访流程、问题按钮、生成建议和发布后回填链路。
3. 不新增视觉设计、不新增暖色覆盖、不新增 clean 视觉变量。
4. 同步前端与 Netlify Function 版本号为 `1.7.2`。

验收：
- `static/war-room-v1.6.1.css` 与 `8ffee10` 对应文件一致；
- `/api/health` 返回 `version: "1.7.2"`；
- 首访 5 问题流程、回填预填和反馈复盘保持可用。

### v1.7.1 · 视觉回滚 + 信息架构保留版

日期：2026-05-27

本次修补：
1. 保留 v1.7.0 的首访信息架构：客户先填 5 个问题，再生成内容建议。
2. 撤回 v1.7.0 末尾新增的强视觉覆盖，恢复早期轻量、暖色、卡片式页面质感。
3. 首访页面不再静态露出新增项目、更多操作等内部入口，避免首次打开产生后台感。
4. 保留最大问题按钮、生成建议、发布后回填、刷新恢复和自动复盘能力。
5. 不新增自动发布、自动抓取、账号矩阵或内部后台功能。

验收：
- 首访首屏仍只展示 5 问表单和生成按钮；
- 页面视觉回到早期轻量风格，不再像新的视觉设计稿；
- 点击内容计划的发布后回填仍能带入计划 ID；
- 保存反馈后回填记录和复盘摘要继续同步更新；
- `/api/health` 返回 `version: "1.7.1"`。

### v1.7.0 · 首访填写体验修复版

日期：2026-05-27

本次整改：
1. 首访页重新回到客户视角：标题改为「帮你找到下一周该发什么内容」，首屏聚焦 5 个问题和生成按钮。
2. 取消首次进入自动载入运营样例，避免客户还没填写就看到作战台、闭环率、复盘数据。
3. 「当前最大问题」从传统下拉框改为 4 个可点击问题按钮，降低首访卡住风险。
4. 生成后默认只展示当前最大问题、本周建议、7天内容计划、发布后回填提醒；复杂复盘数据后置到次级入口。
5. 保留发布后回填、刷新恢复、自动复盘等能力，但不在首访页面堆内部后台信息。

验收：
- 首访页面不再默认展示内部作战台或样例数据；
- 5 项表单可顺利填写，最大问题按钮可选中并参与生成；
- 生成结果清爽展示 4 个核心块；
- 点击计划卡片可进入回填，保存反馈后刷新仍保留；
- `/api/health` 返回 `version: "1.7.0"`。

### v1.6.8 · 回填复盘依据收口版

日期：2026-05-27

本次修补：
1. 将「复盘与判断依据」从计划执行前移到内容数据回填、回填记录、周复盘附近，符合“发布计划 → 数据回填 → 回填记录 → 周复盘 → 判断依据 → 下一步判断”的闭环路径。
2. 用户界面不再显性展示 K/V/R，只展示「客户档案 / 系统判断 / 内容复盘依据」。
3. 回填字段恢复为「曝光｜查看 / 互动｜点赞 / 互动｜评论 / 互动｜收藏/分享 / 转化｜私信/咨询」，把数据从 vanity metrics 拉回增长判断。
4. 移动端优化回填与复盘区域：回填表单单列、保存按钮更清晰、回填记录卡片流、周复盘摘要优先、判断依据默认收起。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `python3 -m pytest -q` 通过；
- `node tests/api_smoke.mjs` 通过；
- 线上需验证 v1.6.8 版本号、回填/周复盘/复盘与判断依据位置与移动端布局。

### v1.6.6 · 回填闭环与标题体验修复版

日期：2026-05-26

本次修补：
1. 将已验收的回填闭环与标题体验修复正式补标为 v1.6.6。
2. 内容标题不再明显硬拼接客户长输入，优先生成可直接作为选题的人话标题。
3. 「复制/回填」按钮可自动带入发布计划 ID，并定位到内容数据回填区域。
4. 保存反馈后刷新页面仍能保留，周复盘会根据回填数据自动更新。
5. 本次只修正版本号和版本说明，不引入新框架、新数据库或产品方向变化。

验收：
- `/api/health` 返回 `version: "1.6.6"`；
- 页面展示 `v1.6.6 · 回填闭环与标题体验修复版`；
- 回填预填、刷新保留和自动周复盘能力保持可用。

### v1.6.5 · 依据中心与复盘判断升级版

日期：2026-05-26

本次修补：
1. 将「客户输入与诊断依据」升级为 K/V/R 三层依据中心：K = 客户原始输入，V = 系统诊断依据，R = 内容复盘依据。
2. 今日动作和下一步判断不再共用模糊依据：有真实反馈时优先锚定 R 层；无反馈时分别锚定 K 或 V。
3. 下一步计划优先读取最新复盘依据：正式周复盘优先，没有正式复盘时读取自动复盘。
4. R 层默认只展示最新复盘和最近 5 条反馈，更多历史折叠提示，避免页面无限拉长。
5. 内容数据回填字段改为「曝光｜查看 / 互动｜点赞评论收藏分享 / 转化｜咨询」，减少把互动误当增长结果的问题。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器打开 `/?v=165`，点击「看示例」后可见 K/V/R 依据中心，今日动作和下一步判断均可指向 R 层，控制台无 JS error。

### v1.6.3 · 客户输入/诊断依据 K/V 区分版

日期：2026-05-26

本次修补：
1. `客户输入与诊断依据` 不再把所有字段混在同一组信息卡里。
2. 新增 K/V 双栏：K = 客户原始输入，V = 系统诊断依据。
3. K 区保留行业、目标、客户、平台、痛点、对标账号等客户填写信息；V 区展示阶段、策略分、最大问题、核心判断、下一步、评分说明和风险提醒。
4. 移动端自动改为上下堆叠，避免双栏挤压。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器打开 `/?v=163`，点击“看示例”与“查看依据”，已显示 K/V 分栏，控制台无 JS error。

### v1.6.2 · 企业营销增长驾驶舱体验修补版

日期：2026-05-26

本次修补：
1. 首屏主按钮按项目状态切换：未诊断=开始增长诊断；待启动=查看内容计划；运营/待回填=回填发布数据；复盘期=查看反馈复盘。
2. 产品标题恢复为 `企业营销增长驾驶舱`，首屏 Slogan 改为 `不只发内容，更要知道哪条内容带来客户。`
3. `内容实验` / `反馈复盘` 本轮采用方案A：先做页内定位锚点，不拆多页面 Tab。
4. 优化 `更多` 数据操作菜单文案、关闭行为和移动端按钮布局。
5. 修复无企业名称时显示“未命名客户”的问题，改为优先用行业生成作战台名称。
6. 修复刷新后未恢复本地作战台的问题：检测到有效 diagnosis/plans/project/cycle 状态时直接恢复工作台。
7. 强化计划卡片回填按钮：自动填入计划 ID，并带入已有发布链接和反馈数据。
8. 不新增自动发布、自动抓取、自动登录或账号矩阵能力；仍只做客户信息收集、数据回填分析、内容策略优化。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器打开 `/`，首屏标题、Slogan、状态按钮和样例驾驶舱页签无 JS error。

### v1.6.1 · 企业增长作战室界面版

日期：2026-05-25

本次迭代：
1. 将运营周期作战台落地为深色 SaaS 决策驾驶舱：顶部状态条、今日动作、下一步判断、关键数据卡、内容实验列表同屏展示。
2. 新增独立样式文件 `static/war-room-v1.6.1.css`，避免历史 `styles.css` 兼容问题影响新版驾驶舱视觉。
3. 页面标题和前端版本展示更新为 `v1.6.1 · 企业增长作战室`。
4. 内容计划卡片改为实验列表样式，保留平台、状态、目标信号、曝光/收藏/咨询等可扫读数据。
5. 不新增自动发布、自动抓取、自动登录或账号矩阵能力；仍只做客户信息收集、数据回填分析、内容策略优化。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器打开 `/`，样例数据可展示顶部状态条、今日动作/下一步判断、关键数据卡、内容实验列表；控制台无 JS error。

### v1.6.0 · 首次客户友好版

日期：2026-05-25

本次整改：
1. 首屏从“运营驾驶舱优先”改回“首次客户 3 分钟诊断入口”，解决新客户不知道从哪里开始的问题。
2. 表单首屏只保留 5 个核心问题：行业、目标、客户、平台、最大问题；其他内部/高门槛信息默认收起。
3. 对标账号从强制可见模块移入可选补充，避免第一次填写就被“对标/数据/复盘”等内部语言压住。
4. 回填区域默认隐藏，避免客户还没生成建议就看到发布后运营字段。
5. 版本号统一为 v1.6.0，前端与 Netlify Function 保持一致。

验收：
- 首屏必须明确显示“从这里开始填写”；
- 首次客户无需理解运营周期、回填、复盘等术语，也能完成第一步；
- 必填项只保留生成建议所需的最小信息；
- 不新增自动发布、自动抓取、自动登录或账号矩阵能力；
- `node --check static/app.js`、`node --check netlify/functions/api.mjs`、`node tests/api_smoke.mjs` 必须通过。

### v1.5.7 · 运营周期优先版

日期：2026-05-23

本次修补：
1. 将默认展示逻辑改为运营周期优先：项目已有计划/发布/反馈/复盘时，首屏先看驾驶舱，而不是重新诊断表单。
2. 修正空状态文案：明确“当前浏览器未检测到运营数据”，避免客户误解为线上数据丢失。
3. 样例入口升级为运营样例：默认带 7 条计划、1 条已发布内容、T+72 回填数据和自动周复盘。
4. 首屏指标恢复为计划内容、已发布、回填率、曝光、互动、咨询数、闭环分，强化数据部分可见性。
5. 诊断表单降级为新客户/重新诊断入口，运营中项目优先展示发布、回填、复盘和下一步建议。

验收：
- 页面标题与版本号显示 v1.5.7；
- 空状态不得暗示数据丢失；
- 点击“查看运营样例”后必须进入运营/复盘驾驶舱，并展示数据指标、发布计划、回填、周复盘；
- `node --check static/app.js`、`node --check netlify/functions/api.mjs`、`node tests/api_smoke.mjs` 必须通过。

### v1.5.5.1 · 客户理解修补版

日期：2026-05-22

本次修补：
1. 保留 v1.5.5 的状态分层与驾驶舱结构，不回滚 v1.5.4。
2. 修复按钮过度精简导致的新用户理解成本问题。
3. 恢复少量极短微提示，帮助首次使用者理解「诊断 → 发布 → 回填 → 复盘」流程。
4. 将关键按钮从纯短词恢复为业务动作，例如「开始诊断」「载入样例」「查看依据」「回填/复盘」「保存反馈」。
5. 不新增自动发布、自动抓取、自动登录或账号矩阵能力。

### v1.5.2 · 回填体验增强版

日期：2026-05-21

本次 QA 修复：

1. 点击「去回填计划 #ID」或「发布后回填」后，自动展开反馈回填区域并填入发布计划 ID。
2. 页面平滑滚动到「首发链接回填 / 营销反馈回填」，并自动聚焦发布链接输入框。
3. 回填区域增加短暂高亮效果，配合提示文案，让测试人员明确知道按钮已生效。
4. 保存反馈成功后提示「反馈已保存，看板已更新。」并保持看板、计划状态、反馈记录同步刷新。
5. 保存反馈时补齐发布计划 ID 必填校验，错误提示改为业务语言。

验收：
- `/api/health` 必须返回 `version: "1.5.2"`；
- 点击任意回填按钮后，计划 ID、滚动、焦点、高亮和提示必须同时生效；
- 不得破坏 v1.5 工作台、样例入口、反馈保存、指标计算和周复盘。

### v1.5.1 · 客户展示体验与审美修复版

日期：2026-05-21

本次体验更新：

1. 首屏从“项目生命周期说明”升级为客户-facing 增长驾驶舱：突出今天做什么、结果怎么样、下一轮怎么赢。
2. 弱化内部字段裸露：`cycle-1` 前台展示为“第1轮增长周期”，按钮改为“进入下一轮”。
3. 工作台卡片改为“本轮成果 / 今天先做 / 值得复制 / 风险提醒”，更适合老板和客户 10 秒扫读。
4. 追加高质感 SaaS Dashboard 视觉覆盖：玻璃拟态卡片、留白、柔和阴影、焦点态、暗色主指标卡。
5. 客户数据与诊断依据默认收起，保留内部核对能力，但不干扰展示体验。

验收：
- 首屏不得再裸露 `cycle-1` 作为客户可见周期；
- 页面标题与版本号必须显示 v1.5.1；
- 首页第一屏必须优先呈现增长驾驶舱，而不是大表单或内部工程字段；
- `node --check static/app.js`、`node --check netlify/functions/api.mjs`、`node tests/api_smoke.mjs` 必须通过。

### v1.5.0 · 项目生命周期工作台

日期：2026-05-21

本次功能更新：

1. 首页升级为「项目生命周期工作台」：根据项目阶段优先展示今日动作、本周期成果、最佳内容和当前风险。
2. 新增最小项目状态模型：`project.id`、`project.name`、`project_stage`、`current_cycle_id`，计划和反馈记录补充 `project_id` / `cycle_id`。
3. 生命周期阶段从“未诊断 → 待启动 → 运营中 → 复盘期”自动推断，避免运营项目每天回到诊断表。
4. 新项目/重新诊断仍可从首页入口进入，但运营期默认先看成果与缺口。
5. 支持“进入下一周期”，用于把一轮复盘后的动作沉淀为下一轮内容增长实验。

验收：
- 首页必须先显示项目工作台，而不是直接显示大表单；
- 样例数据应进入“待启动”阶段，并显示本周期成果和今日动作；
- 发布回填后应进入“运营中”；
- 生成复盘后应进入“复盘期”；
- `node --check static/app.js`、`node --check netlify/functions/api.mjs`、`node tests/api_smoke.mjs` 必须通过。


### v1.4.1 · 极简录入 + 对标账号独立模块

日期：2026-05-20

本次体验优化：

1. 首页极简化：首屏只保留业务核心字段和一个独立对标账号模块。
2. 对标账号从“补充资料”中独立为核心模块，强调用真实市场信号校准选题，而不是让 AI 凭空编爆款。
3. 高级补充默认隐藏，企业名称、联系人、预算、决策周期、内容资产等不再压迫首次填写。
4. 生成结果前弱化反馈/复盘模块，仅保留轻提示；生成计划后再展示发布链接回填、T+24 / T+72 / T+7 和周复盘。
5. 保留 v1.4.0 的对标账号主题参考、多阶段反馈、最新阶段聚合统计和周复盘逻辑。

验收：
- 首屏必须更像“少填信息即可生成建议”，而不是大表单；
- 对标账号必须独立可见；
- 未生成计划前不展开完整反馈表和周复盘；
- 不得新增自动抓取、自动登录或自动发布。

### v1.4.0 · 对标账号主题参考 + 多阶段反馈回填

日期：2026-05-20

本次功能更新：

1. 新增「对标账号主题参考」模块。
2. 第一轮主题建议加入市场校准层，不再只依赖客户自述和系统判断。
3. 支持客户手动输入对标平台、对标账号链接、备注和代表爆款内容/标题/链接/数据摘要。
4. 7天内容计划会提炼对标账号中已验证的痛点和标题结构，但只做主题转译，不照抄原文。
5. 暂不接自动抓取 Skill，不接小红书/抖音自动爬取，不自动登录，不自动发布，避免自动化风险。

验收：
- 页面必须出现「对标账号主题参考」；
- 页面必须提示「对标账号不是用来抄袭，而是用来判断市场已经验证过哪些痛点和表达方式。」；
- assessment 必须包含 `benchmark` 字段；
- 保留 T+24 / T+72 / T+7 多阶段反馈回填。

### v1.4 安全修正 · 移除评论引导

日期：2026-05-19

本次修正：

1. 移除内容决策局与客户内容计划中的评论区/留言式引导。
2. CTA/承接统一改为私信、主页咨询、保存、数据回填等低风险表达。
3. 页面表头从 `CTA` 调整为 `合规承接`，避免继续诱导客户使用评论区引导。
4. 保留「评论数」作为数据指标，但不再生成“评论区告诉我 / 留言某关键词”等动作建议。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过。

### v1.4 · 多时间点反馈回填

日期：2026-05-19

本次功能更新：

1. 将「首发链接回填 / 营销反馈回填」升级为多时间点入口：
   - 新增反馈时间点字段：`T+24`、`T+72`、`T+7`；
   - 同一发布计划可保留多个时间点反馈记录，观察内容生命周期，而不是只看一次数据。
2. 修正统计口径：
   - 看板、动态闭环分、周复盘只使用每条内容的最新时间点数据；
   - 避免 T+24 + T+72 + T+7 被重复累加，造成曝光、互动、咨询虚高。
3. 修正重复提交口径：
   - 同一计划 + 同一反馈时间点重复提交时覆盖旧记录；
   - 同一计划不同时间点同时保留。
4. 验收：
   - `node --check static/app.js` 通过；
   - `node --check netlify/functions/api.mjs` 通过；
   - `node tests/api_smoke.mjs` 覆盖 T+24/T+72 多行保留与最新口径统计。

### v1.3.2 · 快速体检 + 首屏关键结论

日期：2026-05-18

本次小修订/修正：

1. 将「客户营销体检」从大表单压缩为「5项快速体检」：
   - 首屏只保留行业、主要目标、目标客户、当前平台、最大问题、企业名称；
   - 产品/服务入口、客户痛点、内容资产、预算、决策周期等改为可选展开字段；
   - 降低第一次填写阻力，先生成判断，再补资料。
2. 将「关键结论」提前到首屏右侧：
   - 用户不需要滚动很久才能看到诊断；
   - 诊断默认只展示优先问题、核心诊断、下一步；
   - 账号配置、平台建议、评分说明等改为可展开详情。
3. 将「客户数据/诊断依据」默认收起：
   - 保留可审计性；
   - 不再占用主页面高度。
4. 将「7天计划」默认压缩为前三个优先动作：
   - 完整计划表改为展开查看；
   - 页面长度明显降低。
5. 验收：
   - `node --check static/app.js` 通过；
   - `node --check netlify/functions/api.mjs` 通过；
   - `node tests/api_smoke.mjs` 通过；
   - 本地浏览器控制台无 JS error。

### v1.3.1 · 客户数据展示修复 + 首发链接门禁体验调整

日期：2026-05-17

本次小修订/修正：

1. 修复「客户展示」问题：
   - 将「客户营销体检」输入区与「最新诊断报告」展示区拆开；
   - 新增「当前客户数据」展示区，显示本次真正进入诊断引擎的客户输入；
   - 避免出现“诊断有了，但客户数据为空/看不到”的误判。
2. 修复后端 assessment 返回：
   - `POST /api/assessments` 返回中补充 `assessment` 对象；
   - 前端提交体检或载入样例后，可直接用后端返回的 assessment 渲染客户数据。
3. 调整「首发链接回填门禁」交互层级：
   - 从独立大模块降级为计划表区域内的轻提示；
   - 保留“发布后必须回填链接”的闭环提示，但不抢占诊断报告版面。
4. 验证链接回填链路：
   - 缺少发布链接时不能被计为已闭环；
   - 计划/看板统计按 `已发布 + publish_link` 计算；
   - 链接回填后，反馈、看板、闭环分和下一轮建议可联动更新。
5. v1.3 已完成线上部署测试，线上 `/api/health` 返回：`v1.3 · 内容决策局测试样例入口 + 首发链接回填门禁`。
6. 从本条开始，后续同一功能版本内的小修改、补丁、文案、字段和 QA 修复，按 `1.3.1`、`1.3.2` 这类三段式修订版本记录。
7. 当前产品 UI 和 API 对外仍显示功能版本 `1.3`；台账修订号用于内部追踪小改动，不强制每次都改变对外页面主版本号。

验收口径：

- 页面必须能看到「当前客户数据」区；
- `POST /api/assessments` 必须返回 `assessment`；
- 首发链接提示应位于 7 天内容计划区域，而不是独立抢占报告版面；
- 无发布链接不得计入已闭环；
- 后续小改动必须在本台账新增 `1.x.y` 记录；
- 如新增独立功能模块，再升级到下一个 `1.x` 功能版本。

### v1.3 · 内容决策局测试样例入口 + 首发链接回填门禁

日期：2026-05-17

本版新增/修正：

1. 页面顶部新增「载入内容决策局样例」按钮，便于测试者一键进入标准测试案例。
2. 首次进入且浏览器无本地演示数据时，默认载入「内容决策局」样例，避免空看板被误判为系统无数据。
3. 「清空演示数据」后保留空状态，用户可通过样例按钮重新载入测试数据。
4. 「营销反馈回填」中的发布链接改为必填，首条/每条发布必须返回真实发布链接后才算闭环。
5. 发布计划表展示链接回填状态，缺少链接时提示“发布后需回填链接”。
6. 版本标签同步到前端页面、诊断结果、Netlify Function `/api/health` 与 API smoke test。

验收口径：

- 首页必须显示 v1.3；
- 首次进入应看到内容决策局样例数据和 7 天计划；
- 清空演示数据后可再次通过按钮载入样例；
- 保存反馈时必须填写发布链接；缺链接不得被当作完整发布闭环；
- 控制台无 JS 报错，`/api/health` 返回 1.3。

### v1.2 · 账号冷启动配置 + 起步主平台决策

日期：2026-05-17

本版新增/修正：

1. 新增「账号冷启动配置」模块：
   - 账号名建议；
   - 账号定位；
   - 小红书简介；
   - 主页关键词；
   - 头像方向；
   - 起步主平台；
   - 平台表达规则；
   - 对外称呼门禁。
2. 企业营销测试号默认独立定位：企业内容增长 / 企业获客 / AI营销复盘，不再依附 P01/PTE。
3. 默认账号建议调整为「内容决策局」。
4. 对外表达修正：避免高频使用“小老板”，优先使用老板、企业主、商家、门店老板、企业负责人。
5. 平台策略修正：小红书可适量 emoji 和精致感，但该规则不得外推到视频号、公众号、朋友圈、抖音等平台。
6. 闭环成熟度修正：新增动态闭环分，随发布回填、互动、咨询和复盘行为上升。
7. 时间显示修正：前端反馈记录、复盘时间使用北京时间格式，避免 UTC 漂移。
8. API 健康检查返回当前版本号与版本标签。

验收口径：

- 诊断结果必须显示 v1.2；
- 必须出现账号冷启动配置；
- 本账号起步主平台与目标客户适用平台必须分开；
- 初始闭环分不能虚高，回填后动态闭环分应上升；
- 首日选题不得继续使用“小老板”作为标题主称呼。

### v1.1 · 推荐起步平台

日期：2026-05-17

本版新增/修正：

1. 增加推荐起步平台能力。
2. 区分「本账号优先平台」「辅助平台」「目标客户可能适用平台」「暂不建议」。
3. 修复之前把客户平台误当作本账号发布平台的问题。
4. 修复首条内容计划日期按 UTC 漂移的问题，以北京时间为计划基准。

### v1.0 · 最小营销增长闭环

日期：2026-05-17 前

能力范围：

1. 营销体检表单；
2. 自动诊断；
3. 7天内容计划；
4. 发布反馈回填；
5. 周复盘和下一轮建议。

已知问题：

- 单一评分容易虚高；
- 平台建议颗粒度不够；
- 账号基础设置缺失；
- 反馈闭环成熟度未充分联动真实回填。
