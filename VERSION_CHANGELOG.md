# 企业营销增长工具版本更新台账

## 版本号规则

- 当前从 `1.0` 开始，采用 `1.x` 功能版本 + `1.x.y` 修订版本迭代。
- `1.0`：最小可用闭环版本，能完成营销体检、诊断、7天内容计划、反馈回填、周复盘。
- `1.1`：在 1.0 基础上增加「推荐起步平台」，并区分本账号发布平台与目标客户可能适用平台。
- `1.2`：在 1.1 基础上增加「账号冷启动配置模块」，把发布前的账号定位、简介、头像方向、平台表达规则纳入门禁。
- `1.3`：在 1.2 基础上增加「内容决策局测试样例入口」和「首次发布链接回填门禁」，解决测试页初始空状态与发布闭环无链接凭证的问题。
- `1.4`：在 1.3 基础上增加「多时间点反馈回填」，支持 T+24 / T+72 / T+7 分阶段记录，并防止复盘统计重复叠加。
- 后续规则：
  - 同一功能版本内的小功能补齐、文案修正、字段门禁、QA 修复：采用 `1.x.y` 递增，例如 `1.3.1`；
  - 新增一个清晰功能模块或业务流程节点：采用 `1.x` 递增，例如 `1.4`；
  - 数据结构或业务流程明显变化：仍在 MVP 阶段先递增 `1.x`；
  - 形成可交付客户试用版、字段与数据回流稳定后，再进入 `2.0`。

## 更新记录

### v1.6.57 · 备案信息补充版

日期：2026-07-09

本次优化：
1. 底部备案信息由占位文案更新为「网信备案：苏ICP备2026037570号-1」。
2. 暂不展示公安备案占位，避免客户看到未完成的合规项。
3. 保持客户版主流程、生成逻辑、反馈记录和内部版结构不变。

验收：
- 客户页底部显示真实备案号；
- 客户页底部不显示公安备案占位；
- `/api/health` 返回 `version: "1.6.57"`。

### v1.6.56 · 产品可信度与合规底座版

日期：2026-07-09

本次优化：
1. 客户公开页新增网站级导航，补齐「关于我们」「隐私政策」「用户协议」「联系我们」四个信息入口。
2. 页脚新增备案信息展示位，暂不填写虚假备案号，待国内备案完成后替换为正式备案号与链接。
3. 新增本浏览器旧项目提示，区分“继续上次项目”和“新建空白项目”，避免运营测试记录被误认为客户默认页。
4. 保持客户主流程、内容生成内核、反馈记录和下一轮优化逻辑不变，仅补齐产品可信度与合规展示底座。

验收：
- 默认客户首页仍为「引客罗盘」通用产品页，不默认展示篮球客户；
- `/about`、`/privacy`、`/terms`、`/contact` 可作为客户可见信息页访问；
- 页脚只展示备案占位，不伪造备案号；
- `npm test` 通过。

### v1.6.55 · 客户版品牌与产品感收敛版

日期：2026-07-09

本次优化：
1. 客户公开页产品名收敛为「引客罗盘」，强化“持续带来客户咨询”的对外心智。
2. 客户版文案从一次性的“7 天计划”改为“本轮内容计划 → 记录效果 → 下一轮优化”的循环表达，适配后续按月/按年持续服务。
3. 仅调整客户版对外呈现和主题主色，生成内核、计划数组、回填复盘与多轮循环能力不变。
4. 客户版主色从偏紫收敛到深色底上的青绿色增长色，内部版信息密度和工具结构不变。

验收：
- 客户首页不再突出固定 7 天心智；
- 保存效果后提示下一轮内容计划；
- `npm test` 通过。

### v1.6.54 · 商户画像差异化建议版

日期：2026-07-09

本次优化：
1. 在诊断和每条内容计划中新增 `merchant_profile`，记录服务类型、目标客户、当前瓶颈、转化动作和可用素材。
2. 每条计划新增 `customer_reasoning`，把“为什么这样发”拆成客户痛点依据、平台表达依据、转化动作依据、本条验证目标和发布前注意。
3. 下一轮 7 天计划继续继承商户画像、发布前检查和具体解释，避免第二轮、第三轮退回通用模板。
4. 保持客户版轻量呈现：默认仍只看标题、平台和记录入口，详细依据折叠在“为什么这样发”中。

验收：
- 子武限武术搏击计划的 `merchant_profile.service_type` 为 `martial_arts`；
- 所有计划均带 `customer_reasoning` 与 `publish_audit`；
- `npm test` 通过。

### v1.6.53 · 客户版解释与发布审核增强版

日期：2026-07-09

本次优化：
1. 将客户版“为什么这样发”从泛泛说明升级为结构化解释，方便运营人员理解每条内容背后的判断。
2. 新增发布前规则检查 `publish_audit`，优先覆盖小红书场景下的联系方式/站外导流、绝对化用词、互动诱导和课程效果承诺风险。
3. 发布前检查仅作为经验规则提示，不冒充平台官方审核结果。
4. 不新增客户版主流程、不加入内部生成工作台、不改变内容生成内核。

验收：
- 小红书计划出现发布前自查项；
- 高风险表达能被标注为中/高风险；
- 客户默认页面不暴露内部模型/QA/生成工作台信息。

### v1.6.52 · 武术搏击串行业修复版

日期：2026-07-09

本次修正：
1. 新增武术/搏击/散打/拳击/跆拳道等独立体育培训识别，不再落入少儿篮球模板。
2. 收紧少儿篮球识别条件，避免“体验课、体能训练、周末班”等泛词误触发篮球内容。
3. 为武术搏击补充专属选题、平台推荐、共创方向和下一轮复盘题池。
4. 修复客户回填后生成下一轮计划时，武术搏击内容串出“篮球课、运球、投篮”等错行业词的风险。
5. 增加子武限武术搏击 smoke test，要求输出含武术/搏击/安全/体验课语义，且不含篮球错行业词。

验收：
- 子武限武术搏击样例不出现篮球课、少儿篮球、运球、投篮、篮筐、篮球商品；
- 首轮计划、客户共创、下一轮本地复盘题池均识别为武术搏击场景；
- `npm test` 通过。

### v1.6.51 · 团队生产区隔离版

日期：2026-07-02

本次修正：
1. 冻结客户版 v1.6.50 的分步主流程，不改客户公开页体验。
2. 将内部「客户运营工作区」和「素材生产工作台」按路由隔离。
3. `/internal/generation-workbench` 只显示素材、生成任务、QA 和交付相关内容，不再混入诊断、计划、记录和复盘模块。
4. `/internal/` 默认隐藏素材生产工作台，只保留客户运营、诊断、计划和反馈复盘。
5. 增加 CSS 防线，避免未来渲染误解除 `hidden` 后再次同屏混杂。

验收：
- `/internal/` 不显示素材生产工作台；
- `/internal/generation-workbench` 不显示客户运营/诊断/计划/回填/复盘区；
- 客户公开页主流程保持不变；
- `/api/health` 返回 `version: "1.6.51"`。

### v1.6.50 · 客户分步体验版

日期：2026-07-01

本次优化：
1. 客户公开页改为 5 步式体验：填写业务 → 确认方向 → 本周计划 → 记录效果 → 下一轮建议。
2. 只显示当前步骤，避免客户首次使用时同时看到计划、记录和下一轮复盘。
3. 顶部进度条支持在已完成步骤之间切换，未完成步骤会给出友好提示。
4. 记录效果步骤只保留选择内容与关键数字填写；下一轮步骤再展示复盘判断和下一轮计划入口。
5. 保留 v1.6.49 的客户共创确认、观察标签、云端同步和连续内容周期能力。

验收：
- 默认首页只显示业务填写；
- 提交后进入方向确认，不直接堆出全部模块；
- 确认方向后只展示本周计划；
- 点击计划卡片后进入记录效果步骤；
- 保存记录后进入下一轮建议步骤；
- 内部版和模型生成逻辑不变。

### v1.6.49 · 客户共创确认版

日期：2026-07-01

本次新增：
1. 客户公开页在生成 7 天计划前增加“方向确认”层，先让客户选择本周优先测试的内容方向。
2. 篮球培训等客户会看到更贴近业务的方向卡，例如家长痛点型、教练专业信任型、体验课转化型。
3. 支持客户排除不想发的内容，例如不想太硬广、不想强调价格、不想露脸、不想拍孩子正脸、不想承诺效果。
4. 7 天计划生成时会读取 `co_creation` 字段，优先围绕客户确认方向和强调点，并避开客户排除项。
5. 发布效果记录新增“客户主要反馈”标签，下一轮建议会参考这些观察信号。
6. `co_creation` 和 `observation_tags` 会随客户云端同步进入项目库，用于行业品类样本沉淀。

验收：
- 客户表单提交后先进入方向确认，不直接强行生成计划；
- 确认方向后再生成 7 天计划；
- 少儿篮球“体验课转化型”应优先生成体验课、周末班、适合年龄、预约相关选题；
- 回填时可记录客户观察标签，并用于下一轮内容建议。

### v1.6.48 · 客户数据云端同步版

日期：2026-07-01

本次新增：
1. 客户公开页首次生成建议后，会将业务输入、诊断、7 天计划同步到云端项目库。
2. 客户记录发布效果后，会将发布链接、曝光、互动、咨询、备注和下一轮建议同步到云端项目库。
3. 客户启用第 2 / 第 3 / 第 N 轮后，会将当前轮次和历史内容周期同步到云端项目库。
4. 老客户浏览器里已有本地数据时，打开新版页面会自动补一次云端同步。
5. 云端同步失败不影响客户继续使用，本地 localStorage 仍保留完整闭环状态。

验收：
- 内部端“全部客户”列表可读取客户公开页同步后的项目；
- 进入对应客户后可查看业务信息、当前轮次、7 天计划、发布效果和复盘建议；
- 客户公开页生成、记录、启用下一轮的主流程不受云端同步失败影响。

### v1.6.47 · 连续内容周期产品化版

日期：2026-06-30

本次新增：
1. 客户版支持将“下一个七天建议”一键启用为下一轮当前计划。
2. 新增内容周期历史区，显示当前第 N 轮、已归档轮次和每轮前三个选题摘要。
3. 下一轮生成时携带历史轮次标题，避免第 2 / 第 3 / 第 N 轮重复旧选题。
4. 客户复盘模型超时收敛到 Netlify 安全范围内，模型慢时主动 fallback 到本地可用计划，避免 504。
5. 保持客户版轻量呈现，不暴露内部模型、QA 或素材生产模块。

验收：
- `npm test`；
- 客户版记录发布效果后可生成下一轮 7 天计划，并可启用为新一轮当前计划；
- 第 2 / 第 3 轮计划需避开历史选题，内容周期历史区可查看已归档轮次。

### v1.6.46 + generation-workbench-v1 · 项目化素材生成与验收工作台

日期：2026-06-18

本次新增：
1. 在 `/internal/generation-workbench` 增加项目化素材生成与验收工作台，客户默认首页不受影响。
2. 新增素材 asset 与生成任务 generation_task 两类 blobs 数据集合，沿用 `enterprise-marketing-tool-state` store，并按 client_id 分 key。
3. 新增 Provider Adapter mock：`GPT-Image-2` 封面/图片同步、`Seedance 2.0` 视频异步 submit/poll、`Claude Opus` 脚本/文案同步。
4. 新增内部 QA 状态流转，QA 未 passed 的内容不会进入客户交付区。
5. 新增飞书回写 adapter mock，预留 A 客户资料 / B 内容计划 / C 外包制作 / D 内部验收 / E 客户交付 / F 数据回流 六类视图结构。
6. 扩展客户脱敏清单，客户交付区隐藏模型、fallback、provider_job_id、error 等内部字段。

验收：
- `/api/health` 返回 `module: generation-workbench` 与 `module_version: generation-workbench-v1`；
- `npm test` 覆盖视频异步任务 submit → generating → poll → qa_pending → QA failed → QA passed，以及封面同步任务；
- 客户交付视图只返回 QA passed 且 client_visible 的任务。

### v1.6.41 · 回填复盘短模型链路版

日期：2026-06-11

本次修补：
1. 将回填后模型调用收敛为短输出：只让火山方舟生成复盘判断、下一步策略和客户短结论。
2. 下一轮 7 天计划由系统基于模型判断、回填指标和历史 7 天计划展开，避免长生成触发 Netlify 平台 504。
3. 超时上限恢复到平台安全范围内，线上若模型失败继续明确 fallback_reason，不冒充模型输出。

验收：
- npm test；
- 线上 customer-growth-advice 烟测需返回 7 条 next_7_day_plan，并保留模型证据。


### v1.6.40 · 回填复盘模型超时修正版

日期：2026-06-11

本次修补：
1. 将火山方舟调用默认超时时间从 25 秒提高到 45 秒，上限提高到 60 秒，避免回填后 7 天计划生成被过早截断。
2. 保持 fallback 证据透明：若模型仍失败，继续返回 provider/fallback/fallback_reason，不冒充正式模型输出。
3. 继续保留 v1.6.38 冻结锚点和 v1.6.39 回填闭环结构。

验收：
- npm test；
- 线上 /api/customer-growth-advice 模型烟测需确认 provider=volcengine_ark 且 fallback=false。


### v1.6.39 · 回填复盘与下轮7天计划版

日期：2026-06-11

本次修补：
1. 冻结 v1.6.38 火山方舟接入版为可回滚锚点，并保存工作树快照、diff、变更清单和 tag。
2. 客户版效果回填从“下一条建议”升级为“复盘判断 + 下一轮 7 天内容计划”。
3. 回填字段保持营销复盘边界：曝光/播放、点赞、收藏、评论、分享、私信/咨询、预约/到店和备注，不引入 CRM/ERP/销售跟进流程。
4. /api/customer-growth-advice 返回 next_round、review_judgment、customer_summary、next_7_day_plan 和模型证据，火山失败时显式 fallback。
5. 客户版和 /internal/ 共用主闭环能力；内部调试能力仍由 /internal/ 路由门禁保留，/?mode=internal 仍为客户版。

验收：
- npm test；
- Netlify 生产部署后验证 /api/health、客户页、篮球预填链接、/?mode=internal 和 /internal/；
- 线上模型烟测确认 provider/fallback/requested_model/actual_model 证据。


### v1.6.38 · 豆包/火山方舟模型接入版

日期：2026-06-10

本次修补：
1. public 客户版首轮 7 天内容建议默认接入火山方舟 OpenAI-compatible Chat Completions API。
2. 客户发布效果记录后的下一步建议默认接入同一条火山方舟模型链路。
3. 增加统一 `model_info / generation_meta` 模型证据，明确 provider、requested_model、actual_model、fallback、fallback_reason、latency_ms 和 usage。
4. 未配置 `ARK_API_KEY` 或模型 Endpoint ID 时，明确回退到 `rule_template`，不冒充真实 AI。
5. internal 版保留 `model_provider / model_mode` 后端路由入口，后续可测试 Ark、OpenAI、Anthropic 或 local。

验收：
- `node --check static/app.js`；
- `node --check netlify/functions/api.mjs`；
- `npm test`；
- Netlify preview 验证 `/api/assessments` 与 `/api/customer-growth-advice` 均返回模型证据。

### v1.6.37 · AI项目入口门禁版

日期：2026-06-07

本次修补：
1. 内测新增项目入口增加「系统理解卡」，把 AI 从一句业务描述中提取到的业务、目标、客群、平台、问题、主推产品、客户顾虑和素材依据显性展示。
2. 增加缺项提示：当主推产品/服务、客户真实顾虑、现有素材/近期最好内容不足时，先提示补齐，不再让系统直接生成泛模板。
3. 增加生成门禁：`internal_test` 模式下，后端同步拦截缺少关键精度字段的提交，防止前端绕过。
4. 客户公开版不展示「AI项目助手 / 系统理解卡 / 生成门禁」等内测入口文案，继续保持原客户轻表单主路径。
5. 版本统一升级为 `1.6.37`。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `npm test` 通过；
- 本地与线上验证 `/` 客户页无内测入口文案；`/internal` 新增项目入口可展示系统理解卡和缺项提示；
- 线上部署：`https://sales-improve.fpmatrix.cn`，deploy `6a24f32c5cc5de2f94e02d05`。

### v1.6.31 · 项目数据隔离与安标检测模块边界修复版

日期：2026-06-05

本次修补：
1. 修复安标检测客户脚本反馈面板错误出现在“轻工业作战台”的问题。
2. 根因：安标检测客户反馈展示门禁曾把“轻工业”和泛化“检测”写入命中条件，导致轻工业项目也被判定为安标检测项目。
3. 新门禁只允许明确命中：安标检测、安标、医疗器械注册、医疗器械送检、注册送检。
4. 将“安检项目 / 客户真实数据”更名为“安标检测 / 客户脚本反馈样本”，明确该模块是项目级客户反馈资产，不属于作战台核心运营指标。
5. 面板默认 `hidden`，只在明确命中安标检测项目后展示，避免首屏加载闪现和跨项目误显。
6. 新增并更新 `PROJECT_DATA_BOUNDARY.md`，明确工作台主数据、客户反馈资产、模块承载位置、项目级数据隔离和新增真实客户数据模块前的设计要求。

验收：
- `npm test` 通过；
- 线上已验证：轻工业作战台不显示安标检测反馈模块；切换到安标检测项目后显示“安标检测客户脚本确认表”；页面不再出现“安检项目/安检客户”旧称。

### v1.6.28 · 首次生成主路径收口版

- 客户首次生成建议后，自动收起 STEP 1 填写表单，主路径聚焦到“内容建议 + 7 天发布计划 + 效果记录”。
- 刷新页面后如本地已有生成结果，自动恢复 STEP 2/STEP 3，不再把客户带回空白首访表单。
- “修改信息并重新生成”会显式展开 STEP 1，并回填上次输入，避免客户找不到编辑入口。
- 统一前端与 API 版本标记为 `1.6.28`，并更新前端缓存参数。

### v1.6.27 · 计划卡片回填稳定修复版

- 修复计划卡片按钮回填链路：任意计划卡片按钮点击后，稳定写入 `#feedbackForm [name=content_plan_id] = plan.id`。
- 修复 `#selectedPlanDisplay` 同步显示，点击后稳定显示 `计划 #N`。
- 移除计划卡片按钮对 `Number(plan.id)` 的依赖，保留原始 `plan.id`，避免非纯数字 ID 或字符串 ID 被写成 `NaN`/空值。
- 增加 `data-plan-id` + 委托点击绑定，降低动态渲染后按钮事件丢失风险。
- 回填保存时保留 `content_plan_id` 原始值，并用统一 `samePlanId()` 做计划匹配与去重。

### v1.6.26 · 新增项目反馈闭环修复版

- 修复反馈回填后 `project_stage` 未在保存前同步的问题，避免项目列表/跨端状态仍显示“待启动”。
- 验证新增项目 → 计划 → 发布链接回填 → feedback → review → projectStore 持久化闭环。

### v1.6.25 · 新增项目工作台闭环修复版

日期：2026-05-29

本次修补：
1. 内测新增项目入口统一客户轻表单字段，同时保留内测智能诊断、作战台、反馈回填和周复盘能力。
2. 修复平台/最大问题 chip 在真实点击路径下未写入 canonical hidden fields 的风险，补充 button 直绑、pointerup/click 委托和提交前二次同步。
3. 新增项目提交后强制进入 `projects[] + activeProjectId`，当前项目工作台、计划、反馈、周复盘读取同一项目状态。
4. 保持 `/api/state` 云端项目状态同步路径，用于 PC / 移动端刷新后读取同一项目集合。
5. 本版本为内测闭环验证版，客户公开页不展示内部 smart_context。

验收：
- `node --check static/app.js`；
- `node --check netlify/functions/api.mjs`；
- `node tests/api_smoke.mjs`；
- 本地 API bridge 下验证新增项目、历史项目恢复、反馈回填、刷新同步和云端状态读取。

### v1.6.17 · 客户首页视觉基线修复版

日期：2026-05-28

本次修补：
1. 保留 v1.6.16 的客户试用首页、5 个问题填写和所见即所得引导图。
2. 修复客户默认首页误用浅色 SaaS 壳的问题，视觉基线重新对齐内部验证版的深色经营工具风格。
3. 客户表单、填写引导图、按钮卡片、生成结果和发布效果记录统一使用深色卡片/看板样式。
4. 不新增产品功能，不改变客户首页信息架构，不影响内部验证入口。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 线上默认首页仍为客户试用版，但视觉色系与内部验证版保持一致。

### v1.6.16 · 所见即所得填写引导版

日期：2026-05-28

本次修补：
1. 默认客户首页新增“所见即所得填写引导图”，用模拟表单贴着 5 个真实字段说明怎么填写。
2. 引导图字段名、平台选择、最大问题选项和真实表单保持一致，避免客户看完示例后找不到对应输入项。
3. 桌面端在表单旁展示示例，手机端默认折叠为“不知道怎么填？看示例”，降低首屏压力。
4. 增加“我知道怎么填了”按钮，收起示例并聚焦第一个输入框。
5. 保留客户版生成建议、复制建议、发布后记录效果和内部验证入口。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 线上默认首页引导图不展示内部词，真实表单仍可提交并生成建议。

### v1.6.15 · 客户试用入口版

日期：2026-05-28

本次修补：
1. 默认 `/` 改为真实客户试用版，隐藏版本号、内部看板和内部术语。
2. 新增内部验证入口 `/internal`，保留作战台、回填、周复盘、判断依据等内部验证能力。
3. 客户首页改为 5 个问题表单，当前最大问题使用按钮卡片选择。
4. 生成结果改为客户可读的内容建议：当前问题、内容方向、3 个选题、第一条怎么发、发布后记录效果。
5. 客户版增加复制建议和轻量发布效果记录，记录保存在当前浏览器。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 线上默认首页不展示内部版本号或内部系统语言，`/internal` 可访问内部验证版。

### v1.6.13 · 可读性完整修复版

日期：2026-05-28

本次修补：
1. 顶部版本信息改成可见胶囊标签，提高对比度，不再像灰色弱提示。
2. 新增项目页「返回当前运营项目」从表单内部移到首屏右上「更多」按钮左侧，进入新项目时优先可见。
3. 「客户输入与诊断依据」从密集双列表改为“客户输入摘要 / 系统诊断摘要”两张卡，完整字段折叠到「查看完整客户输入」。
4. 完整 7 天计划从密集表格改为逐条卡片：主题、状态、日期/平台、发布角度、合规承接、观察目标和操作分区展示。
5. 保留 v1.6.12 的周复盘指标卡和按钮显性化，但补齐证据区与完整计划的可读性。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 浏览器验证：新项目返回按钮在「更多」左侧；版本标签高对比显示；客户证据区为摘要卡；完整 7 天计划为卡片列表。

### v1.6.12 · 复盘区可读性修复版

日期：2026-05-27

本次修补：
1. 周复盘从大段文字改为卡片化结构：顶部时间卡、4个指标卡、胜出主题/当前瓶颈/下一步动作分区展示。
2. 「更新复盘」移到「周复盘」标题同一行，改为蓝绿渐变主按钮，并加刷新符号，降低“看不出能点”的问题。
3. 顶部「当前项目」标签强制单行展示，项目下拉框用 flex 布局，避免“当前项目”几个字被挤成两行。
4. 回填记录改成轻量标签式指标，减少一整段文字堆叠。
5. 调整回填/复盘两列空间分配与间距，复盘列优先保证阅读宽度；窄屏自动单列。
6. 保留 v1.6.11 的移动端 hidden 修复，避免新项目创建页露出回填/复盘模块。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器验证：`#reviewBtn` 位于 `.review-panel-head` 内，样式为 `review-primary-btn`；复盘卡片包含 4 个指标卡；「当前项目」标签 `white-space: nowrap`；视觉二次验收未见明显换行/按钮不可点击问题。

### v1.6.11 · 移动端隐藏状态修复版

日期：2026-05-27

本次修补：
1. 修复移动端「新项目创建 / 首访填写」页面底部错误露出「内容数据回填」「周复盘」的问题。
2. 根因：`war-room-v1.6.1.css` 在移动端使用 `.feedback-focus{display:grid!important}`，覆盖了 HTML `hidden` 状态。
3. 在最后加载的战情室 CSS 末尾增加 hidden-state guard：`[hidden] / .feedback-focus[hidden] / .grid[hidden] / .panel[hidden] / .cards[hidden]` 强制 `display:none!important`。
4. 保留 v1.6.10 的导航修复：作战台按钮恢复、新增项目页可返回当前运营项目、计划编号按项目内 #1/#2/#3 显示。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器验证：进入新项目填写状态后，`#feedbackWorkflow.hidden === true` 且 computed display 为 `none`；页面文本不再包含「内容数据回填」「周复盘」。

### v1.6.10 · 页面降噪与导航修复版

日期：2026-05-27

本次修补：
1. 恢复作战台头部右侧操作按钮区：保留「主动作 / 查看计划 / 周复盘」，避免状态卡右侧空白。
2. 新增项目填写页增加「返回当前运营项目」入口，可回到最近运营项目。
3. 内容计划显示编号改为当前项目内序号：即使后端真实 plan id 是 8/9/10，界面仍显示 #1/#2/#3；回填仍使用真实 id，不影响数据保存。
4. 保留 v1.6.9 已确认的降噪逻辑：顶部不恢复伪主按钮/看示例，依据入口继续收口到复盘区域。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器验证：作战台头部按钮恢复；新增项目页出现返回当前运营项目；模拟 plan id=8/9/10 时界面显示 #1/#2/#3，今日动作显示发布 #1。

### v1.6.9 · 页面降噪与主路径收口版

日期：2026-05-27

本次修补：
1. 以线上 `v1.6.6 · 回填闭环与标题体验修复版` 为界面地基，撤回本地 v1.7.x 首访重构思路，不混入新视觉/新框架/新产品方向。
2. 删除首屏标题旁重复主按钮：不再长期展示「开始增长诊断 / 查看周复盘」和「看示例」。
3. 合并「依据 R / 为什么 / 查看依据」为一个用户可理解入口：「查看判断依据」。
4. 7天内容计划默认只展示前三条扫读卡片：标题、平台、状态、日期/反馈、核心操作；发布角度和目标收进展开层。
5. 「内容复盘依据」移动到「回填记录」之后，符合发布 → 回填 → 记录 → 复盘 → 判断依据的生成路径。
6. 「客户输入与诊断依据」并入「周复盘」右侧的复盘与判断依据区域，作为复盘依据链条的一部分，不再单独插在计划执行前后打断主路径。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器 `http://127.0.0.1:4173` 载入样例后确认：无首屏伪主按钮/看示例；作战台只保留一个「查看判断依据」；计划卡片默认 3 条；复盘依据在回填记录后；Console 0 error。

### v1.7.3 · 战情室视觉基线恢复版

日期：2026-05-27

本次修补：
1. 将 `static/war-room-v1.6.1.css` 精确恢复到 commit `183fec6` 的深色战情室视觉基线。
2. 移除 `8ffee10` 的浅色 v17 视觉覆盖作为当前视觉来源。
3. 保留 5 问题首访流程、问题按钮、生成建议、前三条计划和发布后回填链路。
4. 同步前端与 Netlify Function 版本号为 `1.7.3`。

验收：
- `static/war-room-v1.6.1.css` 与 `183fec6` 对应文件一致；
- 线上 CSS body 背景包含 `#08090a / #0c0d10 / #111214`；
- `/api/health` 返回 `version: "1.7.3"`。

### v1.7.2 · CSS 精确回滚版

日期：2026-05-27

本次修补：
1. 将 `static/war-room-v1.6.1.css` 精确恢复到 commit `8ffee10` 的版本。
2. 保留 v1.7.1 的 5 问题首访流程、问题按钮、生成建议和发布后回填链路。
3. 不新增视觉设计、不新增暖色覆盖、不新增 clean 视觉变量。
4. 同步前端与 Netlify Function 版本号为 `1.7.2`。

验收：
- `static/war-room-v1.6.1.css` 与 `8ffee10` 对应文件一致；
- `/api/health` 返回 `version: "1.7.2"`；
- 首访 5 问题流程、回填预填和反馈复盘保持可用。

### v1.7.1 · 视觉回滚 + 信息架构保留版

日期：2026-05-27

本次修补：
1. 保留 v1.7.0 的首访信息架构：客户先填 5 个问题，再生成内容建议。
2. 撤回 v1.7.0 末尾新增的强视觉覆盖，恢复早期轻量、暖色、卡片式页面质感。
3. 首访页面不再静态露出新增项目、更多操作等内部入口，避免首次打开产生后台感。
4. 保留最大问题按钮、生成建议、发布后回填、刷新恢复和自动复盘能力。
5. 不新增自动发布、自动抓取、账号矩阵或内部后台功能。

验收：
- 首访首屏仍只展示 5 问表单和生成按钮；
- 页面视觉回到早期轻量风格，不再像新的视觉设计稿；
- 点击内容计划的发布后回填仍能带入计划 ID；
- 保存反馈后回填记录和复盘摘要继续同步更新；
- `/api/health` 返回 `version: "1.7.1"`。

### v1.7.0 · 首访填写体验修复版

日期：2026-05-27

本次整改：
1. 首访页重新回到客户视角：标题改为「帮你找到下一周该发什么内容」，首屏聚焦 5 个问题和生成按钮。
2. 取消首次进入自动载入运营样例，避免客户还没填写就看到作战台、闭环率、复盘数据。
3. 「当前最大问题」从传统下拉框改为 4 个可点击问题按钮，降低首访卡住风险。
4. 生成后默认只展示当前最大问题、本周建议、7天内容计划、发布后回填提醒；复杂复盘数据后置到次级入口。
5. 保留发布后回填、刷新恢复、自动复盘等能力，但不在首访页面堆内部后台信息。

验收：
- 首访页面不再默认展示内部作战台或样例数据；
- 5 项表单可顺利填写，最大问题按钮可选中并参与生成；
- 生成结果清爽展示 4 个核心块；
- 点击计划卡片可进入回填，保存反馈后刷新仍保留；
- `/api/health` 返回 `version: "1.7.0"`。

### v1.6.8 · 回填复盘依据收口版

日期：2026-05-27

本次修补：
1. 将「复盘与判断依据」从计划执行前移到内容数据回填、回填记录、周复盘附近，符合“发布计划 → 数据回填 → 回填记录 → 周复盘 → 判断依据 → 下一步判断”的闭环路径。
2. 用户界面不再显性展示 K/V/R，只展示「客户档案 / 系统判断 / 内容复盘依据」。
3. 回填字段恢复为「曝光｜查看 / 互动｜点赞 / 互动｜评论 / 互动｜收藏/分享 / 转化｜私信/咨询」，把数据从 vanity metrics 拉回增长判断。
4. 移动端优化回填与复盘区域：回填表单单列、保存按钮更清晰、回填记录卡片流、周复盘摘要优先、判断依据默认收起。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `python3 -m pytest -q` 通过；
- `node tests/api_smoke.mjs` 通过；
- 线上需验证 v1.6.8 版本号、回填/周复盘/复盘与判断依据位置与移动端布局。

### v1.6.6 · 回填闭环与标题体验修复版

日期：2026-05-26

本次修补：
1. 将已验收的回填闭环与标题体验修复正式补标为 v1.6.6。
2. 内容标题不再明显硬拼接客户长输入，优先生成可直接作为选题的人话标题。
3. 「复制/回填」按钮可自动带入发布计划 ID，并定位到内容数据回填区域。
4. 保存反馈后刷新页面仍能保留，周复盘会根据回填数据自动更新。
5. 本次只修正版本号和版本说明，不引入新框架、新数据库或产品方向变化。

验收：
- `/api/health` 返回 `version: "1.6.6"`；
- 页面展示 `v1.6.6 · 回填闭环与标题体验修复版`；
- 回填预填、刷新保留和自动周复盘能力保持可用。

### v1.6.5 · 依据中心与复盘判断升级版

日期：2026-05-26

本次修补：
1. 将「客户输入与诊断依据」升级为 K/V/R 三层依据中心：K = 客户原始输入，V = 系统诊断依据，R = 内容复盘依据。
2. 今日动作和下一步判断不再共用模糊依据：有真实反馈时优先锚定 R 层；无反馈时分别锚定 K 或 V。
3. 下一步计划优先读取最新复盘依据：正式周复盘优先，没有正式复盘时读取自动复盘。
4. R 层默认只展示最新复盘和最近 5 条反馈，更多历史折叠提示，避免页面无限拉长。
5. 内容数据回填字段改为「曝光｜查看 / 互动｜点赞评论收藏分享 / 转化｜咨询」，减少把互动误当增长结果的问题。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器打开 `/?v=165`，点击「看示例」后可见 K/V/R 依据中心，今日动作和下一步判断均可指向 R 层，控制台无 JS error。

### v1.6.3 · 客户输入/诊断依据 K/V 区分版

日期：2026-05-26

本次修补：
1. `客户输入与诊断依据` 不再把所有字段混在同一组信息卡里。
2. 新增 K/V 双栏：K = 客户原始输入，V = 系统诊断依据。
3. K 区保留行业、目标、客户、平台、痛点、对标账号等客户填写信息；V 区展示阶段、策略分、最大问题、核心判断、下一步、评分说明和风险提醒。
4. 移动端自动改为上下堆叠，避免双栏挤压。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器打开 `/?v=163`，点击“看示例”与“查看依据”，已显示 K/V 分栏，控制台无 JS error。

### v1.6.2 · 企业营销增长驾驶舱体验修补版

日期：2026-05-26

本次修补：
1. 首屏主按钮按项目状态切换：未诊断=开始增长诊断；待启动=查看内容计划；运营/待回填=回填发布数据；复盘期=查看反馈复盘。
2. 产品标题恢复为 `企业营销增长驾驶舱`，首屏 Slogan 改为 `不只发内容，更要知道哪条内容带来客户。`
3. `内容实验` / `反馈复盘` 本轮采用方案A：先做页内定位锚点，不拆多页面 Tab。
4. 优化 `更多` 数据操作菜单文案、关闭行为和移动端按钮布局。
5. 修复无企业名称时显示“未命名客户”的问题，改为优先用行业生成作战台名称。
6. 修复刷新后未恢复本地作战台的问题：检测到有效 diagnosis/plans/project/cycle 状态时直接恢复工作台。
7. 强化计划卡片回填按钮：自动填入计划 ID，并带入已有发布链接和反馈数据。
8. 不新增自动发布、自动抓取、自动登录或账号矩阵能力；仍只做客户信息收集、数据回填分析、内容策略优化。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器打开 `/`，首屏标题、Slogan、状态按钮和样例驾驶舱页签无 JS error。

### v1.6.1 · 企业增长作战室界面版

日期：2026-05-25

本次迭代：
1. 将运营周期作战台落地为深色 SaaS 决策驾驶舱：顶部状态条、今日动作、下一步判断、关键数据卡、内容实验列表同屏展示。
2. 新增独立样式文件 `static/war-room-v1.6.1.css`，避免历史 `styles.css` 兼容问题影响新版驾驶舱视觉。
3. 页面标题和前端版本展示更新为 `v1.6.1 · 企业增长作战室`。
4. 内容计划卡片改为实验列表样式，保留平台、状态、目标信号、曝光/收藏/咨询等可扫读数据。
5. 不新增自动发布、自动抓取、自动登录或账号矩阵能力；仍只做客户信息收集、数据回填分析、内容策略优化。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过；
- 本地浏览器打开 `/`，样例数据可展示顶部状态条、今日动作/下一步判断、关键数据卡、内容实验列表；控制台无 JS error。

### v1.6.0 · 首次客户友好版

日期：2026-05-25

本次整改：
1. 首屏从“运营驾驶舱优先”改回“首次客户 3 分钟诊断入口”，解决新客户不知道从哪里开始的问题。
2. 表单首屏只保留 5 个核心问题：行业、目标、客户、平台、最大问题；其他内部/高门槛信息默认收起。
3. 对标账号从强制可见模块移入可选补充，避免第一次填写就被“对标/数据/复盘”等内部语言压住。
4. 回填区域默认隐藏，避免客户还没生成建议就看到发布后运营字段。
5. 版本号统一为 v1.6.0，前端与 Netlify Function 保持一致。

验收：
- 首屏必须明确显示“从这里开始填写”；
- 首次客户无需理解运营周期、回填、复盘等术语，也能完成第一步；
- 必填项只保留生成建议所需的最小信息；
- 不新增自动发布、自动抓取、自动登录或账号矩阵能力；
- `node --check static/app.js`、`node --check netlify/functions/api.mjs`、`node tests/api_smoke.mjs` 必须通过。

### v1.5.7 · 运营周期优先版

日期：2026-05-23

本次修补：
1. 将默认展示逻辑改为运营周期优先：项目已有计划/发布/反馈/复盘时，首屏先看驾驶舱，而不是重新诊断表单。
2. 修正空状态文案：明确“当前浏览器未检测到运营数据”，避免客户误解为线上数据丢失。
3. 样例入口升级为运营样例：默认带 7 条计划、1 条已发布内容、T+72 回填数据和自动周复盘。
4. 首屏指标恢复为计划内容、已发布、回填率、曝光、互动、咨询数、闭环分，强化数据部分可见性。
5. 诊断表单降级为新客户/重新诊断入口，运营中项目优先展示发布、回填、复盘和下一步建议。

验收：
- 页面标题与版本号显示 v1.5.7；
- 空状态不得暗示数据丢失；
- 点击“查看运营样例”后必须进入运营/复盘驾驶舱，并展示数据指标、发布计划、回填、周复盘；
- `node --check static/app.js`、`node --check netlify/functions/api.mjs`、`node tests/api_smoke.mjs` 必须通过。

### v1.5.5.1 · 客户理解修补版

日期：2026-05-22

本次修补：
1. 保留 v1.5.5 的状态分层与驾驶舱结构，不回滚 v1.5.4。
2. 修复按钮过度精简导致的新用户理解成本问题。
3. 恢复少量极短微提示，帮助首次使用者理解「诊断 → 发布 → 回填 → 复盘」流程。
4. 将关键按钮从纯短词恢复为业务动作，例如「开始诊断」「载入样例」「查看依据」「回填/复盘」「保存反馈」。
5. 不新增自动发布、自动抓取、自动登录或账号矩阵能力。

### v1.5.2 · 回填体验增强版

日期：2026-05-21

本次 QA 修复：

1. 点击「去回填计划 #ID」或「发布后回填」后，自动展开反馈回填区域并填入发布计划 ID。
2. 页面平滑滚动到「首发链接回填 / 营销反馈回填」，并自动聚焦发布链接输入框。
3. 回填区域增加短暂高亮效果，配合提示文案，让测试人员明确知道按钮已生效。
4. 保存反馈成功后提示「反馈已保存，看板已更新。」并保持看板、计划状态、反馈记录同步刷新。
5. 保存反馈时补齐发布计划 ID 必填校验，错误提示改为业务语言。

验收：
- `/api/health` 必须返回 `version: "1.5.2"`；
- 点击任意回填按钮后，计划 ID、滚动、焦点、高亮和提示必须同时生效；
- 不得破坏 v1.5 工作台、样例入口、反馈保存、指标计算和周复盘。

### v1.5.1 · 客户展示体验与审美修复版

日期：2026-05-21

本次体验更新：

1. 首屏从“项目生命周期说明”升级为客户-facing 增长驾驶舱：突出今天做什么、结果怎么样、下一轮怎么赢。
2. 弱化内部字段裸露：`cycle-1` 前台展示为“第1轮增长周期”，按钮改为“进入下一轮”。
3. 工作台卡片改为“本轮成果 / 今天先做 / 值得复制 / 风险提醒”，更适合老板和客户 10 秒扫读。
4. 追加高质感 SaaS Dashboard 视觉覆盖：玻璃拟态卡片、留白、柔和阴影、焦点态、暗色主指标卡。
5. 客户数据与诊断依据默认收起，保留内部核对能力，但不干扰展示体验。

验收：
- 首屏不得再裸露 `cycle-1` 作为客户可见周期；
- 页面标题与版本号必须显示 v1.5.1；
- 首页第一屏必须优先呈现增长驾驶舱，而不是大表单或内部工程字段；
- `node --check static/app.js`、`node --check netlify/functions/api.mjs`、`node tests/api_smoke.mjs` 必须通过。

### v1.5.0 · 项目生命周期工作台

日期：2026-05-21

本次功能更新：

1. 首页升级为「项目生命周期工作台」：根据项目阶段优先展示今日动作、本周期成果、最佳内容和当前风险。
2. 新增最小项目状态模型：`project.id`、`project.name`、`project_stage`、`current_cycle_id`，计划和反馈记录补充 `project_id` / `cycle_id`。
3. 生命周期阶段从“未诊断 → 待启动 → 运营中 → 复盘期”自动推断，避免运营项目每天回到诊断表。
4. 新项目/重新诊断仍可从首页入口进入，但运营期默认先看成果与缺口。
5. 支持“进入下一周期”，用于把一轮复盘后的动作沉淀为下一轮内容增长实验。

验收：
- 首页必须先显示项目工作台，而不是直接显示大表单；
- 样例数据应进入“待启动”阶段，并显示本周期成果和今日动作；
- 发布回填后应进入“运营中”；
- 生成复盘后应进入“复盘期”；
- `node --check static/app.js`、`node --check netlify/functions/api.mjs`、`node tests/api_smoke.mjs` 必须通过。


### v1.4.1 · 极简录入 + 对标账号独立模块

日期：2026-05-20

本次体验优化：

1. 首页极简化：首屏只保留业务核心字段和一个独立对标账号模块。
2. 对标账号从“补充资料”中独立为核心模块，强调用真实市场信号校准选题，而不是让 AI 凭空编爆款。
3. 高级补充默认隐藏，企业名称、联系人、预算、决策周期、内容资产等不再压迫首次填写。
4. 生成结果前弱化反馈/复盘模块，仅保留轻提示；生成计划后再展示发布链接回填、T+24 / T+72 / T+7 和周复盘。
5. 保留 v1.4.0 的对标账号主题参考、多阶段反馈、最新阶段聚合统计和周复盘逻辑。

验收：
- 首屏必须更像“少填信息即可生成建议”，而不是大表单；
- 对标账号必须独立可见；
- 未生成计划前不展开完整反馈表和周复盘；
- 不得新增自动抓取、自动登录或自动发布。

### v1.4.0 · 对标账号主题参考 + 多阶段反馈回填

日期：2026-05-20

本次功能更新：

1. 新增「对标账号主题参考」模块。
2. 第一轮主题建议加入市场校准层，不再只依赖客户自述和系统判断。
3. 支持客户手动输入对标平台、对标账号链接、备注和代表爆款内容/标题/链接/数据摘要。
4. 7天内容计划会提炼对标账号中已验证的痛点和标题结构，但只做主题转译，不照抄原文。
5. 暂不接自动抓取 Skill，不接小红书/抖音自动爬取，不自动登录，不自动发布，避免自动化风险。

验收：
- 页面必须出现「对标账号主题参考」；
- 页面必须提示「对标账号不是用来抄袭，而是用来判断市场已经验证过哪些痛点和表达方式。」；
- assessment 必须包含 `benchmark` 字段；
- 保留 T+24 / T+72 / T+7 多阶段反馈回填。

### v1.4 安全修正 · 移除评论引导

日期：2026-05-19

本次修正：

1. 移除内容决策局与客户内容计划中的评论区/留言式引导。
2. CTA/承接统一改为私信、主页咨询、保存、数据回填等低风险表达。
3. 页面表头从 `CTA` 调整为 `合规承接`，避免继续诱导客户使用评论区引导。
4. 保留「评论数」作为数据指标，但不再生成“评论区告诉我 / 留言某关键词”等动作建议。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过。

### v1.4 · 多时间点反馈回填

日期：2026-05-19

本次功能更新：

1. 将「首发链接回填 / 营销反馈回填」升级为多时间点入口：
   - 新增反馈时间点字段：`T+24`、`T+72`、`T+7`；
   - 同一发布计划可保留多个时间点反馈记录，观察内容生命周期，而不是只看一次数据。
2. 修正统计口径：
   - 看板、动态闭环分、周复盘只使用每条内容的最新时间点数据；
   - 避免 T+24 + T+72 + T+7 被重复累加，造成曝光、互动、咨询虚高。
3. 修正重复提交口径：
   - 同一计划 + 同一反馈时间点重复提交时覆盖旧记录；
   - 同一计划不同时间点同时保留。
4. 验收：
   - `node --check static/app.js` 通过；
   - `node --check netlify/functions/api.mjs` 通过；
   - `node tests/api_smoke.mjs` 覆盖 T+24/T+72 多行保留与最新口径统计。

### v1.3.2 · 快速体检 + 首屏关键结论

日期：2026-05-18

本次小修订/修正：

1. 将「客户营销体检」从大表单压缩为「5项快速体检」：
   - 首屏只保留行业、主要目标、目标客户、当前平台、最大问题、企业名称；
   - 产品/服务入口、客户痛点、内容资产、预算、决策周期等改为可选展开字段；
   - 降低第一次填写阻力，先生成判断，再补资料。
2. 将「关键结论」提前到首屏右侧：
   - 用户不需要滚动很久才能看到诊断；
   - 诊断默认只展示优先问题、核心诊断、下一步；
   - 账号配置、平台建议、评分说明等改为可展开详情。
3. 将「客户数据/诊断依据」默认收起：
   - 保留可审计性；
   - 不再占用主页面高度。
4. 将「7天计划」默认压缩为前三个优先动作：
   - 完整计划表改为展开查看；
   - 页面长度明显降低。
5. 验收：
   - `node --check static/app.js` 通过；
   - `node --check netlify/functions/api.mjs` 通过；
   - `node tests/api_smoke.mjs` 通过；
   - 本地浏览器控制台无 JS error。

### v1.3.1 · 客户数据展示修复 + 首发链接门禁体验调整

日期：2026-05-17

本次小修订/修正：

1. 修复「客户展示」问题：
   - 将「客户营销体检」输入区与「最新诊断报告」展示区拆开；
   - 新增「当前客户数据」展示区，显示本次真正进入诊断引擎的客户输入；
   - 避免出现“诊断有了，但客户数据为空/看不到”的误判。
2. 修复后端 assessment 返回：
   - `POST /api/assessments` 返回中补充 `assessment` 对象；
   - 前端提交体检或载入样例后，可直接用后端返回的 assessment 渲染客户数据。
3. 调整「首发链接回填门禁」交互层级：
   - 从独立大模块降级为计划表区域内的轻提示；
   - 保留“发布后必须回填链接”的闭环提示，但不抢占诊断报告版面。
4. 验证链接回填链路：
   - 缺少发布链接时不能被计为已闭环；
   - 计划/看板统计按 `已发布 + publish_link` 计算；
   - 链接回填后，反馈、看板、闭环分和下一轮建议可联动更新。
5. v1.3 已完成线上部署测试，线上 `/api/health` 返回：`v1.3 · 内容决策局测试样例入口 + 首发链接回填门禁`。
6. 从本条开始，后续同一功能版本内的小修改、补丁、文案、字段和 QA 修复，按 `1.3.1`、`1.3.2` 这类三段式修订版本记录。
7. 当前产品 UI 和 API 对外仍显示功能版本 `1.3`；台账修订号用于内部追踪小改动，不强制每次都改变对外页面主版本号。

验收口径：

- 页面必须能看到「当前客户数据」区；
- `POST /api/assessments` 必须返回 `assessment`；
- 首发链接提示应位于 7 天内容计划区域，而不是独立抢占报告版面；
- 无发布链接不得计入已闭环；
- 后续小改动必须在本台账新增 `1.x.y` 记录；
- 如新增独立功能模块，再升级到下一个 `1.x` 功能版本。

### v1.3 · 内容决策局测试样例入口 + 首发链接回填门禁

日期：2026-05-17

本版新增/修正：

1. 页面顶部新增「载入内容决策局样例」按钮，便于测试者一键进入标准测试案例。
2. 首次进入且浏览器无本地演示数据时，默认载入「内容决策局」样例，避免空看板被误判为系统无数据。
3. 「清空演示数据」后保留空状态，用户可通过样例按钮重新载入测试数据。
4. 「营销反馈回填」中的发布链接改为必填，首条/每条发布必须返回真实发布链接后才算闭环。
5. 发布计划表展示链接回填状态，缺少链接时提示“发布后需回填链接”。
6. 版本标签同步到前端页面、诊断结果、Netlify Function `/api/health` 与 API smoke test。

验收口径：

- 首页必须显示 v1.3；
- 首次进入应看到内容决策局样例数据和 7 天计划；
- 清空演示数据后可再次通过按钮载入样例；
- 保存反馈时必须填写发布链接；缺链接不得被当作完整发布闭环；
- 控制台无 JS 报错，`/api/health` 返回 1.3。

### v1.2 · 账号冷启动配置 + 起步主平台决策

日期：2026-05-17

本版新增/修正：

1. 新增「账号冷启动配置」模块：
   - 账号名建议；
   - 账号定位；
   - 小红书简介；
   - 主页关键词；
   - 头像方向；
   - 起步主平台；
   - 平台表达规则；
   - 对外称呼门禁。
2. 企业营销测试号默认独立定位：企业内容增长 / 企业获客 / AI营销复盘，不再依附 P01/PTE。
3. 默认账号建议调整为「内容决策局」。
4. 对外表达修正：避免高频使用“小老板”，优先使用老板、企业主、商家、门店老板、企业负责人。
5. 平台策略修正：小红书可适量 emoji 和精致感，但该规则不得外推到视频号、公众号、朋友圈、抖音等平台。
6. 闭环成熟度修正：新增动态闭环分，随发布回填、互动、咨询和复盘行为上升。
7. 时间显示修正：前端反馈记录、复盘时间使用北京时间格式，避免 UTC 漂移。
8. API 健康检查返回当前版本号与版本标签。

验收口径：

- 诊断结果必须显示 v1.2；
- 必须出现账号冷启动配置；
- 本账号起步主平台与目标客户适用平台必须分开；
- 初始闭环分不能虚高，回填后动态闭环分应上升；
- 首日选题不得继续使用“小老板”作为标题主称呼。

### v1.1 · 推荐起步平台

日期：2026-05-17

本版新增/修正：

1. 增加推荐起步平台能力。
2. 区分「本账号优先平台」「辅助平台」「目标客户可能适用平台」「暂不建议」。
3. 修复之前把客户平台误当作本账号发布平台的问题。
4. 修复首条内容计划日期按 UTC 漂移的问题，以北京时间为计划基准。

### v1.0 · 最小营销增长闭环

日期：2026-05-17 前

能力范围：

1. 营销体检表单；
2. 自动诊断；
3. 7天内容计划；
4. 发布反馈回填；
5. 周复盘和下一轮建议。

已知问题：

- 单一评分容易虚高；
- 平台建议颗粒度不够；
- 账号基础设置缺失；
- 反馈闭环成熟度未充分联动真实回填。
