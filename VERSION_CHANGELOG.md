# 企业营销增长工具版本更新台账

## 版本号规则

- 当前从 `1.0` 开始，采用 `1.x` 功能版本 + `1.x.y` 修订版本迭代。
- `1.0`：最小可用闭环版本，能完成营销体检、诊断、7天内容计划、反馈回填、周复盘。
- `1.1`：在 1.0 基础上增加「推荐起步平台」，并区分本账号发布平台与目标客户可能适用平台。
- `1.2`：在 1.1 基础上增加「账号冷启动配置模块」，把发布前的账号定位、简介、头像方向、平台表达规则纳入门禁。
- `1.3`：在 1.2 基础上增加「内容决策局测试样例入口」和「首次发布链接回填门禁」，解决测试页初始空状态与发布闭环无链接凭证的问题。
- `1.4`：在 1.3 基础上增加「多时间点反馈回填」，支持 T+24 / T+72 / T+7 分阶段记录，并防止复盘统计重复叠加。
- 后续规则：
  - 同一功能版本内的小功能补齐、文案修正、字段门禁、QA 修复：采用 `1.x.y` 递增，例如 `1.3.1`；
  - 新增一个清晰功能模块或业务流程节点：采用 `1.x` 递增，例如 `1.4`；
  - 数据结构或业务流程明显变化：仍在 MVP 阶段先递增 `1.x`；
  - 形成可交付客户试用版、字段与数据回流稳定后，再进入 `2.0`。

## 更新记录

### v1.4.1 · 极简录入 + 对标账号独立模块

日期：2026-05-20

本次体验优化：

1. 首页极简化：首屏只保留业务核心字段和一个独立对标账号模块。
2. 对标账号从“补充资料”中独立为核心模块，强调用真实市场信号校准选题，而不是让 AI 凭空编爆款。
3. 高级补充默认隐藏，企业名称、联系人、预算、决策周期、内容资产等不再压迫首次填写。
4. 生成结果前弱化反馈/复盘模块，仅保留轻提示；生成计划后再展示发布链接回填、T+24 / T+72 / T+7 和周复盘。
5. 保留 v1.4.0 的对标账号主题参考、多阶段反馈、最新阶段聚合统计和周复盘逻辑。

验收：
- 首屏必须更像“少填信息即可生成建议”，而不是大表单；
- 对标账号必须独立可见；
- 未生成计划前不展开完整反馈表和周复盘；
- 不得新增自动抓取、自动登录或自动发布。

### v1.4.0 · 对标账号主题参考 + 多阶段反馈回填

日期：2026-05-20

本次功能更新：

1. 新增「对标账号主题参考」模块。
2. 第一轮主题建议加入市场校准层，不再只依赖客户自述和系统判断。
3. 支持客户手动输入对标平台、对标账号链接、备注和代表爆款内容/标题/链接/数据摘要。
4. 7天内容计划会提炼对标账号中已验证的痛点和标题结构，但只做主题转译，不照抄原文。
5. 暂不接自动抓取 Skill，不接小红书/抖音自动爬取，不自动登录，不自动发布，避免自动化风险。

验收：
- 页面必须出现「对标账号主题参考」；
- 页面必须提示「对标账号不是用来抄袭，而是用来判断市场已经验证过哪些痛点和表达方式。」；
- assessment 必须包含 `benchmark` 字段；
- 保留 T+24 / T+72 / T+7 多阶段反馈回填。

### v1.4 安全修正 · 移除评论引导

日期：2026-05-19

本次修正：

1. 移除内容决策局与客户内容计划中的评论区/留言式引导。
2. CTA/承接统一改为私信、主页咨询、保存、数据回填等低风险表达。
3. 页面表头从 `CTA` 调整为 `合规承接`，避免继续诱导客户使用评论区引导。
4. 保留「评论数」作为数据指标，但不再生成“评论区告诉我 / 留言某关键词”等动作建议。

验收：
- `node --check static/app.js` 通过；
- `node --check netlify/functions/api.mjs` 通过；
- `node tests/api_smoke.mjs` 通过。

### v1.4 · 多时间点反馈回填

日期：2026-05-19

本次功能更新：

1. 将「首发链接回填 / 营销反馈回填」升级为多时间点入口：
   - 新增反馈时间点字段：`T+24`、`T+72`、`T+7`；
   - 同一发布计划可保留多个时间点反馈记录，观察内容生命周期，而不是只看一次数据。
2. 修正统计口径：
   - 看板、动态闭环分、周复盘只使用每条内容的最新时间点数据；
   - 避免 T+24 + T+72 + T+7 被重复累加，造成曝光、互动、咨询虚高。
3. 修正重复提交口径：
   - 同一计划 + 同一反馈时间点重复提交时覆盖旧记录；
   - 同一计划不同时间点同时保留。
4. 验收：
   - `node --check static/app.js` 通过；
   - `node --check netlify/functions/api.mjs` 通过；
   - `node tests/api_smoke.mjs` 覆盖 T+24/T+72 多行保留与最新口径统计。

### v1.3.2 · 快速体检 + 首屏关键结论

日期：2026-05-18

本次小修订/修正：

1. 将「客户营销体检」从大表单压缩为「5项快速体检」：
   - 首屏只保留行业、主要目标、目标客户、当前平台、最大问题、企业名称；
   - 产品/服务入口、客户痛点、内容资产、预算、决策周期等改为可选展开字段；
   - 降低第一次填写阻力，先生成判断，再补资料。
2. 将「关键结论」提前到首屏右侧：
   - 用户不需要滚动很久才能看到诊断；
   - 诊断默认只展示优先问题、核心诊断、下一步；
   - 账号配置、平台建议、评分说明等改为可展开详情。
3. 将「客户数据/诊断依据」默认收起：
   - 保留可审计性；
   - 不再占用主页面高度。
4. 将「7天计划」默认压缩为前三个优先动作：
   - 完整计划表改为展开查看；
   - 页面长度明显降低。
5. 验收：
   - `node --check static/app.js` 通过；
   - `node --check netlify/functions/api.mjs` 通过；
   - `node tests/api_smoke.mjs` 通过；
   - 本地浏览器控制台无 JS error。

### v1.3.1 · 客户数据展示修复 + 首发链接门禁体验调整

日期：2026-05-17

本次小修订/修正：

1. 修复「客户展示」问题：
   - 将「客户营销体检」输入区与「最新诊断报告」展示区拆开；
   - 新增「当前客户数据」展示区，显示本次真正进入诊断引擎的客户输入；
   - 避免出现“诊断有了，但客户数据为空/看不到”的误判。
2. 修复后端 assessment 返回：
   - `POST /api/assessments` 返回中补充 `assessment` 对象；
   - 前端提交体检或载入样例后，可直接用后端返回的 assessment 渲染客户数据。
3. 调整「首发链接回填门禁」交互层级：
   - 从独立大模块降级为计划表区域内的轻提示；
   - 保留“发布后必须回填链接”的闭环提示，但不抢占诊断报告版面。
4. 验证链接回填链路：
   - 缺少发布链接时不能被计为已闭环；
   - 计划/看板统计按 `已发布 + publish_link` 计算；
   - 链接回填后，反馈、看板、闭环分和下一轮建议可联动更新。
5. v1.3 已完成线上部署测试，线上 `/api/health` 返回：`v1.3 · 内容决策局测试样例入口 + 首发链接回填门禁`。
6. 从本条开始，后续同一功能版本内的小修改、补丁、文案、字段和 QA 修复，按 `1.3.1`、`1.3.2` 这类三段式修订版本记录。
7. 当前产品 UI 和 API 对外仍显示功能版本 `1.3`；台账修订号用于内部追踪小改动，不强制每次都改变对外页面主版本号。

验收口径：

- 页面必须能看到「当前客户数据」区；
- `POST /api/assessments` 必须返回 `assessment`；
- 首发链接提示应位于 7 天内容计划区域，而不是独立抢占报告版面；
- 无发布链接不得计入已闭环；
- 后续小改动必须在本台账新增 `1.x.y` 记录；
- 如新增独立功能模块，再升级到下一个 `1.x` 功能版本。

### v1.3 · 内容决策局测试样例入口 + 首发链接回填门禁

日期：2026-05-17

本版新增/修正：

1. 页面顶部新增「载入内容决策局样例」按钮，便于测试者一键进入标准测试案例。
2. 首次进入且浏览器无本地演示数据时，默认载入「内容决策局」样例，避免空看板被误判为系统无数据。
3. 「清空演示数据」后保留空状态，用户可通过样例按钮重新载入测试数据。
4. 「营销反馈回填」中的发布链接改为必填，首条/每条发布必须返回真实发布链接后才算闭环。
5. 发布计划表展示链接回填状态，缺少链接时提示“发布后需回填链接”。
6. 版本标签同步到前端页面、诊断结果、Netlify Function `/api/health` 与 API smoke test。

验收口径：

- 首页必须显示 v1.3；
- 首次进入应看到内容决策局样例数据和 7 天计划；
- 清空演示数据后可再次通过按钮载入样例；
- 保存反馈时必须填写发布链接；缺链接不得被当作完整发布闭环；
- 控制台无 JS 报错，`/api/health` 返回 1.3。

### v1.2 · 账号冷启动配置 + 起步主平台决策

日期：2026-05-17

本版新增/修正：

1. 新增「账号冷启动配置」模块：
   - 账号名建议；
   - 账号定位；
   - 小红书简介；
   - 主页关键词；
   - 头像方向；
   - 起步主平台；
   - 平台表达规则；
   - 对外称呼门禁。
2. 企业营销测试号默认独立定位：企业内容增长 / 企业获客 / AI营销复盘，不再依附 P01/PTE。
3. 默认账号建议调整为「内容决策局」。
4. 对外表达修正：避免高频使用“小老板”，优先使用老板、企业主、商家、门店老板、企业负责人。
5. 平台策略修正：小红书可适量 emoji 和精致感，但该规则不得外推到视频号、公众号、朋友圈、抖音等平台。
6. 闭环成熟度修正：新增动态闭环分，随发布回填、互动、咨询和复盘行为上升。
7. 时间显示修正：前端反馈记录、复盘时间使用北京时间格式，避免 UTC 漂移。
8. API 健康检查返回当前版本号与版本标签。

验收口径：

- 诊断结果必须显示 v1.2；
- 必须出现账号冷启动配置；
- 本账号起步主平台与目标客户适用平台必须分开；
- 初始闭环分不能虚高，回填后动态闭环分应上升；
- 首日选题不得继续使用“小老板”作为标题主称呼。

### v1.1 · 推荐起步平台

日期：2026-05-17

本版新增/修正：

1. 增加推荐起步平台能力。
2. 区分「本账号优先平台」「辅助平台」「目标客户可能适用平台」「暂不建议」。
3. 修复之前把客户平台误当作本账号发布平台的问题。
4. 修复首条内容计划日期按 UTC 漂移的问题，以北京时间为计划基准。

### v1.0 · 最小营销增长闭环

日期：2026-05-17 前

能力范围：

1. 营销体检表单；
2. 自动诊断；
3. 7天内容计划；
4. 发布反馈回填；
5. 周复盘和下一轮建议。

已知问题：

- 单一评分容易虚高；
- 平台建议颗粒度不够；
- 账号基础设置缺失；
- 反馈闭环成熟度未充分联动真实回填。
