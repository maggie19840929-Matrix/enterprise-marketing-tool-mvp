# Codex 开发任务书：客户版「内容策略」交互优化 V1

> 交给 Codex 的权威提示词。**只优化客户版(client_viewer)的内容策略流的"呈现",不改策略内核,不给客户版加生成模块,不改内部版密度。**
> 一句话目标:客户版做到"**内核和内部版一样准,呈现比内部版轻得多**"。配套阅读:`CODEX_BRIEF_配置化单系统重构_V1.md`(profile 机制)、`区块清点与归屏映射_V1.md`。

---

## 0. 真实代码与现状

- 客户版 = 根路径 `/`,`client_viewer` profile;内部版 = `/internal*`,`internal_admin`。判定见 `static/app.js`:`isInternalProfile()` / `currentProfile()` / `VIEW_PROFILES`。
- 客户脱敏层 `sanitizeCustomerPayload`(api.mjs + app.js)已存在,**继续生效,不得削弱**。
- 客户旅程(录入→方案→计划→复盘)目前呈现偏密(信息墙),客户反馈"信息密集恐惧"。录入已在前期瘦身为 3 必填 + 折叠(保持)。
- **本任务只动客户版这条策略流的呈现层。**

---

## 1. 范围红线

- ✅ 做:客户版(client_viewer)的 **内容方向/建议、7 天计划、记录效果/下一个七天复盘** 三块的轻量化呈现。
- ❌ 不做:
  - 不给客户版加任何**生成/素材/QA/交付**模块(那是内部运营工具);
  - 不改**内部版(internal_admin)** 的密度/布局——内部保持现状全信息;
  - 不改策略**生成逻辑/算法/质量**(诊断、计划、复盘的"内核"不动,只改怎么展示);
  - 不引入新框架/新依赖。

---

## 2. 两个客户版屏幕的目标形态(按已确认 mockup 实现)

### 屏 A：客户版 7 天内容计划
- **每天一张极简卡**,默认只显示:`第N天` + `一句话标题(发什么)` + `平台标签(pill)`。
- 理由/指标/风险/评分等 **默认折叠**,卡上给一个「为什么这样发 ›」入口,点开才显示理由(其余内部评分/调试**完全不展示**)。
- 整屏**一个主行动**按钮(如「这条发完了,去记录效果」),其余降级。
- 7 天卡纵向排列,留白充足;一屏能扫懂"这周发什么"。

### 屏 B：客户版记录效果 + 下一个七天(三步)
把内部版那套 A–F 多块压成**三步引导**:
1. **我发了哪一条**(从本周计划里选一条,下拉/选择即可);
2. **填几个数**:`曝光` / `互动` / `咨询` 三个数字 + 一句话观察(可选)——**就这几个,不要表格**;
3. **下一个七天建议**:**一句结论 + 3 条勾选式动作**;判断依据(发布数据/诊断)收进「查看判断依据」折叠。

> 视觉/交互参考:卡片极简、平台用浅色 pill、勾选动作用对勾、依据用 `<details>` 折叠。原生组件即可,无需新库。

---

## 3. 交互原则(对抗"信息密集恐惧")

1. **一屏一任务**:每屏只服务一件事,不堆叠多块。
2. **默认只摆"该看/该做的一件事"**,理由/依据/指标一律**渐进披露**(默认折叠)。
3. **每屏一个主行动**:唯一主 CTA 突出,其余降级为次要/链接。
4. **客户视角正式、干净**:无 preview/debug 感,无任何内部代号(P01-03/Matrix/SunPace/Sunny/PTE/OpenClaw/Hermes…)。
5. 三态齐全(空/加载/错误);禁止 `undefined/null/NaN/[object Object]`。

---

## 4. 硬约束

1. **profile 隔离**:所有改动只对 `client_viewer` 生效;`internal_admin` 视图**逐像素不变**(内部仍看全信息)。用现有 profile 机制实现,不要新建一套客户 DOM/逻辑去复制策略(优先复用、按 profile 渲染轻量版)。
2. **内核不动**:不改 `/api/assessments`、诊断/计划/复盘的生成逻辑;只改前端如何展示这些数据。
3. **脱敏不减**:客户视图仍走 `sanitizeCustomerPayload`,零代号泄露。
4. **不上生成模块**:客户版不出现素材/生成/QA/交付任何入口。
5. 可部署到现 Netlify;`npm test` 通过;`/api/health` 正常。

---

## 5. 验收材料

1. 改了哪些文件、哪些 render 函数。
2. 客户版 `/` 截图/说明:7 天计划是极简卡 + 折叠理由;记录效果是三步;下一个七天是一句结论+动作+折叠依据。
3. **内部版 `/internal/` 未变**的证明(对比改动前后,内部密度/布局一致)。
4. 客户视图代号泄露扫描:P01-03/Matrix/SunPace/Sunny/PTE/OpenClaw/Hermes + 内部字段(requested_model/provider/debug…)= 0。
5. 浏览器 smoke(`/` 客户旅程可走完:录入→方案→选一条→记录→看下一个七天);控制台无报错。
6. `npm test`、`/api/health` 结果。

---

## 6. 部署
保持可部署到现 Netlify site(`f0efe912-abbc-4362-afdc-c9e513cb986c`,禁用旧短 ID `9af65138`);build 配置不变。

---

## 7. 备注
- 这是产品的**销售门面**(新客户最先看到的就是这条策略流),呈现要正式、轻、专业。
- mockup 若有评审微调(字段增减/步骤合并),按最终意见为准;本任务书描述的是目标形态。
