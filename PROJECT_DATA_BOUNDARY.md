# 项目数据边界与工作台定位

## 1. 工作台定位

当前工作台不是“所有客户数据的混合展示页”，而是企业营销增长驾驶舱的项目级作战台。

它服务一个项目在一个增长周期内的闭环：

客户/行业输入 → 策略诊断 → 内容计划 → 发布回填 → 周复盘 → 下一轮动作

## 2. 作战台主数据

作战台原生主数据只应该展示当前 `activeProjectId` 对应项目的增长闭环数据：

- `project`：当前项目名称、阶段、周期；
- `assessment / diagnosis`：当前项目的客户输入与系统诊断；
- `plans`：当前项目生成的内容计划；
- `feedback`：当前项目对应计划的发布与反馈；
- `review`：当前项目的复盘与下一轮判断。

这些数据回答的问题是：当前项目下一步怎么增长、哪条内容有效、下一轮怎么调整。

## 3. 项目级客户反馈资产

“安标检测客户脚本确认表”不是作战台原生主数据，也不是通用 KPI。

它属于项目级客户反馈资产，来源是客户对脚本/选题的真实反馈，字段包括：选题编号、标题、客户修改建议、客户评分。

它回答的问题是：客户认可哪些选题、哪些脚本要重写、哪些专业表达需要校正。

因此它必须由独立模块承载，模块名称为：`客户脚本反馈样本`。

展示规则：

- 只在内测版展示；
- 只在明确命中安标检测/安标/医疗器械注册/医疗器械送检/注册送检项目时展示；
- 不进入通用作战台 KPI；
- 不因“检测”“轻工业”等宽泛词触发；
- 后续如转入产品化，应迁移为项目详情页里的“客户反馈资产/素材库”，而不是插入所有项目的作战台主流程。

## 4. 项目数据隔离规则

每个项目都必须有独立的数据边界，不允许因为行业相近、字段含有相似关键词而串台。

- 安标检测项目数据，只能展示在安标检测项目下；
- 轻工业作战台不能展示安标检测客户脚本确认表；
- “检测”“轻工业”等宽泛词不能作为安标检测项目识别条件；
- 项目专属客户反馈资产必须用明确项目名、客户名、项目 ID 或白名单字段触发；
- 内测展示可以接入客户真实数据，但必须先有项目边界说明和验收规则。

## 5. 新增真实客户数据模块前的设计要求

之后每个接入真实客户数据的项目，必须先写项目需求/边界文档，至少包含：

1. 项目名称与客户/行业范围；
2. 该模块要回答的核心业务问题；
3. 数据类型归属：增长闭环数据 / 客户反馈资产 / 素材库 / 外部参考数据；
4. 允许展示的数据源；
5. 不允许展示的数据源；
6. 与其他项目的隔离规则；
7. 页面承载位置：作战台主流程 / 项目详情 / 独立资产模块；
8. 验收路径：切换到相关项目时应显示什么，切换到不相关项目时必须隐藏什么。

## 6. 本次事故记录

问题：安标检测客户脚本确认表被宽泛关键词规则触发，曾出现在轻工业作战台下；同时模块命名为“客户真实数据”，数据类型表达不清，容易被误认为作战台主数据。

根因：前端 `isAnbiaoCustomerProject()` 使用了过宽的正则条件，包含 `轻工业` 与泛化词 `检测`；模块接入前没有先定义数据归属、页面承载位置和验收规则。

修复原则：删除宽泛触发词，只保留明确安标检测/安标/医疗器械送检相关条件；模块文案改为“客户脚本反馈样本”；以后项目专属数据接入前必须先写边界文档和验收规则。
