# Codex 任务书:内容质量精修 —— CTA 去噪 + 小红书语感增强

> **背景:** 豆包整体达标(贴业务、受众框定、抽象 B2B 都稳),但线上实测有两处短板。
> **只改 prompt + 生成后处理,不换模型、不改字段结构(topic/angle/content_type/cta 四字段 + 规则补齐的下游不变)。**
> 入口:`netlify/functions/api.mjs` 的 `contentPlanPrompt` + 平台语感规则(现有小红书规则约在 `~723` 行)+ CTA 生成/拼接处。改完 bump 版本、部署。

---

## 问题 1:CTA 措辞发飘(最优先)

**线上真实样本(财税代理):**
- `点击咨询咨询代账方案`(**"咨询咨询"叠词**)
- `咨询问你的财税情况`(语病)
- `咨询获取…` / `咨询…` 五条几乎都以"咨询"开头(动作单一)

### 1a. 先定位来源(你有实时代码)
"点击咨询" + 模型输出"咨询…" 很像**代码前缀拼接**撞上模型自带前缀 → 排查 CTA 是否有硬编码前缀(如"点击咨询"/"咨询")与模型输出拼接;**若是拼接 bug,先在源头去掉重复前缀**。

### 1b. 收紧 CTA 的 prompt 要求
在 `contentPlanPrompt` 里对 cta 明确:
- **一句话、口语、≤14 字**,不带语病、不半句;
- **动词不重复**(禁止"咨询咨询"这类叠词);
- **五条 CTA 动作要多样**,不要都是"咨询"——覆盖 `私信/评论区扣/主页预约/到店体验/保存收藏/截图问款` 等**不同**入口,按平台选合适动作;
- CTA 不与 topic 文案重复。

### 1c. 加"去噪清洗"后处理(兜底,即使 prompt 没约束住也能救)
模型返回后、入库前,对 cta(及 topic)做轻量清洗:
- 合并相邻重复词(正则去 `(咨询|预约|到店|了解)\1` 类叠词);
- 去掉明显语病/半句、首尾多余标点;
- 若五条 cta 动作雷同,做去重轮换。
- **纯清洗,不改语义**;清洗失败就退回原文,别抛错。

---

## 问题 2:小红书语感偏"教科书"(次优先)

**样本(美甲):** `一文讲清真相`、`干货整理` —— competent 但不够种草。

现有小红书规则(`~723` 行)已要求"标题像真实问题、别像工具说明书",**在此基础上加强**(仅当平台含小红书时生效):
- 标题更口语、带**情绪钩子**(如"谁懂啊/原来/救命/后悔没早知道"),可用**数字/清单感**;
- 保持**真实、不夸大、不虚假承诺、不违反平台规则**(不要标题党到失真、不堆 emoji);
- **抖音**保持现有钩子风格、**视频号**保持口播/科普,**不要把小红书那套生搬到其它平台**(现有代码已有这条约束,别破坏)。

---

## 硬约束(零回归)
- **不换模型、不改字段结构**;规则兜底(planTemplates)路径与下游解构不变。
- 不回归:平台逻辑(`planPlatforms`,选啥平台出啥)、多轮、脱敏(客户零代号/无模型元数据)、异步 `/api/plan-jobs`、P0 计量/限流、持久化。
- 若测试断言了具体 CTA/标题文案,同步更新断言,但不得删功能性断言;`npm test` 通过。
- 只动内容生成的 prompt/后处理,别碰身份/计费/安全。

## 验收(附改前改后对比)
1. 拿这几个行业重跑:**财税代理 / 美甲 / 推拿 / 篮球**——
   - CTA **无叠词、无语病**,五条动作多样(附样本);
   - 小红书标题更种草但不失真(附样本);
2. 业务贴合度、受众框定(写给客户非老板)、平台逻辑**不退**;
3. 抽象 B2B(财税)仍不滑向泛化模板;
4. `npm test` 通过;bump 版本;客户响应仍无模型元数据。

---

## 一句话给 Codex
**别换模型——先在源头修 CTA(排查前缀拼接 bug + 收紧 prompt + 加去叠词清洗),再强化小红书语感(在现有平台规则上加情绪钩子、保持真实、不外溢到别的平台)。功能与安全零回归,交付附改前改后样本。**
