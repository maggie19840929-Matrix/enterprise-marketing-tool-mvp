# Codex 小清理单:可读性收尾(3 处一两行的活)

> P0 三项已达标,勿动。以下 3 处漏网,都是客户浅色页的小修补。只改样式/文案,不碰逻辑。改完 bump 版本号并部署。

## 1. 漏改的 1 处绿字
`static/war-room-v1.6.1.css` 里还有一处 `color:#bdf7df`(压在 `background:rgba(16,185,129,.08)` 绿底上,对比度仅约 1:1)。
→ 换成深绿墨 **`#065f46`**(和已修好的 `.plan-day-pill` 一致)。全文件搜 `#bdf7df`,客户浅色作用域内的**一律**清成 `#065f46`;内部深色页(war-room)不动。

## 2. 紫色选中态同款低对比
选中胶囊类 `color:#d8dcff` 压在 `background:rgba(94,106,210,.2)` 紫底上 = **1.04:1**,看不清(和绿字同一个病,只是紫色)。
→ 文字换深紫墨 **`#312e81`**(或墨色 `#1f2937`),背景 tint 可保留。目标 ≥4.5:1。
→ 顺带搜一下客户区其它 `#d8dcff / #d8dc*` 淡紫文字,同样处理;内部深色页不动。

## 3. 多余的星号
`static/index.html` 第 ~729 行 `<legend>你当前最大的内容问题是什么？*</legend>`,但其输入已是 `type="hidden"`(不再拦客户提交)。
→ 去掉这个 `*`,避免显得仍需必填。确认该 legend 若整块已隐藏/移入可选区,文案与状态一致即可。

---

## ⚠️ 返工补充(上一轮紫色只改了 1 处,还漏 2 处)

`#d8dcff` 你只把某个选中胶囊改成了 `#312e81`(对),但客户区**还剩 2 处 `#d8dcff` 没改**,已确认低对比:
- **`.customer-more-fields > summary`**:`color:#d8dcff` 压白底 = **1.35:1**(这是"更多(可选)"展开按钮的字,客户要点它,几乎看不见);
- 另一处 `.customer-plan-item` 相关的 `.plan-day` 态(`#d8dcff`)。

**要求:全文件 `grep #d8dcff`,客户浅色作用域内的每一处都换成深墨 `#312e81`(或 `#1f2937`),一个不留。** 内部深色页不动。
**交付必须附上 `grep -c "#d8dcff" 客户CSS` 的结果(客户区应为 0),别再只改一处就说改完了。**

## 硬约束
- 只改客户浅色作用域的颜色/文案;**内部深色页(war-room / `#8ff2cb` 等)不动**。
- 不回归:安全、平台、多轮、异步 plan-jobs、持久化、P0/P1 既有成果。
- `name`/`data-*`/`id` 钩子不动;`npm test` 通过;bump 版本号。

## 验收(给数值)
1. 客户区已无 `#bdf7df`;第 1 处绿字对比度 ≥4.5(给色值+比值)。
2. 紫色选中态文字对比度 ≥4.5(给色值+比值)。
3. "最大的内容问题" legend 无多余 `*`。
4. 内部深色页颜色无变化;`npm test` 通过。
