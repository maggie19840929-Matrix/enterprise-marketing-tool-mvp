# 获客罗盘支付与收单方案 V1

> 文档状态：评审已确认，待资质准备后分阶段实施
>
> 日期：2026-08-10
>
> 适用主体：南京尚下联信息科技有限公司
>
> 关联文档：`获客罗盘_商业化产品说明书_V1.md`

---

## 1. 目标与边界

本方案用于让获客罗盘完成合规、可审计、可对账的 SaaS 订阅收款闭环。

首发采用“商户销售自身软件服务 + 持牌支付机构直接收款”的模式：南京尚下联信息科技有限公司是服务销售方，微信支付、支付宝或经核验的持牌服务商负责支付处理，款项直接结算至公司绑定的对公账户。

系统不得提供或变相提供以下能力：

- 代收代付、资金归集、清分结算、用户余额、钱包、提现或转账；
- 个人收款码、免签收款、监听到账或根据截图自动开通；
- 收集、存储或处理银行卡号、支付密码等支付敏感凭据。

正式上线前，退款规则、开票规则、用户协议、隐私政策、主体经营范围和支付产品准入须由公司法务、财务或外部专业人士最终确认。

## 2. 已确认的商业决策

| 项目 | 已确认规则 |
|---|---|
| 收款主体 | 南京尚下联信息科技有限公司 |
| 首发渠道 | 微信支付、支付宝；Pro 或合同客户可走对公转账人工审核 |
| 续费方式 | 首发不启用自动续费，用户主动购买月度或年度服务期 |
| 计价结构 | 套餐提供策略周期等基础权益；高成本详细文本、图片、视频以清晰的独立额度包或明确套餐权益提供 |
| 产品分层 | Free 可试用；Plus 公开销售；Pro 在生产能力成本和质量稳定前保持邀约开通 |

客户购买的是获客罗盘的服务期和明确权益，不购买 Token、模型名称、供应商调用次数或内部算力成本。

## 3. 首发支付产品

### 3.1 支付方式

| 使用场景 | 首发支付方式 | 说明 |
|---|---|---|
| 桌面浏览器 | 微信扫码支付、支付宝网页支付 | 按浏览器环境展示适合的渠道 |
| 手机浏览器 | 微信 H5 支付、支付宝 WAP 支付 | 以支付机构最终准入能力为准 |
| 合同与企业客户 | 对公转账 + 人工核验 | 仅作为企业服务通道，不使用个人账户 |
| 银行卡 | 不直接接入 | 客户可通过微信或支付宝已绑定银行卡付款 |

若某支付产品尚未通过渠道审核，前端只能展示已可用方式或对公转账联系入口，不能伪造“支付成功”。

### 3.2 订阅与到期

- 月度和年度服务期均为一次性主动购买，不默认扣款续费。
- 到期前通过站内和邮件提醒用户主动续费。
- 到期未续费：账户回到 Free 可用范围，历史项目、发布记录和已交付内容保留，不删除客户数据。
- Plus / Pro 的续费、升级、降级和补偿均留下订单或内部审计记录。

未来如开启自动续费，必须另立项目，完成显著授权、每次扣费通知、价格与周期展示、便捷取消、客服与争议处理的全量验收后方可开放。

## 4. 订单、支付与权益状态机

订单、支付单和订阅权益分离管理，避免支付重试、回调重复或网络中断导致重复开通。

```text
商业订单：
draft -> pending_payment -> payment_creating -> awaiting_payment
  -> paid -> activated
  -> expired | canceled | failed
  -> refund_pending -> refunded

支付单：
created -> paying -> succeeded | failed | closed | refunded
```

关键规则：

1. 前端仅提交套餐、周期和幂等标识；价格、币种、权益快照均由服务端读取套餐配置并锁定。
2. 同一个幂等标识只能对应一笔待支付订单；网络重试复用原订单和支付单。
3. 一笔商业订单可有多个已关闭的支付尝试，但最多一个成功支付单。
4. 外部支付单建议 30 分钟失效；商业订单可保留 72 小时，过期后可创建新的支付尝试。
5. 只有经过支付机构回调验签或服务端主动查单确认成功的支付单，才可以触发一次权益开通。
6. 浏览器的“我已支付”只执行查单，不得直接将订单改为已支付。
7. 对公转账走 `offline_bank_transfer` 支付单，由受限内部人员核验付款信息后开通；同样必须幂等和留痕。

## 5. 服务端架构

现有 `manual_review` 订单、订阅和权益状态机保留。自动收单阶段新增支付 Adapter，不迁移、不修改客户 `project_store` 数据。

```text
套餐页
  -> POST /api/orders
  -> POST /api/payments/create
  -> 微信 / 支付宝支付页
  -> POST /api/payments/{provider}/notify
  -> 验签、解密、查单与金额核验
  -> applyPaidOrderSubscription(orderId)
  -> 订阅与权益生效
```

建议新增独立存储命名空间：

- `payment-intents/v1`：渠道、支付单号、订单号、金额、状态、过期时间；
- `payment-events/v1`：回调事件、查单结果、退款事件的脱敏审计记录；
- `reconciliation/v1`：每日对账结果、异常项和处理状态。

支付机构是实际支付状态的权威来源；应用侧 Blob 仅保存订单、权益开通和审计所需的最小记录。回调原文应脱敏，密钥、证书、完整支付凭据不写入日志或 Git。

### 5.1 回调校验

每次支付成功通知至少校验：

- 支付机构签名或平台证书；
- 商户号、应用号和订单号；
- 订单金额、币种、支付状态；
- 回调事件唯一标识；
- 订单是否已激活。

任一项不一致时不授予权益，记录异常并主动向支付机构查单。支付回调必须在服务端处理；浏览器和前端轮询不构成支付凭证。

### 5.2 退款与对账

- 退款由内部受限人员发起，绑定原订单、原支付单、退款原因和审批记录；优先原路退回。
- 退款成功后按已发布退款规则变更订阅状态，禁止直接删除订单或篡改付款记录。
- 每日拉取或导入支付机构账单，与 `payment-intents/v1` 对账；金额、状态或流水不一致时进入异常队列。
- 对账、退款、人工确认和权益变更均记录操作人、时间、前后状态与关联订单。

## 6. 客户端体验

客户侧页面只展示产品与支付信息：

1. 套餐名称、服务期、价格、权益摘要。
2. 确认订单页：应付金额、服务开始与结束规则、退款/售后入口、用户协议和隐私政策链接。
3. 支付页：渠道选择、二维码或安全跳转、支付处理中、支付成功、支付失败和重新支付。
4. 账号页：当前套餐、到期日、剩余权益、订单记录、主动续费入口。

客户侧不展示商户密钥、回调内容、模型成本、内部审核、人工操作人或支付调试信息。

## 7. 安全与合规清单

### 7.1 环境变量与证书

真实接入时使用 Netlify 环境变量或受管密钥，不得写入仓库：

- P1 内部开关：`PAYMENT_P1_INTERNAL_ENABLED=false`、`PAYMENT_P1_SANDBOX_ENABLED=false`；仅在隔离测试环境短时启用沙箱，并另设高强度 `PAYMENT_P1_SANDBOX_TOKEN`；
- 微信支付：商户号、应用号、API v3 密钥、商户私钥/证书、平台证书或公钥、回调地址；
- 支付宝：应用 ID、应用私钥、支付宝公钥或证书、网关与回调地址；
- 对账与告警：只读账单凭据、内部异常通知配置。

生产环境在真实渠道 Adapter 尚未通过验收前，两个 P1 开关必须保持 `false`。沙箱令牌只用于测试回调验签，不是微信或支付宝的真实密钥。

### 7.2 业务与法务准备

- 公司营业执照、对公账户、支付产品申请资料；
- 对外套餐价格、服务说明、发票规则、退款规则、投诉与客服渠道；
- 用户协议和隐私政策中的订阅、订单、支付服务商、退款和数据保留说明；
- 生产回调 HTTPS 域名及支付机构后台白名单配置；
- 内部最小权限：订单查看、退款、对账、人工对公确认分权处理。

## 8. 分阶段实施与发布闸门

### P0：资质与方案确认（当前）

- 固化本方案与外部资料清单；
- 不创建真实支付单，不接收真实款项，不部署支付接口；
- 继续保留当前 `manual_review` 人工开通能力。

### P1：支付基础设施（内部环境）

- 新增支付单、渠道 Adapter、回调验签、主动查单、退款与对账骨架；
- 使用渠道沙箱或 mock 验证，不在客户版开放；
- 完成重放回调、重复回调、金额篡改、订单过期、失败重试测试。

当前实现范围：已具备独立 `payment-intents/v1`、`payment-events/v1`、退款申请记录和内部对账汇总；微信支付与支付宝 Adapter 仅允许显式沙箱 mock。真实渠道请求、真实签名验证、真实退款和渠道账单拉取均留在 P2，生产环境默认关闭 P1 开关。

### P2：渠道沙箱与小范围真实收款

- 微信、支付宝按各自审核通过的产品接入；
- 内部白名单完成真实支付、回调、查单、退款和次日对账；
- 先开放 Plus，Pro 保持人工邀约。

### P3：公开销售

仅当下列项目全部通过才对外开放：

1. 支付机构产品、主体和回调域名审核完成；
2. 支付成功、关闭、退款、回调重放和对账全部验收；
3. 客户可见的价格、周期、退款、客服和隐私说明已发布；
4. 连续观察期内不存在“已付款未开通”或“未付款获权益”异常；
5. 财务可按订单与渠道账单完成对账。

自动续费不属于 P3 范围，另行评审。

## 9. 本期不做

- 不自建钱包、余额、充值、提现或转账；
- 不直连和保存银行卡数据；
- 不默认自动续费；
- 不把高成本图像、视频生成在成本未验证前承诺为无限套餐权益；
- 不因接入支付迁移、合并或清理已有匿名用户和 `project_store` 数据。

## 10. 实施时依据的官方资料

- 中国人民银行：《非银行支付机构监督管理条例》；
- 国家市场监督管理总局：涉及自动续费、自动扣款的显著提示与便捷取消规则；
- 《中华人民共和国个人信息保护法》：金融账户等敏感个人信息处理要求；
- 微信支付商户平台：支付结果通知、API v3 密钥和商户证书安全要求；
- 支付宝开放平台届时有效的网页支付、手机网站支付、异步通知与退款接口文档。

实施前应重新核对上述资料和渠道当前准入规则；本方案不替代法律、财务或支付机构的正式意见。
