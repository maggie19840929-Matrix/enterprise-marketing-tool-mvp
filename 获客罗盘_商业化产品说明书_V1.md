# 获客罗盘商业化产品说明书 V1

> 文档状态：商业规则已确认，进入分阶段实施
>
> 版本：V1.0
>
> 日期：2026-08-08
>
> 产品：获客罗盘 by FP Matrix
>
> 一句话定位：面向老板、企业主和商家的内容增长决策与复盘引擎。

---

## 1. 文档目的与效力

本文档是获客罗盘自助 SaaS 商业化的产品事实来源，用于统一产品、开发、运营、销售、客服和财务的理解。

本文档确定：

1. Free / Plus / Pro 三档自助订阅的产品边界。
2. 额度的名称、计量方法、扣减与返还规则。
3. 匿名试用、账号绑定、跨端数据找回的路径。
4. 人工开通与自动支付的分阶段方案。
5. 前端展示、后端权益判定、安全隔离与验收标准。

### 1.1 与旧文档的关系

- `CODEX_BRIEF_商业化落地_套餐额度_分阶段_V1.md` 作为实施任务书继续有效。
- `变现方案_三层产品与算力豆_V1.md` 只保留作为历史成本测算参考。自助客户版不再使用“算力豆”作为对外计价单位。
- `FP_Matrix_定位与品牌规范_V1.md` 继续是品牌、产品与服务边界的权威文档。
- 若旧文档与本文档的自助套餐规则冲突，以本文档为准。

---

## 2. 产品与服务边界

FP Matrix 同时拥有两条业务线，两者不混卖、不混算成本。

| 业务线 | 产品形态 | 收费方式 | 交付责任 |
|---|---|---|---|
| 获客罗盘 | 自助 SaaS | Free / Plus / Pro 订阅 | 系统提供决策、计划、数据记录与优化建议 |
| FP Matrix 增长服务 | 代运营、咨询、项目制 | 月费 / 项目费 / 战略合作 | 包含人工策略、执行推进、外包协同、QA 和客户交付 |

商业红线：

- 获客罗盘 Pro 不自动等于人工代运营。
- 人工审稿、专家诊断、摄影、剪辑、发布和运营推进作为独立服务或增值项报价。
- 自助套餐页不承诺流量、咨询量、成交量或具体增长结果。

---

## 3. 目标客户与使用场景

### 3.1 核心客户

- 本地门店：美业、口腔、餐饮、摄影、产康、本地生活服务。
- 教育培训：少儿篮球、武术搏击、素质教育、留学与考试培训。
- 服务型企业：轻咨询、企业服务、检测认证、本地 B2B 服务。
- 运营人员：同时管理多个商户或多个项目的内容运营者。

### 3.2 核心任务

1. 输入真实业务信息。
2. 获得内容方向和发布建议。
3. 按周期执行内容计划。
4. 记录发布链接和真实数据。
5. 根据数据生成下一轮优化建议。
6. 长期保留项目和历史轮次，形成可比较的增长记录。

---

## 4. 商业模式总览

采用“免费试用 + 分级订阅 + 套餐内完整内容 + 可选增值服务”模式。

客户只需理解三件事：当前套餐、每月能完成多少轮策略、每月能获得多少份完整内容。Token、模型供应商、失败重试和内部算力成本不作为客户计价单位。

### 4.1 分层原则

- **Free 负责证明价值**：新客户可完整体验三轮“生成→发布→记录→优化”，体验结束后保留每月一次基础使用。
- **Plus 负责持续使用**：适合单个商家或小团队长期按周运营。
- **Pro 负责规模与深度**：适合多项目、多门店、专业运营和更长数据周期；详细内容生产模块完成真实成本与稳定性验证前仅邀请开通。
- 不通过故意降低 Free 内容质量促进付费。三档都使用通过质量门禁的核心生成链路。
- 付费差异主要来自额度、项目数、历史深度、高级分析、导出、协作和服务响应。

---

## 5. 计量单位与扣减规则

### 5.1 对外计量单位

客户侧只展示两种容易理解的结果数量，不展示 Token、模型价格、供应商名称、算力豆或内部调用次数：

1. **策略周期**：一次首轮内容增长建议，或一次基于真实反馈生成的下一轮完整计划。
2. **完整内容**：一份可继续编辑使用的标题、正文或脚本、发布建议和平台规则检查；图片能力稳定后可按套餐包含一张配套封面。

整体重新生成如成功交付，计入对应的策略周期或完整内容。系统内部可以按 `usage_type` 区分策略、文本、图片和视频成本，但客户不需要理解模型换算关系。

视频生成在质量、失败率和真实成本完成验证前不计入普通套餐承诺。未来如开放，客户侧只看到明确的“AI 视频制作包”或套餐包含数量，不展示按 Token 计价。

### 5.2 不扣额度

- 查看已有结果、项目或历史数据。
- 保存发布链接、效果数据、备注和其他用户输入。
- 系统轮询、网络重试、结构修复、超时重试和同一 `request_id` 的重复请求。
- 模型调用失败且未向用户交付可用结果。
- 由平台故障导致的规则模板降级结果。
- 平台主动补偿、客服处理或内部 QA 重生成。

### 5.3 扣减时点

1. 提交生成时进行额度预留，防止并发超用。
2. 结果成功交付时正式扣减。
3. 失败、超时、取消或不可用降级时释放预留。
4. 同一幂等 `request_id` 只能存在一个 reservation，不得重复扣减。
5. 供应商内部重试可记入成本账本，但对客户仍只扣 1 次对应的产品额度。

### 5.4 结算周期

- 月付套餐：自首次开通日起每个月同日刷新。
- 年付套餐：订阅有效期为 12 个月，额度仍每月刷新，避免一次性消耗全年额度。
- 未用完的套餐额度默认不结转至下个周期。
- 额外加购包不纳入 V1 首发。真实用量成熟后，客户侧最多提供一个“内容加量包”；视频如开放则作为独立且明确的视频制作包，不设计多层算力换算。

---

## 6. Free / Plus / Pro 套餐定义

### 6.1 V1 已确认商业参数

> 下列价格和权益是 V1 的正式商业基线，运行值从环境变量或套餐配置读取，不在代码中硬编码。Pro 在详细内容生产模块完成稳定性和成本验收前保持邀请开通。

| 权益 | 免费版 Free | 专业版 Plus | 旗舰版 Pro（邀请开通） |
|---|---:|---:|---:|
| 月付价 | ¥0 | ¥299 / 月 | ¥899 / 月 |
| 年付价 | ¥0 | ¥2,990 / 年 | ¥8,990 / 年 |
| 策略周期 | 新客一次性 3 轮；之后 1 轮 / 月 | 4 轮 / 月 | 12 轮 / 月 |
| 完整内容 | 少量体验，以页面实际开放为准 | 12 份 / 月 | 40 份 / 月 |
| 每日防滥用上限 | 1 次 | 10 次 | 30 次 |
| 活跃项目数 | 1 | 3 | 10 |
| 项目与反馈历史 | 近 30 天 | 近 12 个月 | 订阅期间完整保留 |
| 内容方向与周期计划 | 支持 | 支持 | 支持 |
| 发布效果记录 | 基础 | 完整 | 完整 |
| 下一轮优化建议 | 额度内支持 | 支持 | 支持 |
| 历史周期对比 | 不提供 | 支持 | 支持 |
| 平台规则检查 | 基础 | 完整 | 完整 + 批量 |
| 数据导出 | 不提供 | 基础导出 | 完整报告 / 品牌导出 |
| 协作 | 不提供 | 1 个账号 | 最多 5 个成员 |
| 处理优先级 | 标准 | 优先 | 最高优先 |
| 客服 | 帮助中心 | 邮件支持 | 优先支持 |

套餐内已包含对应能力的基础模型费用，不按 Token 收费，也不会因为供应商内部重试自动产生额外费用。图片和视频只在相应能力完成验收并明确写入套餐后才构成付费承诺。

### 6.2 Free 产品意图

Free 必须能让客户理解核心价值，而不是只给一张无法验证的演示报告。

- 新客户获得一次性 3 轮策略体验，有效期建议为 30 天。
- 一次性体验用完或过期后进入 Free 维护状态，每月获得 1 轮基础策略周期，不重新赠送新客 3 轮。
- 页面明确告知剩余策略周期和刷新日期。
- 用完后保留已有项目的查看和效果记录能力，只限制新的 AI 生成。
- 不弹技术错误，而是展示“本月免费额度已用完”和清晰的升级入口。

### 6.3 Plus 产品意图

- 满足单个商家、品牌或小型运营团队按周持续使用。
- 支持正常周期运营、合理重新生成和多平台尝试。
- 通过历史轮次对比、平台规则检查和导出提供明确付费价值。
- 正式承诺以每月 4 轮策略周期为核心；完整内容模块上线后包含每月 12 份完整内容。
- 不包含 FP Matrix 人工审稿或代运营。

### 6.4 Pro 产品意图

- 满足专业运营、多门店、多项目和小团队协作。
- 提供更高额度、更多项目、完整历史、批量检查和品牌报告。
- 正式销售前必须完成详细文本、图片和视频能力的真实成本、失败率、重试率和交付稳定性验收；验收前只允许邀请测试，不公开承诺未成熟能力。
- 可购买专家复核、人工策略或内容制作增值服务，但不在订阅中默认承诺。
- 需要跨门店执行推进、飞书协同和人工负责交付的客户，升级为 FP Matrix 增长服务。

---

## 7. 账号、匿名试用与数据归属

### 7.1 账号原则

- 未登录客户仍可开始 Free 试用，不增加首屏登录墙。
- 购买套餐、跨设备找回、团队协作和修改订阅时必须登录。
- 系统以 `account_id` 作为付费权益主体，不再以单个浏览器 `client_id` 作为最终收费身份。
- `client_id` 继续用于项目数据隔离和兼容已有匿名用户。
- 一个账号可关联多个 `client_id`，但必须通过原客户凭证或内部授权完成绑定，不得仅凭客户名称合并。

### 7.2 身份方案

V1 采用可插拔身份层：

1. 首发 MVP：邮箱 6 位验证码或 Magic Link。
2. 国内放量前：优先接微信网页授权 / 扫码登录。
3. 备选：手机号 + 短信验证码。

身份标识在服务端以 hash 或必要的加密形式存储，不将明文邮箱、手机号、OpenID 或登录凭证写入前端可枚举数据。

### 7.3 匿名转账号

1. 匿名用户使用现有 `client_id + customer access token` 产生项目。
2. 用户绑定账号时，服务端验证其对该 `client_id` 的所有权。
3. 通过后写入 `account_client_links`，不复制、不删除、不重建原项目数据。
4. 绑定后账号可在新设备获取已授权的项目列表。
5. 原浏览器继续可用，不破坏已有 localStorage 和云端项目。

---

## 8. 套餐生命周期

### 8.1 订阅状态

`trialing / active / past_due / paused / canceled / expired`

- `trialing`：可使用 Free 权益。
- `active`：付费已确认，权益正常。
- `past_due`：自动续费失败，进入宽限期。
- `paused`：风险、争议、客服或合规原因暂停生成。
- `canceled`：已取消续费，当期截止日前权益仍有效。
- `expired`：已到期，回到 Free 权益。

### 8.2 升级、降级和取消

- 升级立即生效，首发人工开通阶段由运营确认差额后处理。
- 自动支付阶段按剩余周期折算差额，订单页显示实付金额和新周期。
- 降级下个计费周期生效，不立即删除超出新套餐上限的项目或数据。
- 降级后超出的项目变为只读，用户选择保留的活跃项目。
- 取消订阅不删除用户数据，数据保留按隐私政策和数据保留政策执行。

### 8.3 宽限期

- 自动续费失败后建议提供 3 天宽限期。
- 宽限期保留查看、记录数据和已开始任务，可限制新的高成本生成。
- 宽限期结束仍未支付时回到 Free，不删除已有内容。

---

## 9. 权益与额度数据模型

### 9.1 账号

```json
{
  "account_id": "acct_xxx",
  "status": "active",
  "identity_provider": "email|wechat|phone",
  "identity_hash": "...",
  "created_at": "ISO-8601",
  "updated_at": "ISO-8601"
}
```

### 9.2 订阅

```json
{
  "subscription_id": "sub_xxx",
  "account_id": "acct_xxx",
  "plan_code": "free|plus|pro",
  "status": "trialing|active|past_due|paused|canceled|expired",
  "billing_interval": "month|year",
  "period_start": "ISO-8601",
  "period_end": "ISO-8601",
  "source": "default|manual|wechat_pay|alipay",
  "updated_by": "system|internal_operator"
}
```

### 9.3 权益快照

```json
{
  "account_id": "acct_xxx",
  "plan_code": "plus",
  "strategy_cycle_limit": 4,
  "strategy_cycle_used": 2,
  "complete_content_limit": 12,
  "complete_content_used": 5,
  "generation_reserved": {"strategy_cycle": 1, "complete_content": 0},
  "active_project_limit": 3,
  "history_days": 365,
  "features": ["cycle_comparison", "platform_review", "basic_export"],
  "period_start": "ISO-8601",
  "period_end": "ISO-8601"
}
```

### 9.4 用量账本

```json
{
  "usage_id": "usage_xxx",
  "account_id": "acct_xxx",
  "client_id": "customer_xxx",
  "request_id": "idempotency_xxx",
  "usage_type": "initial_plan|next_round|regenerate|script|image|video",
  "customer_unit": "strategy_cycle|complete_content|video_package",
  "quantity": 1,
  "status": "reserved|consumed|released|compensated",
  "occurred_at": "ISO-8601",
  "reason": "delivered|provider_failed|manual_compensation"
}
```

### 9.5 存储边界

V1 继续使用现有 Netlify Blobs，在同一 store 中使用独立命名空间：

- `accounts/v1/<account_id>`
- `account-identities/v1/<identity_hash>`
- `account-client-links/v1/<account_id>/<client_id>`
- `subscriptions/v1/<account_id>`
- `entitlements/v1/<account_id>/<period>`
- `usage/v1/<account_id>/<period>/<usage_id>`
- `orders/v1/<account_id>/<order_id>`

不迁移、不重建、不删除现有 `global-project-store.<client_id>` 项目数据。

---

## 10. 后端权益判定

### 10.1 核心原则

- 套餐、额度和功能权限必须由后端判定，不得仅靠前端隐藏。
- 生成前必须先通过身份、订阅、额度、频率、成本闸和客户数据权限检查。
- 额度校验挂载在 `/api/plan-jobs` 和 `/api/customer-growth-advice` 真实模型生成之前。
- 后端在任何情况下不向客户端返回 API Key、模型内部元数据、计费成本或其他客户的权益记录。

### 10.2 生成前判定顺序

1. 验证客户访问凭证或账号会话。
2. 解析 `account_id`；匿名用户落到 Free 匿名权益。
3. 加载当前订阅和权益快照。
4. 检查活跃项目上限和功能权限。
5. 检查月度剩余额度。
6. 执行短窗口、IP 和每日防滥用限流。
7. 通过 `SAFE_TO_RUN` 成本总闸。
8. 以 `request_id` 建立额度预留并创建幂等任务。
9. 交付成功后扣减，失败后释放。

### 10.3 标准错误代码

| code | 客户文案 | 处理 |
|---|---|---|
| `authentication_required` | 登录后可继续使用当前套餐 | 打开登录/绑定 |
| `quota_exceeded` | 本月生成额度已用完 | 查看套餐 |
| `project_limit_reached` | 当前套餐的项目数已达上限 | 管理项目/升级 |
| `feature_not_in_plan` | 该功能未包含在当前套餐中 | 展示套餐差异 |
| `rate_limited` | 生成太频繁，稍等片刻再试 | 等待后重试 |
| `subscription_past_due` | 订阅续费未完成，请检查支付状态 | 打开订阅管理 |
| `generation_temporarily_unavailable` | 刚刚生成失败了，请稍后重试 | 不扣额度 |

---

## 11. 客户端产品体验

### 11.1 入口

- 默认首页仍以完成核心任务为主，不改成价格营销页。
- 导航或账号菜单增加“套餐与额度”入口。
- 未登录用户可先体验，在保存、跨端或升级时引导绑定。

### 11.2 额度展示

客户只需理解：

- 当前套餐名称。
- 本月策略周期和完整内容的已用 / 总量。
- 下次刷新日期。
- 当前操作消耗的是 1 轮策略周期还是 1 份完整内容。

不展示 Token、模型路由、供应商失败、provider attempt 或内部成本。

### 11.3 额度不足

- 不清空已填写的信息。
- 不跳转回首页。
- 不显示技术错误。
- 保留当前项目上下文，提供“查看套餐”和“继续记录数据”。
- 升级完成后可返回原任务继续生成。

### 11.4 套餐页

套餐页一屏内说清：

1. 三档适合谁。
2. 每月可用额度。
3. 可管理的项目数。
4. 最重要的 3–5 个权益差异。
5. 月付/年付价格与实际付款金额。
6. 人工代运营不在自助订阅内，可单独联系。

---

## 12. 开通与支付路径

### 12.1 Phase 3a：人工开通首发

在自动支付上线前，先跑通真实付费闭环：

1. 客户在套餐页选择 Plus 或 Pro。
2. 展示“联系开通”，进入企业微信、收款码或对公付款流程。
3. 运营确认到款后，在内部后台为指定 `account_id` 开通套餐。
4. 权益即时生效，并记录操作人、来源、订单号、周期和备注。
5. 非内部令牌无法修改任何账号的套餐。

### 12.2 Phase 3b：自动支付

后续接入：

- 微信支付：优先支持国内客户的扫码/网页支付。
- 支付宝：作为第二主渠道。
- 对公转账：保留企业客户的人工对账通道。

自动支付必须具备：

1. 服务端创建订单。
2. 签名过的支付回调。
3. 回调幂等。
4. 订单、支付、订阅和权益的一致性校验。
5. 续费、取消、退款、异常订单和对账流程。
6. 支付密钥、商户证书和回调秘钥全部使用服务端环境变量。

---

## 13. 后台运营能力

内部管理员需要以下最小能力：

- 按账号查看当前套餐、周期和用量。
- 查看客户的产品交付次数、供应商调用次数和 fallback 率。
- 人工开通、升级、降级、暂停和到期。
- 人工补偿额度，必须填写原因并保留审计日志。
- 查看漏斗：免费使用、额度用完、查看套餐、联系开通、成功开通、续费。
- 不在客户端显示内部模型、成本、毛利、调试和计量哈希。

---

## 14. 分析指标与商业验证

### 14.1 核心漏斗

1. 访问首页。
2. 开始填写。
3. 提交生成。
4. 成功获得结果。
5. 记录发布效果。
6. 进入下一轮。
7. 用完 Free 额度。
8. 查看套餐。
9. 发起升级。
10. 完成开通。
11. 续费或流失。

### 14.2 V1 定价评估指标

- 首次生成成功率。
- 从首次生成到记录效果的转化率。
- 进入第二轮的客户比例。
- Free 额度用完率与用完时间。
- Free → Plus，Plus → Pro 转化率。
- 每个付费账号的月均产品生成次数与供应商调用次数。
- 平均单次交付成本、套餐毛利和人工支持成本。
- 月续费率、取消率和付费客户的有效使用率。

### 14.3 调价规则

- V1 首发价格至少观察 30 天再调整。
- 不只根据模型 Token 成本定价，还要考虑客户价值、支持成本、渠道成本和毛利。
- 调整新价格或额度时，已付费订阅在当期内保持原权益。
- 价格和额度从环境变量或套餐配置读取，支持不发版调整。

---

## 15. 安全、隐私与合规

- 匿名项目、账号、订阅、订单、用量账本分属独立命名空间。
- 客户所有读写接口按自身会话隔离，不接受裸 `account_id` 或 `client_id` 作为授权证明。
- 内部人工开通、补偿和订阅修改必须经 `INTERNAL_ACCESS_TOKEN` 或未来的管理员身份认证。
- 客户响应继续经过脱敏层，不暴露模型、fallback、debug、内部代号和其他客户信息。
- 邮箱、手机号、OpenID、支付凭证、密钥和商户证书不进入 Git，不下发前端。
- 订阅、额度、人工操作和支付回调保留可审计日志。
- 套餐页、订单页、用户协议和隐私政策对价格、周期、自动续费、取消、退费、额度和数据保留规则保持一致。

---

## 16. 环境变量与可配置项

建议新增以下配置，具体命名可在实施时小幅调整：

```text
COMMERCIALIZATION_ENABLED=false
FREE_TRIAL_STRATEGY_CYCLES=3
FREE_TRIAL_VALID_DAYS=30
FREE_MONTHLY_STRATEGY_CYCLES=1
PLUS_MONTHLY_STRATEGY_CYCLES=4
PRO_MONTHLY_STRATEGY_CYCLES=12
FREE_MONTHLY_COMPLETE_CONTENT=0
PLUS_MONTHLY_COMPLETE_CONTENT=12
PRO_MONTHLY_COMPLETE_CONTENT=40
PRO_PUBLIC_SALES_ENABLED=false
FREE_DAILY_GENERATIONS=1
PLUS_DAILY_GENERATIONS=10
PRO_DAILY_GENERATIONS=30
FREE_ACTIVE_PROJECTS=1
PLUS_ACTIVE_PROJECTS=3
PRO_ACTIVE_PROJECTS=10
PLUS_MONTHLY_PRICE_CNY=299
PRO_MONTHLY_PRICE_CNY=899
PLUS_YEARLY_PRICE_CNY=2990
PRO_YEARLY_PRICE_CNY=8990
AUTH_PROVIDER=email
ACCOUNT_AUTH_ENABLED=false
ACCOUNT_AUTH_SECRET=
ACCOUNT_EMAIL_RESEND_SECONDS=60
ACCOUNT_EMAIL_DAILY_IP_MAX=20
EMAIL_PROVIDER=
EMAIL_FROM=
RESEND_API_KEY=
WECHAT_APP_ID=
WECHAT_APP_SECRET=
WECHAT_PAY_MCH_ID=
WECHAT_PAY_API_V3_KEY=
ALIPAY_APP_ID=
ALIPAY_PRIVATE_KEY=
ALIPAY_PUBLIC_KEY=
```

开发、预览与生产环境分开配置。功能未完成全部验收前，`COMMERCIALIZATION_ENABLED` 保持 `false`。

---

## 17. 分阶段交付计划

### P0：已有成本与安全底座

状态：已基本完成。

- 生成 reservation 和 `request_id` 幂等。
- 产品交付次数与供应商尝试次数分开计量。
- 客户/IP/每日限流与影子模式。
- `SAFE_TO_RUN` 真实模型成本闸。
- 基础商业漏斗事件。
- 客户访问凭证与云端项目隔离。

### P1：账号与数据找回

- 可插拔身份接口。
- 邮箱验证码 / Magic Link 首发实现。
- 账号会话、退出和凭证轮换。
- 匿名 `client_id` 安全绑定到 `account_id`。
- 跨设备找回项目。
- 不增加首屏强制登录墙。

### P2：套餐与额度

状态：v1.6.133 已完成权益、用量、幂等预留与客户套餐页地基；生产默认观察模式，待真实数据验证后再人工开启强制拦截。

- 订阅、权益快照和用量账本。
- Free / Plus / Pro 套餐配置。
- 分别计量“策略周期”和“完整内容”，不向客户展示 Token 或供应商成本。
- 生成前预留、成功扣减、失败释放；同一请求不得跨单位重复扣减。
- 月度刷新和每日防滥用限制。
- 客户端套餐页、用量展示和升级引导。
- 超额时保留已填数据和原任务上下文。
- Pro 在详细内容生产成本与稳定性验收完成前保持邀请开通。

### P3a：人工开通

- 内部账号列表与套餐操作。
- 人工开通、到期、升降级和补偿。
- 审计日志。
- 联系开通的第一批真实收费闭环。

### P3b：自动支付

- 微信支付、支付宝。
- 订单、签名回调、幂等、续费、取消和对账。
- 客户订阅管理与付费凭证。
- 监控支付异常和权益不一致。

### P4：增长与组织能力

- 微信登录、手机号登录。
- 团队成员、角色和项目权限。
- 加购包、企业套餐和渠道码。
- 专家复核、人工策略和 FP Matrix 增长服务升级。

---

## 18. 上线验收标准

### 18.1 P1

- 匿名用户不登录仍可完成当前试用流程。
- 绑定账号后换浏览器能找回授权项目。
- 不能通过修改 `account_id` 或 `client_id` 读取他人数据。
- 身份标识不明文落库。

### 18.2 P2

- Free / Plus / Pro 的额度和项目上限均从配置读取。
- 成功交付扣 1 次；重试、修复和轮询不重复扣减。
- 失败或平台降级不扣客户额度。
- 额度用完时显示友好升级引导，已有项目仍可查看和记录数据。
- 同一账号在两个设备上看到一致用量。

### 18.3 P3a

- 内部人工开通后客户权益立即更新。
- 任何套餐变更均有操作人、时间、原因和变更前后快照。
- 非内部身份无法修改套餐、补偿额度或查看全局商业数据。

### 18.4 全局回归

- 异步生成、轮询、多轮循环、反馈保存、云端同步和客户脱敏不回归。
- 客户页面不暴露模型、供应商、计量哈希、内部代号和管理操作。
- `/api/health` 始终免鉴权可用。
- 支付、邮件、微信、短信和模型凭证均不进入 Git。

---

## 19. 上线前需要用户提供的外部条件

### P1 账号阶段

- 邮件服务商账号。
- 发件域名、发件地址和 SPF / DKIM 验证。
- 若改用微信登录，需已认证公众号或开放平台应用及对应回调域名。

### P3b 支付阶段

- 微信支付商户号、API v3 密钥、商户证书和回调域名。
- 支付宝开放平台应用、应用私钥、支付宝公钥和回调地址。
- 对公收款信息、发票规则、退费规则和客服联系方式。

Codex 不代替用户注册外部商户账号，不自行生成或保存真实密钥。

---

## 20. 已确认事项与实施前置

已确认：

1. Plus 正式价格采用 ¥299 / 月、¥2,990 / 年。
2. Pro 目标价格采用 ¥899 / 月、¥8,990 / 年，详细内容生产验收前仅邀请开通。
3. Free 为一次性 3 轮新客体验，之后每月 1 轮基础策略；Plus / Pro 分别包含 4 / 12 轮策略周期。
4. 客户侧按“策略周期”和“完整内容”理解权益，不展示 Token、模型名称或复杂算力换算。
5. 人工审稿、专家复核、拍摄、剪辑和 FP Matrix 增长服务独立报价。

进入后续实施前仍需完成：

1. 确认 P1 邮箱服务商、发件域名和所需环境变量。
2. 对详细文本、图片和视频分别积累真实调用成本、失败率、重试率与交付时长，至少形成一批可审计样本后再开放 Pro 生产权益。
3. Pro 多成员协作是否首发，仍可延后至 P4，不阻塞 Plus 商业验证。

---

## 21. V1 成功标准

本商业化版本成功不以“支付按钮已上线”为标准，而以以下事实同时成立为标准：

1. 客户不登录可完整体验核心价值。
2. 绑定账号后可跨设备找回项目。
3. 每个账号的套餐、额度和项目权益在服务端可准确判定。
4. 同一请求不重复扣减，失败不误扣，所有变更可审计。
5. Free 用户能理解价值，额度用完时能理解为什么升级。
6. 运营可以不改代码调整价格和额度。
7. 第一批客户可通过人工开通完成真实付费，无需等待自动支付。
8. 获客罗盘订阅与 FP Matrix 人工增长服务的价格、责任和交付边界保持清晰。
