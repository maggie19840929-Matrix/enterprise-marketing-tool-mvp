# Codex 返工单:上一轮 P0 三项未达标(P1 已 OK,勿动)

> 上一轮只做了 P1(seed 扰动 / 重试按钮 / 门槛文案),**这些保留别动**。
> 下面 P0 三项没做到位,重做。给了钉死的数值,别再"淡绿换淡绿"。

---

## P0-1 绿色对比度(上轮把 `#8ff2cb` 换成了 `#bdf7df`,还是淡绿,对比度 1.01,等于没修)

**实测对比度(白底上叠绿 tint 背景,AA 要 ≥4.5):**
- `#bdf7df` 文字 / 绿底 = **1.01** ❌
- `#065f46` 文字 / 绿底 = **6.46** ✅ ← 用这个
- `#047857` 文字 / 绿底 = **4.61** ✅(最低可接受)

**改法:客户浅色作用域里,所有压在绿底上的绿色文字,前景色一律换成深绿墨 `#065f46`(别再用任何 `#bdf7df/#8ff2cb/#9ee5c*` 这类淡绿)。绿 tint 背景可保留。** 已确认的客户元素(在 `buildCustomerPlanList` / `buildCustomerSuggestion` 里渲染):
- `.plan-day` / `.plan-day-pill` / `.plan-select` —— `color:#bdf7df` → `#065f46`;
- 客户结果/计划/下一轮区里其余 `#bdf7df`(共 6 处)全部 → `#065f46`;
- 若某处绿底更深导致深绿字也不够,则该处文字用墨色 `#111827`。

**不要动内部深色页(war-room / `.war-tag.green` 的 `#8ff2cb`)** —— 那是深底,合理。

**自检(必须给):** 列出 plan-day-pill、计划卡标签、下一轮区 三处的 `前景色 / 实际背景色 / 对比度`,证明每处 ≥4.5。

---

## P0-2 记录效果表单瘦身(上轮 6 个指标还都在主表单)

- 客户记录表单**主区只留 3 项**:`views 曝光 / engagement 互动 / consultations 咨询`;
- `likes 点赞 / comments 评论 / shares 转发` **移进折叠的"更多(可选)"**,默认收起、可空。
- 三个核心字段 `name` 不变;隐藏项缺省填 0,**不得让 feedback 提交或下一轮 advice 报错**。
- (内部页的记录表单不用改。)

---

## P0-3 "填 3 项"与实际必填对齐(上轮平台/策略/最大问题仍是必填)

现状客户表单必填是 5 项:行业、目标、客群、**发布平台(*)**、**最大内容问题(*)**(content_mode 已有默认)。
**推荐做法:只让 前 3 项 必填,后两项给默认、可后补:**
- 发布平台:未选时默认按"还不确定 / 全部"或按行业猜,不拦生成;
- 最大内容问题:设默认值(如"发了没咨询")或移入可选,不拦生成。
- 这样 hero"填 3 项核心信息"才成立。若产品坚持要平台必选,则改 hero 文案为实际项数——二选一,别又留 5 项还写 3 项。

---

## 硬约束
- P1 三项(seed / 重试 / 门槛文案)已 OK,**不要回退**。
- 不回归:安全(plan-jobs 隔离、`/api/customers` 401、响应无模型元数据)、平台逻辑、多轮、异步、持久化、页脚/圆角等既有精修。
- 只改客户浅色作用域,内部深色页不动;`name`/`data-*`/`id` 钩子不重命名。
- `npm test` 通过;bump 版本号;控制台无报错。

## 验收
1. plan-day-pill 等三处对比度 ≥4.5(给色值+比值);客户页目视不再有"淡绿字看不清"。
2. 记录表单默认 3 项,可选 3 项折叠;填 3 项能提交、能进下一轮。
3. 客户表单仅 3 项必填即可生成(或 hero 文案已改为实际项数)。
4. 内部深色页绿色无变化;安全/平台/多轮/异步/持久化零回归;`npm test` 通过。
