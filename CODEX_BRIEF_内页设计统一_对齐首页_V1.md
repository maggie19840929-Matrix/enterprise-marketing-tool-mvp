# Codex 任务书:内页设计统一 —— 把首页精致风延伸到"结果/确认/计划"页

> **问题:** 首页(customer-first-screen:hero+表单)已是高端精修(发丝边、大留白、单色克制、无重阴影);
> 但**从第二步起的页面没跟上**——确认方向、结果建议、本轮内容计划、为什么这样发、记录效果、下一轮,
> 仍是老风格:绿 tint 色块、重阴影、高饱和绿/淡紫、"大绿块看着像按钮实则只是选项"。用户反馈"和主页完全不搭、瘆得慌"。
> **目标:让第二步之后的所有客户页,视觉语言与首页一致。** 只改样式与可点性暗示,不动功能逻辑。
> 入口:`static/war-room-v1.6.1.css`、`static/index.html`、`static/app.js`(仅渲染模板的 class/结构,不动事件逻辑)。

---

## 设计基准(直接沿用首页那套,别另发明)
- 卡片:**白底 + 0.5px 发丝边 + 极淡或无阴影**(和首页 `.customer-card` 精修后一致);圆角统一 16px。
- 颜色:**单色 + 中性灰为主**;绿色只做**极小点缀**(小图标/细边),**不再用绿 tint 大色块承载内容**。
- 文字:正文墨色 `#1f2937`;强调用深墨(绿 `#065f46` / 紫 `#312e81`),对比度 ≥ AA;**禁止 `#8f93ff`、`#8ff2cb`、`font-weight:850` 这类又淡又超粗的组合**。
- 间距:8px 体系,拉开留白。

---

## 已确认视觉参照(产品已拍板,照此落地)

> 下面是和产品对齐过的目标形态,逐条照做即可;整体"白底 + 发丝边 + 无阴影 + 单色克制 + 绿色只做极小点缀"。

**「确认方向」屏:**
- 顶部小标签"第 2 步 · 确认方向"(muted,12–13px,字母微间距);标题用衬线、字重 500、20px;副文案 secondary、14px。
- 三个方向做成**竖排的三选一行卡**(不是大色块):
  - **选中项**:`1.5px 深墨边 + 左侧实心深色圆点内含 ✓`;标题 15px/500,描述 13px secondary。
  - **未选项**:`0.5px 发丝边 + 左侧空心圆(border-strong)`。
  - 点选后选中态**必须一眼可见**地在选中/未选之间切换。
- 三选一下面放**一个独立的深色实心主按钮**(圆角胶囊,占满宽)"用这个方向生成内容 →"——这是唯一的"主行动",与"选择"在视觉上分开。

**「本轮内容计划」屏:**
- 每条计划一张**白底发丝边卡(0.5px,圆角 10–12px,无阴影)**:
  - 日期编号 = **发丝边圆圈内墨色数字**(26px 圆,secondary 数字),**不要绿色块**;
  - 平台标签 = **低调细边灰胶囊**(secondary 文字、0.5px 边),不要高饱和绿底;
  - 选题 15px/500;角度 13px secondary;
  - "为什么这样发" = 卡内底部**一行低调可展开**(顶部一条发丝分隔线 + secondary 文字 + `ti-chevron-down`),**常规字重、墨色**,不要 `#8f93ff`/850 超粗。
- 底部"查看全部 7 天 →"用安静的 secondary 文本链接。

**全局:** 字重只用 400/500(禁 700/850);强调色至多一个;绿色仅作极小点缀,不承载大色块;圆角统一 10–16px;间距走 8px。

---

## P0 —— 修"大绿块假按钮"(用户实际卡住的地方)

第二步"确认方向"的三张卡(`.customer-direction-card`,渲染在 `app.js` 的 co-creation,值=痛点型/信任型/转化型):
- **它们能点(选其一),但长得像三个大号绿色 CTA,选中态又太弱,用户以为点不了。**
- 改成**明确的"多选一选择器"外观**,不是大 CTA:
  - 缩小视觉份量,做成**分段/选项卡样式**(细边卡片,不是绿色实块);
  - **选中态必须强而明确**:选中项用**实心深色边/背景 + 一个勾选标记(✓)**,未选为浅色发丝边——一点就明显变化;
  - 卡上加一句极小提示"选一个方向"或用 radio 语义;
- 保留**唯一的主 CTA**(如"用这个方向生成/继续")做真正的"主行动",让"选择"和"行动"在视觉上分开。
- **不改点击逻辑/`data-cocreation-direction` 钩子/事件绑定**,只改 class 与样式、加强选中态。

---

## P1 —— 内页视觉对齐首页

1. **结果/建议页(`buildCustomerSuggestion`)**:平台矩阵、内容模式、"3 个选题"列表里的绿 tint 块、编号"一二三"——去绿块化,改中性卡 + 发丝边;编号做成**安静的数字**(墨色小圆点/细边),不是绿色块。
2. **本轮内容计划(`buildCustomerPlanList` / `.customer-plan-item`)**:去掉绿 tint 底 `rgba(16,185,129,.08)` 和重阴影,改白底发丝边;`.plan-day` 保持已修好的深绿字即可,但底色淡化到中性。
3. **"为什么这样发"(`.customer-why`)**:标题 `#8f93ff` + `font-weight:850` → 改墨色(`#1f2937`)常规字重(600 左右);去掉/减弱阴影;整体做成低调的"展开看依据",不抢视觉。
4. **记录效果 / 下一轮**:同样对齐——白底发丝边、单色、无重阴影。
5. **全内页扫一遍 `box-shadow`**:客户内页凡是首页已弃用的重阴影,一律降到发丝边 + `0 1px 2px rgba(0,0,0,.04)` 或无。

---

## 硬约束(零回归)
- **只改视觉与可点性暗示**;不动:方向选择逻辑、计划渲染、平台逻辑、多轮、异步 plan-jobs、持久化、安全(401/隔离/元数据剥离)、已修好的对比度成果。
- 不重命名/删除 `data-*`、`name`、`id`、事件绑定的钩子;direction-card 的点击仍工作。
- 只动客户浅色页,**内部深色页(war-room)不动**。
- 所有文字对比度 ≥ AA;`npm test` 通过;bump 版本号并部署。

## 验收
1. 第二步三张方向卡:一眼看出是"多选一",点选有明显选中态(✓/实心);另有独立主 CTA 做"继续/生成"。
2. 结果/计划/why/记录/下一轮:白底发丝边、单色克制、无刺眼绿块与重阴影,与首页一致(给关键几屏的截图或类名对照)。
3. 关键文字对比度 ≥4.5(给色值)。
4. 功能/安全/多轮/异步/持久化零回归;`npm test` 通过;内部深色页无变化。
