# Codex 任务书:内测 → 收费 商业化落地(套餐 + 额度,分阶段)

> **背景:** 核心产品(生成质量、增长闭环、安全、异步性能、UI)已就绪;缺的是"收费依赖的商业化层"——身份、计量、成本控制。
> **方案定调(已拍板):用「套餐 + 额度」,不做虚拟货币**(取代旧"算力豆"方案)。计价单位对外就叫"次数/额度",不造币。
> **分阶段做,内测不中断**;付款集成(最重、依赖外部)放最后,前面先跑通,必要时先人工开通收款。
> 代码:`netlify/functions/api.mjs`、`static/app.js`、`static/index.html`。每阶段独立部署、bump 版本。

---

## 决策(已拍板,照此做)

1. **套餐:三档 `Free / Plus / Pro`(学 Claude/GPT)**,UI 用中英双标「免费版 / 专业版 Plus / 旗舰版 Pro」。
   - **差异主轴 = 每月生成次数额度**(Free 极少 / Plus 中 / Pro 高);
   - **高成本功能(视频生成等)锁到 Pro**(视频火山成本高,做 Pro 的价值锚点);
   - **具体次数与价格全部 env 可配**,内测埋点数据(Phase 0)出来再定值——别硬编码。
2. **身份:先接「邮箱」跑通闭环(依赖最轻、无资质等待),微信/手机号做可插拔预留、放量前补上**。
   - **MVP 先接邮箱**(magic-link 或 6 位验证码;仅需域名 DKIM/SPF 验证 + 邮件服务商,`fpmatrix.cn` 已有邮箱基础);
   - 身份层**必须做成可插拔**,`微信登录(公众号网页授权 / 开放平台扫码,需尚下联资质)` 与 `手机号+短信验证码(阿里云短信)` 预留接口,资质就绪后接入;
   - **定位说明**:邮箱用于先验证"愿不愿付费"的商业闭环;国内老板邮箱转化偏低,**正式放量前补微信登录**。
3. **收款方式**:微信支付 / 支付宝(**Phase 3 才集成**;商户号由用户开通)。
4. **限流阈值**:每客户每日生成上限,初值 **30 次/日/客户**,env 可调。

---

## Phase 0 —— 成本止血 + 埋点(本周可上,不依赖身份)

**目的:内测放量前先别让火山成本裸奔,同时开始收集定价数据。与账号解耦,先做。**

### 0-1 生成接口限流 + 成本闸
- `/api/plan-jobs`(及其它触发火山付费生成的入口)加:**按 client_id + IP 限流**(短窗口节流 + 每日上限,阈值走 env),超限返回明确的 `429 / rate_limited`,前端提示"今天生成次数已达上限"。
- 保留/接上 `SAFE_TO_RUN` 成本闸;记录每次生成的成本量(次数即可,后续接账单)。
- **不影响正常单个客户的正常使用**;只挡异常高频。

### 0-2 轻量隐私友好埋点
- 接**自托管、无 cookie**的分析(Plausible / Umami 自托管;或一个极简 `/api/track` 事件日志入库 blobs)。
- 埋这几个关键漏斗事件(先够定价用):`访问首页 → 开始填写 → 提交生成 → 生成成功/失败(含是否走 rule_template 兜底) → 记录效果 → 进入下一轮`。
- **隐私红线**:不采集 PII、不把业务内容/手机号放进 URL 或第三方;事件只记类型+时间+匿名 client_id。

---

## Phase 1 —— 身份 + 数据找回(收费的地基)

**目的:让付费数据不再被单个浏览器 localStorage 绑架——清缓存/换设备能找回。**

- 做**可插拔身份层,先接邮箱**(magic-link 或验证码,依赖最轻),微信登录/手机号验证码**预留接口、放量前补**;首次"保存/绑定"时创建账号。
- **服务端账号记录**(blobs,键含 account_id):`{account_id, 身份标识(手机号hash/openid), created_at, plan, quota_used, period_start}`;身份标识**加密/hash 存**,不明文落库。
- **数据迁移**:绑定时把当前浏览器 localStorage 的 client_id 数据**关联到账号**,之后按 account 读写;跨设备登录能拉回。
- **匿名试用仍保留**:未绑定可继续用(内测老用户不能被强制登录墙拦死),但提示"绑定后数据才不会丢"。
- **依赖提醒**:短信/邮件需服务商 + 域名/签名审核;微信需公众号资质。这些**要你提供账号/资质**,Codex 只接口对接。

---

## Phase 2 —— 套餐 + 额度(计量与拦截)

- 套餐与额度**全部 env 可配**(层级、价格展示、每月次数),别硬编码。
- **服务端按账号按周期计量**:每次生成成功 +1 usage;`/api/plan-jobs` 生成前校验 `quota_used < plan.limit`,超额返回 `quota_exceeded`(**不是崩溃**)。
- 前端:
  - 顶部/结果页显示 **"本月已用 X / Y 次"**;
  - 超额时弹**升级引导**(而不是报错),给"查看套餐"入口;
  - 免费额度用完的引导文案要体面,不吓人。
- **匿名用户**:按 client_id 给一个很小的试用额度(如 3 次),用完提示绑定/升级。

---

## Phase 3 —— 收款

- **3a(MVP,先能收钱,不等集成):人工开通** —— 用户点"升级"→ 走你的收款码/对公 → 你在**内部后台(INTERNAL_ACCESS_TOKEN 保护)手动把该账号 plan 改档**。够撑第一批付费客户,不阻塞收入。
- **3b(随后):微信/支付宝集成** —— 下单 + 回调 webhook 自动改档 + 对账。较重,依赖商户号,单独排期。
- **红线**:支付密钥/商户证书走 env、不入 git;任何涉及真实收款凭据的操作由**你**完成配置,Codex 只写对接逻辑。

---

## 全局硬约束(零回归)

1. **不破坏内测老用户**:匿名 localStorage 流程、异步 `/api/plan-jobs`、增长闭环、多轮、既有 UI/可读性成果全部保留。
2. **不破坏安全**:账号/额度接口**按自身身份自我隔离**(读不到别人账号);内部人工开通走 `INTERNAL_ACCESS_TOKEN`;不重开 `/api/customers` 等枚举面;客户响应仍无模型元数据。
3. 所有密钥/凭据(短信、微信、支付、分析)走 **env,不入 git、不进前端打包**。
4. 隐私:身份标识 hash 存;埋点无 PII;不把手机号/业务内容放 URL。
5. **数字先 env 化**(额度、价格、限流阈值),等内测数据再定值。
6. `npm test` 通过;`/api/health` 免鉴权 200;每阶段独立部署可回滚。

## 各阶段验收
- **P0**:匿名脚本狂刷 `/api/plan-jobs` → 触发 429/每日上限;埋点能看到"提交→成功/失败"计数;正常单用户不受影响。
- **P1**:绑定后换浏览器/清缓存能找回数据;身份标识非明文;匿名仍可试用;跨账号读不到别人数据(curl 自证)。
- **P2**:免费额度用完 → `quota_exceeded` + 升级引导(非崩溃);"已用 X/Y"显示正确;env 改额度即时生效。
- **P3a**:内部后台手动改档后,该账号额度/权限即时生效;非内部令牌改不了别人套餐。
- 全程安全/异步/多轮/持久化零回归;`npm test` 通过。

---

## 一句话给 Codex
**先做 Phase 0(限流+成本闸+埋点,止血且收数据),再做 Phase 1(可插拔身份+数据找回)→ Phase 2(套餐额度计量拦截,数字 env 可配)→ Phase 3a(内部手动开通先收钱)。全程不加登录墙拦死匿名内测、不破安全、凭据走 env。付款自动化(3b)最后单独排。**
