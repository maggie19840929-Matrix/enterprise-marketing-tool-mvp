# 区块清点与归屏映射 V1

> 把现有页面的每个区块逐个清点,标注「做什么 / 谁用 / 频率」,再归到优化后的「客户端 3 步」和「内部端 6 Tab」。供 UI 同事和后续开发对齐。
> 来源:`static/index.html` + `static/app.js`(35 个 render 函数)。

---

## 一、客户端 `/`(customerApp)→ 3 步引导流程

| 现区块(id) | 做什么 | 角色 | 频率 | 归到 |
|---|---|---|---|---|
| customerHero | 标题/定位 | 客户 | 常驻 | 顶部 |
| customerProgressStrip | 操作进度条 | 客户 | 常驻 | 贯穿 3 步 |
| **customerFormCard**(12 字段) | 录入业务/目标/客群/产品/区域/可约时间/补充/资质/常问/素材/对标… | 客户 | 开局 1 次 | **步骤①** |
| customerResultSection | 「你的专属发布建议」内容方向+结果 | 客户 | 生成后看 | **步骤②** |
| customerPlanBlock / PlanList / WhyBox | 「这 7 天可以这样安排」7 天计划 + 理由 | 客户 | 生成后看 | **步骤②** |
| customerEffectSection / EffectList | 「发完后回来填」记录已发布内容效果 | 客户 | 发布后回填 | **步骤③** |

**3 步:** ① 极简录入(3 必填 + 可选折叠) → ② 方案·主行动「选一条去发」 → ③ 记录效果/复盘
**首屏砍字段:** 必填仅 `行业/业务`、`目标`、`目标客户`;其余(产品、区域、可约时间、资质、常问、素材、对标)折叠进"补充更多(可选)",能预填先预填。

---

## 二、内部端 `/internal`(internalApp,12 区块)→ 6 Tab

| 现区块(id) | 做什么 | 角色 | 频率 | 归到 Tab |
|---|---|---|---|---|
| lifecycleWorkbench | 生命周期/阶段总览面板 | 运营/admin | 每日 | **A 概览/首页** |
| metricCards | 指标卡(曝光/互动/咨询等汇总) | 运营/admin | 每日 | **A 概览/首页** |
| diagnosisWorkflow | 录入 5 问 + AI 理解(aiIntakeUnderstanding)+ 可选对标 | 运营 | 每客户开局 | **B 诊断与策略** |
| (side)「内测主流程」 | 流程引导/说明 | 运营 | 常驻辅助 | **B 诊断与策略** |
| internalResultSection / latestDiagnosis | 「内测生成结果」内容方向+诊断 | 运营 | 诊断后 | **B 诊断与策略** |
| planSection(plansSummary + plansBody) | 前三动作 + 完整 7 天计划 | 运营 | 计划阶段 | **C 内容计划** |
| generationWorkbench ①登记/上传素材(assetList/Picker) | 素材库 + sha256 + 选素材 | 运营/外包/(未来自助客户) | 生产时 | **D 素材生成工作台** |
| generationWorkbench ②创建生成任务(表单/模型选择) | 选项目/客户/计划+类型+模型+需求 | 运营/(未来自助) | 生产时 | **D 素材生成工作台** |
| generationWorkbench ③任务状态与内部验收(taskList) | 状态机 + QA 验收 | admin(QA) | 验收时 | **E 验收与交付** |
| generationWorkbench ④客户交付区(clientDelivery) | 仅 QA passed 的成品 | **client_viewer** | 交付/客户看 | **E 验收与交付** |
| generationWorkbench ⑤飞书回写 Adapter Mock | 回写结构预览 | admin | 协作回写 | **E 验收与交付** |
| feedbackHint / feedbackWorkflow(A~F) | 回填:样本池/输入/记录/复盘判断/依据/下一轮 | 运营 | 发布后 | **F 数据与复盘** |
| refillCockpit | 回填驾驶舱 | 运营 | 发布后 | **F 数据与复盘** |
| reviewBox / feedbackList / reviewEvidenceBox | 周复盘 + 回填记录 + 证据汇总 | 运营/admin | 复盘时 | **F 数据与复盘** |
| clientSnapshot | 客户数据快照 | 运营/admin | 复盘时 | **F 数据与复盘** |
| nextSevenDataPage | 「下一个七天数据」 | 运营 | 下一轮 | **F 数据与复盘** |

---

## 三、6 Tab = 项目闭环 6 阶段(IA 自解释)

```
A 概览/首页  →  B 诊断与策略  →  C 内容计划  →  D 素材生成  →  E 验收与交付  →  F 数据与复盘
   (仪表盘)      (录入+诊断)      (7天计划)      (素材+建任务)   (QA+客户交付)    (回填+复盘+下一轮)
                                                                                      └──→ 回到 B 下一轮
```

这正是原始规划的闭环:项目/客户/计划 → 素材 → 生产 → QA → 交付 → 数据回流 → 下一轮。**Tab 顺序 = 工作流顺序**,用户不用学。

---

## 四、角色映射(天然对接后续权限隔离)

| Tab | internal_admin | 运营 | outsourced 外包 | client_viewer 客户 |
|---|---|---|---|---|
| A 概览 | ✅ | ✅ | — | — |
| B 诊断策略 | ✅ | ✅ | — | — |
| C 内容计划 | ✅ | ✅ | 只读分配项 | — |
| D 素材生成 | ✅ | ✅ | ✅(分配任务) | (未来自助) |
| E 验收与交付 | ✅(QA) | 提交 | — | **只看客户交付区** |
| F 数据复盘 | ✅ | ✅ | — | — |

> 拆 Tab 不只是美化——「哪个角色进哪个 Tab」拆完就自然成立,后面做权限隔离、算力豆自助直接复用这套分区。

---

## 五、关键发现 & 清理项

1. **内部页 = 客户旅程副本 + 生产工作台 + 数据**,三件大事压在一条滚动里 → 这是"信息太多"的根因。
2. **生产 vs 验收必须拆**:现在 generationWorkbench 把「上传素材+建任务」(生产)和「QA+客户交付」(验收)揉成一个巨块 → 拆成 Tab D / Tab E,也对应不同角色。
3. **清理项**:
   - 两个几乎相同的 hero 标题(「企业内容增长助手」/「多平台内容增长助手」)需统一。
   - 废弃目录 `static/internal(内测版)`、`static/internal(内测)`(此前已标注,确认无引用后删)。
4. **客户录入 12 字段 → 3 必填**,是 P0 速赢里收益最大的一项。

---

## 六、落地优先级(承接上一版框架)

| 优先级 | 动作 |
|---|---|
| **P0 速赢** | 客户录入砍到 3 必填+折叠;调试/QA 证据默认折叠;统一视觉层级与 hero 标题 |
| **P1** | 客户端改 3 步引导流程 |
| **P2** | 内部端按本表拆成 6 Tab 工作区(同时为权限隔离/算力豆自助打地基) |
