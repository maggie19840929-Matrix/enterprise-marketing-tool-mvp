# Codex 开发任务书:对标账号参考(手填)强化 V1

> 目标:把"对标账号参考"从**埋在客户版"补充更多"里的一个大文本框**,升级为**首页可见、结构化、有引导**的输入,并**确保它真的喂进火山生成、影响出来的选题**。
> **纯手填,零爬取、零外部调用**(爬取的合规/技术风险见另附 API 调研结论)。不阻断生成(可选增强,不填也能出内容)。

---

## 0. 现状(已核实)

- 后端**已有对标处理**:`normalizeBenchmark`(解析 benchmark_accounts/platform/notes/sample_content)、`benchmark_reference`(带"不照抄标题/封面/脚本/人设"规则)、并把 `benchmark_signal: diagnosis.benchmark_reference?.recent_topics?.slice(0,3)` 喂进 `arkContentPlanPrompt`。
- 但**客户版表单**只有一个 `best_recent_content` 文本框(埋在"补充更多(可选)"里);**结构化的对标块(benchmark-block:平台/账号/参考点)只存在于内部表单**。
- 需要确认并保证:客户手填的对标**代表作标题**能变成 `recent_topics` → `benchmark_signal` → 真正影响火山生成。

---

## 1. 要做的

### A. 客户版首页加一个"对标参考"结构化块(可选、可折叠但显眼)
放在首页"平台/策略"之后、"补充更多(可选)"之前,或作为一个默认收起但有明确引导的块。字段:
- **对标平台**(下拉,与发布平台一致)
- **对标账号**(1-3 个,链接或名称,换行/逗号分隔)—— 复用 `benchmark_accounts`
- **对标代表作标题**(粘贴 2-5 条对方收藏/咨询高的标题)—— 这是关键,复用/新增 `benchmark_sample_content` 或 `benchmark_titles`
- **你想参考它什么**(痛点表达 / 标题结构 / 信任证据 / 高收藏主题)—— 复用 `benchmark_notes`
- 引导文案:如"填了对标,生成的选题会更贴近你赛道已验证的打法(我们只借鉴主题和结构,不照抄标题/封面/人设)"。

### B. 打通数据链路(核心,必须验证)
- 确保客户填的**对标代表作标题**被解析进 `benchmark_reference.recent_topics`(现在 `benchmark_signal` 取的就是它);
- 确保 `arkContentPlanPrompt` 里 `benchmark_signal` 非空时,火山生成的选题**明显参考了对标主题结构**(但不照抄)。
- 若当前 `recent_topics` 只从内部/seed 来、没接客户手填 → 修通它。

### C. 保留并强化"不照抄"护栏
`benchmark_reference` 已有规则(不照抄标题/封面/脚本、不套人设),保留;并在提示词里明确"只迁移主题和结构,转译成本客户服务场景,禁止复制原文"。

---

## 2. 硬约束
1. **纯手填,禁止任何爬取/第三方数据抓取/外部账号数据调用**。
2. 对标是**可选增强,不得阻断生成**(不填也能正常出 7 天计划)。
3. 只动**客户版(client_viewer)**首页表单 + 打通数据链路;内部版不回归。
4. 客户视图零内部代号泄露;`npm test` 通过;`/api/health` 正常;可部署 Netlify。
5. 不引入新依赖/新框架。

---

## 3. 验收材料
1. 客户版首页能看到结构化"对标参考"块(平台/账号/代表作标题/参考点)。
2. **数据链路证据**:填入几条对标标题 → 生成的 7 天选题与"不填对标"时**明显不同、且贴合对标主题结构**(贴两次生成对比)。
3. 不填对标时仍能正常生成(不阻断)。
4. `npm test`、`/api/health`、客户视图零泄露、控制台无报错。

---

## 4. 部署
可部署到现 Netlify site(`f0efe912-abbc-4362-afdc-c9e513cb986c`)。
