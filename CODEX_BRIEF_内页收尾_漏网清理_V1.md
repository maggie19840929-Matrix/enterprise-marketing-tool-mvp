# Codex 内页收尾清理单(4 处漏网,都很小)

> 内页重设计主体已达标(确认方向卡、"为什么这样发"、记录/计划步骤都对了)。以下 4 处没跟上同一套规则。
> 只改客户浅色作用域样式;**内部深色页(war-room / `#8ff2cb` 等)不动**。改完 bump 版本号并部署。

## 1. 漏改的淡紫 kicker
`.customer-preview-kicker` 仍是 `color:#8f93ff`(白底 **2.70:1**,不达 AA)。
→ 换成深墨 `#1f2937`(或与其它 kicker 一致的 `#63666d`,≥4.5)。

## 2. inline-link 未纳入重设计(可能隐形/或已废弃)
`.customer-inline-link` 还是老样式(`#8f93ff` + `font-weight:850` + 虚线胶囊),且 `.customer-mode .customer-inline-link` 覆盖后**生效色算出来是 `#fff`**——若压在白底上会看不见。
→ 先确认它**是否还在渲染**(在 `static/app.js` 里 grep `customer-inline-link`,似乎已无引用)。
  - 若**已废弃**:直接删掉相关 CSS,别留隐形元素。
  - 若**仍在用**:统一成新风格——墨色文字(`#1f2937`)、常规字重(500)、实线发丝边(非虚线)、确保对比度 ≥4.5,别 `#fff` 压白底。

## 3. 计划卡选中态仍泛绿
`.customer-plan-item.is-selected` 仍 `background:rgba(16,185,129,.08)` + `border-color:rgba(16,185,129,.58)`(绿),没在客户作用域盖掉,和"去绿块"不一致。
→ 客户作用域改中性:选中态用 **墨色发丝边(如 `1.5px solid #111827`)+ 白底或极浅中性底**,不要绿 tint。与"确认方向卡"选中态保持同一套视觉。

## 4. 次要:两处灰字略低于 AA
`.customer-section-head p` / `.customer-plan-head p` = `#777b83`(**4.25:1**,小字略低)。
→ 加深到 `#667085`(≈5.7:1)或更深,保证小字也 ≥4.5。

---

## 硬约束
- 只改客户浅色作用域;内部深色页不动。
- 不回归:安全(401/隔离/元数据剥离)、平台、多轮、异步 plan-jobs、持久化、已完成的内页重设计与对比度成果。
- `name`/`data-*`/`id` 钩子不动;`npm test` 通过。

## 验收(给数值)
1. `.customer-preview-kicker` 对比度 ≥4.5(给色值+比值)。
2. inline-link:已删 或 已达 ≥4.5 且非 `#fff` 压白底(说明选了哪种)。
3. 计划卡选中态无绿 tint,改中性墨边(给最终色值)。
4. 两处灰字 ≥4.5。
5. 客户区 grep `#8f93ff` 在**可见文字**上为 0;内部深色页无变化;`npm test` 通过。
