# Codex 任务书:上线后优化 —— 可读性 + 核心循环体验 V1

> 产品已可发布,本单为体验精修。**按 P0→P2 优先级做,P0 先上。**
> 全程不得回归:安全整改(plan-jobs 自我隔离、`/api/customers` 等 401、客户响应无模型元数据)、
> 平台逻辑(planPlatforms)、多轮生成、异步 `/api/plan-jobs` 流程、blobs 持久化、之前的 UI 精修(页脚/发丝边/圆角)。
> 入口:`static/index.html`、`static/war-room-v1.6.1.css`、`static/app.js`。改完 bump 资源版本号并部署。

---

# P0 —— 立刻做(可读性 + 保护核心循环)

## P0-1 绿色文字/色块对比度(用户实测"浅绿字看不清")
**根因:深色主题的浅薄荷绿被用到了客户浅色页。** 具体元凶:
- `.war-tag.green { color:#8ff2cb }` —— 薄荷绿字,浅底上几乎看不见;
- `.customer-brief-badges span` —— 边框 `rgba(143,242,203,.22)` + 绿底,同样低对比;
- `.plan-day-pill { background:rgba(16,185,129,.18) }` 及一批 `rgba(16,185,129,.08~.22)` 绿色块——若其上文字也是绿/浅色,双低对比。

**改法(目标 WCAG AA:正文 ≥4.5:1,大字 ≥3:1):**
- 客户浅色页里凡是**绿色文字**(尤其 `#8ff2cb` / `#a7f3d0` 这类浅薄荷),一律换成**深绿墨**(建议 `#065f46` 或 `#047857`)或直接用正文墨色 `#1f2937`;绿色只保留在**边框/小图标/背景 tint** 上做点缀,不承担正文可读性。
- **绿色块上的文字**统一改深墨色,杜绝"绿字压绿块"。绿 tint 背景本身可保留(`rgba(16,185,129,.08~.12)` 作为淡底),但把 tint 上的文字提到深色。
- `.war-tag.green` 若客户页在用,给客户作用域覆盖:`color:#065f46; background:rgba(16,185,129,.12); border-color:rgba(16,185,129,.35)`。
- **自检**:改完对 生成结果页(customerResult)、计划卡(customer-plan-item / plan-day-pill)、badges、下一轮建议区 逐块量对比度 ≥ AA,给出关键几处的前景/背景色值。
- **别动内部深色主题(/internal war-room)** 里 `#8ff2cb` 等——那是深底,本就合理;只改客户浅色作用域。

## P0-2 记录效果表单:6 个指标 → 3 个核心(保护"循环")
现状记录表单必填/罗列 `曝光 views / 互动 engagement / 咨询 consultations / 点赞 likes / 评论 comments / 转发 shares` 六项。老板懒得填 → 闭环断 → 多轮生成没人用。
- 主表单只保留 **曝光 / 互动 / 咨询** 三项(咨询=转化,最关键);`likes/comments/shares` 折进"更多(可选)"、默认可空。
- **约束**:`name="views|engagement|consultations"` 三个钩子不变;隐藏项缺省填空/0,**不得让 feedback 提交或下一轮 advice 逻辑因缺字段报错**(advice/readiness 本就主要看 views/engagement/consultations)。

## P0-3 "填 3 项"文案与实际必填对齐
hero 说"填 3 项核心信息",但平台(*)、最大内容问题(*)也是必填,实际要主动填 5 项。**承诺与现实不符,伤第一印象。**
- 二选一(推荐前者):**真的只让 3 项必填** —— 平台给智能默认(如按行业猜或"还不确定"占位)、"最大问题"设默认或移到可选,允许先生成再补;或者把文案改成实际项数。
- 与"首屏轻、门槛低"的精致方向一致,倾向"只必填 3 项、其余智能默认可后补"。

---

# P1 —— 顺手做(质量 / 体验)

## P1-1 同赛道内容雷同
同输入连打两次选题完全一样(低温确定性)。同行业多客户会拿到近似内容。
- 给计划生成加轻度随机(温度略升 / 传入 seed / 加时间或 client 维度扰动),在**不降质**前提下让不同客户/不同轮次有差异。

## P1-2 生成失败一键重试
异步超时/失败现在只弹提示(如"生成时间较长,请稍后再试一次"),用户得自己重走。
- 失败态加**"重试"按钮**,点击复用上次 payload 重新提交,别让人以为白填。

## P1-3 下一轮门槛措辞改鼓励式
现状"再记录 X 条不同内容后,才会开放…"像限制。
- 改成鼓励式:"再记录 X 条,就能解锁更准的下一轮建议"。逻辑门槛不变,只改文案。

---

# P2 —— 需产品判断 / 真机确认

## P2-1 "确认方向"步骤(step2 co-creation)
它在"提交"和"看到计划"之间加了一步。
- 判断它是"共创让内容更准"的价值步,还是多余摩擦。若价值不明显,考虑**默认跳过、想调再展开**(把它变成可选的"微调方向",而非必经)。这条**请先和产品确认再动**。

## P2-2 移动端
CSS 有 35 个断点、覆盖较全,但发现一个 `max-width:180px` 的可疑断点(疑似笔误)。
- 核对该断点是否写错;并在**真机**过一眼首屏 + 记录表单的排版(这条自动化测不了)。

---

## 硬约束(全单通用)
- 不回归:安全整改、平台逻辑、多轮、异步 plan-jobs、持久化、既有 UI 精修。
- 不重命名 JS 依赖的 `name` / `data-*` / `id` 钩子;记录表单三个核心字段名不变。
- 只在**客户浅色作用域**改绿色,内部深色主题不动。
- `npm test` 通过;`/api/health` 免令牌 200;控制台无报错;bump 版本号。

## 验收
1. **P0-1**:生成结果页/计划卡/badges 关键文字对比度 ≥ AA(给色值);内部深色页绿色无变化。
2. **P0-2**:记录表单默认只 3 项;填 3 项能正常提交、能推进到下一轮;隐藏项为空不报错。
3. **P0-3**:仅 3 项必填即可生成(或文案已改为实际项数)。
4. P1:同输入两次选题有差异且不降质;失败有重试按钮;门槛文案已改。
5. 全程安全/平台/多轮/持久化零回归;`npm test` 通过。
